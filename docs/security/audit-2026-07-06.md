# Security Audit — parallax-workflows

AUDIT_DATE: 2026-07-06
AUDIT_COMMIT: 830e8bc579772b4e64c7b5f7c85434598fa52991
PROJECT_TYPE: MCP
THREAT_MODEL: absent

## Scope Note

Branch under audit: `fix/gate-agent-claude` (HEAD 830e8bc, 1 commit ahead of
`origin/main` @ 3a09f1d). Verified via `git diff origin/main --stat`: the ONLY
change vs origin/main is `.no-mistakes.yaml` (16 lines, 10 insertions / 6
deletions) — repointing the repo's local gate agent from `acp:gemini` to
`claude` after the Google account behind `acpx` was disabled for ToS violation
(programmatic Gemini access). No application code, skill, eval spec, or test
file differs from origin/main.

`origin/main` @ 3a09f1d is the exact commit audited on 2026-06-25
(`docs/security/audit-2026-06-25.md`, CRITICAL_FAILS=0, PASS=4/FAIL=0/NA=8) —
the audit-refresh commit (0421c8f) and the white-label-stock-report feature
merge (3a09f1d) landed together that day and origin/main has not moved since.
This audit independently re-ran the SEC-01..12 probes against the current
tracked tree (not merely carried forward from the prior report) and confirms
the 2026-06-25 findings hold unchanged, then separately evaluated the
`.no-mistakes.yaml` delta on its own merits.

Note: a separate, broader Sonnet security sweep also ran earlier today
(2026-07-06) over the `house-view` branch — a SUPERSET diff containing
different application code (parallax-judge/make/stress-house-view skills).
That sweep is out of scope for this audit; this report covers only
`fix/gate-agent-claude` as diffed against `origin/main` above.

## Checklist Results

SEC-01 PRIVACY_POSTURE: NA
  evidence: No user data collected. Repo is a public skill pack (Markdown +
  Python helpers) with no server, no DB, no user accounts, no data storage.
  `.no-mistakes.yaml` is a local CI/gate config comment change only — no new
  data-collection surface. Unchanged from 2026-06-25 baseline (re-verified).

SEC-02 RLS_COVERAGE: NA
  evidence: No Supabase or Postgres connection in any tracked surface.
  `grep -rl "supabase|create policy|enable row level security" --include=*.ts
  --include=*.js --include=*.py --include=*.sql .` returns 0 matches (re-run
  fresh this audit). No database layer in `.no-mistakes.yaml` or elsewhere.
  RLS remains upstream Parallax MCP server responsibility. NA unchanged.

SEC-03 AUTH_EDGE_CASES: NA
  evidence: No auth surface. `grep -rl "login|signin|password.*reset|signup"`
  across .ts/.tsx/.js/.jsx/.py returns 9 files, all substring matches of
  "signin" inside "signing" in `skills/_parallax/house-view/{manifest_cache,
  manifest_verify,chain_emit}.py` and their tests — Ed25519 audit-trail
  signing, not user authentication (same false-positive set noted in prior
  audits). `.no-mistakes.yaml` introduces no auth surface. NA unchanged.

SEC-04 SERVER_SIDE_VALIDATION: NA
  evidence: No HTTP API server. `grep -rl "req\.body|request\.json|
  request\.form" api/ server/ pages/api/ app/api/` returns 0 matches — those
  directories do not exist in this repo. `.no-mistakes.yaml` is a YAML config
  file with no request-body handling. NA unchanged.

SEC-05 RATE_LIMITS: PASS
  evidence: Paid-API calls occur via `claude` CLI subprocesses in the eval
  harness (`evals/graders/run_judge.py`, allowlist-tested in
  `evals/graders/test_run_judge.py`). Spend-cap control re-verified this audit:
  `MAX_ROLLOUTS=20` hard cap at `evals/baseline/run_baseline.sh:16`, with an
  explicit abort-before-live-calls check at lines 35-36. `.no-mistakes.yaml`
  changes only the local gate-agent identity/comment block and does not touch
  the eval harness or its spend-cap logic. PASS unchanged.

SEC-06 CORS_ALLOWLIST: NA
  evidence: No HTTP API surface. `grep -rl "cors|CORS|allowOrigin|
  allow_origin"` across .ts/.js/.py returns 0 matches. No server process or
  network-exposed endpoint introduced anywhere in the tracked tree. NA
  unchanged.

SEC-07 CAPTCHA_PUBLIC_FORMS: NA
  evidence: No public-facing forms. `grep -rl "<form|action="` across
  .html/.tsx/.jsx/.svelte/.astro returns 0 matches. All surfaces remain
  operator-internal CLI tooling. NA unchanged.

SEC-08 ERROR_RESPONSE_HYGIENE: NA
  evidence: No HTTP API surface. `grep -rl "err\.message|error\.stack|
  error\.details"` across .ts/.js/.py (excluding console/log) returns 0
  matches. `.no-mistakes.yaml` has no error-handling code. NA unchanged.

SEC-09 NO_API_KEYS_FRONTEND: PASS
  evidence: `grep -rn "NEXT_PUBLIC_|VITE_|REACT_APP_"` combined with
  key|secret|token|password across .ts/.tsx/.js/.jsx returns 0 matches.
  `grep -rn "sk_[a-zA-Z0-9]{20,}|pk_live_[a-zA-Z0-9]{20,}|Bearer
  [a-zA-Z0-9]{20,}"` across the full tracked tree returns 0 matches.
  `.no-mistakes.yaml` diff itself contains no key material — only prose
  describing the agent switch and the ToS-violation rationale. PASS confirmed.

SEC-10 NO_ENV_IN_FRONTEND: PASS
  evidence: `find public dist static build -name ".env*"` returns nothing
  (none of those directories exist). `git log --all --full-history
  --diff-filter=A -- "**/.env" "**/*.env"` returns 0 commits (re-run this
  audit). No secrets in log statements (`grep` for key|secret|token|password
  in console.log/print/logger returns 0 matches). `.no-mistakes.yaml` contains
  no environment values or credentials — it names a gate-agent identifier
  string (`claude`) only. PASS confirmed.

SEC-11 SECURITY_HEADERS: NA
  evidence: No deployed production URL and no HTTP server in any tracked
  surface. `grep -rl "Content-Security-Policy|Strict-Transport-Security|
  X-Frame-Options|Referrer-Policy|Permissions-Policy|helmet"` across
  .ts/.js/.py/.toml/.yaml/.yml returns 0 matches. NA unchanged.

SEC-12 OWASP_TOP10: PASS
  evidence: OWASP sweep of the branch delta (`.no-mistakes.yaml` only, 10
  insertions / 6 deletions vs origin/main). A05 Security Misconfiguration is
  the only applicable category: the change repoints a CI/local gate-agent
  pin from a now-ToS-disabled Google/Gemini route to `claude` (local, no
  external network egress change to a third party beyond what already ran
  via `claude` elsewhere in the repo's tooling). No credentials, no
  auth/session logic, no injection surface, no new dependency, no new
  network call introduced by this file. 0 Critical findings. Full detail
  below.

## Critical Findings Detail

None. No items failed with Critical severity. CRITICAL_FAILS=0.

## OWASP Findings Detail (supporting SEC-12) — branch delta only (`.no-mistakes.yaml`)

**A01 Broken Access Control:** N/A — no access-control surface in a YAML gate
config. PASS.

**A02 Cryptographic Failures:** N/A — no cryptographic material in the diff.
PASS.

**A03 Injection:** N/A — pure YAML/comment content, no executable code path
introduced. PASS.

**A04 Insecure Design:** No threat model present (unchanged from all prior
audits of this repo). The gate-agent pin change is an operational/process
decision, not a design surface. LOW, unchanged recommendation to run
`/threat-model` before any network-exposed endpoint is ever added.

**A05 Security Misconfiguration:** This is the one category the diff actually
touches. The change moves the repo's LOCAL gate-agent override from
`acp:gemini` to `claude`, with an explicit comment recording the reason
(Google account behind `acpx` disabled for ToS violation — programmatic
Gemini access is banned) and an explicit "do NOT re-add acp:gemini / acpx /
agy" guard comment. This is a configuration HARDENING, not a weakening: it
removes a route to a now-banned/disabled external provider and replaces it
with the CLI already used elsewhere in this repo's own tooling (`claude` is
also the eval harness's model in `evals/graders/run_judge.py`, allowlisted in
`test_run_judge.py`). No default credentials, no debug mode, nothing
disabled that shouldn't be. PASS.

**A06 Vulnerable Components:** No dependency change. `requirements.txt`
untouched by this diff. PASS.

**A07 Authentication Failures:** N/A — no auth surface. PASS.

**A08 Software and Data Integrity:** The gate-agent identifier controls which
CLI runs the repo's own CI/review gate (a private-side, non-shipped
tooling concern) — it does not affect the integrity of any shipped artifact,
webhook, or update mechanism in the public skill pack itself. PASS.

**A09 Security Logging:** N/A — no logging code in the diff. PASS.

**A10 SSRF:** N/A — no new outbound HTTP call introduced; if anything, the
diff REMOVES a route to an external service (`acp:gemini`/acpx) whose
account was disabled. PASS.

**Public-Repo Perimeter Check (branch delta only):**
- Commit message is functional and does not name what was "scrubbed" — it
  describes the operational reason for the agent switch (ToS-disabled
  account) plus a forward-looking policy guard. No proprietary methodology,
  no internal schema/warehouse names, no client or partner names. CLEAN.
- The diff itself is prose comments inside a YAML file — no secrets, no
  tokens, no internal identifiers, no scoring/ensemble internals. CLEAN.
- Canary-term grep (locally maintained internal-identifier/partner-name list,
  not reproduced in this tracked file) against the diff: 0 matches. CLEAN.

**Attacker Perspective (branch delta only):**
1. Could naming the disabled Google account or the ToS-violation reason leak
   anything sensitive? No — it names the AGENT ROUTE (`acpx`/Gemini), not any
   account credential, token, or internal system; the same fact (Gemini CLI
   access banned) is already public policy recorded elsewhere in this user's
   infra (`~/.claude`, 2026-07-04 agy/CLI removal, referenced generically).
   No account IDs, emails, or secrets appear in the diff.
2. Does switching the LOCAL gate agent to `claude` change what this PUBLIC
   repo's shipped code does at runtime for end users? No — `.no-mistakes.yaml`
   governs a private, operator-side CI/review gate; it ships no runtime
   behavior to consumers of the skill pack.
3. Replay/idempotency, privilege escalation, large-input resilience: not
   applicable — no such surface touched by this diff.

## Deep OWASP Findings (parallel track)

Not invoked — Default Mode only for this run. The full-tree OWASP baseline
(commits through 3a09f1d) was last swept in `audit-2026-06-25.md` with 0
Critical findings; this audit confirms no application-surface commits have
landed since, so that baseline stands for the shared codebase. The isolated
`.no-mistakes.yaml` delta is covered above.

SUMMARY: PASS_COUNT=4 FAIL_COUNT=0 NA_COUNT=8 CRITICAL_FAILS=0 AUDIT_DATE=2026-07-06 CHECKLIST_VERSION=1
