# Security Audit — parallax-workflows

AUDIT_DATE: 2026-07-31
AUDIT_COMMIT: 9fa8525
PROJECT_TYPE: MCP
THREAT_MODEL: absent

## Scope Note

Branch under audit: `fix/screening-fail-closed`, one commit (`9fa8525`) ahead of
`origin/main` (`3fcc05f`). Working tree clean at audit time (`git status`
confirmed no uncommitted changes).

This is a refresh of the prior audit (`AUDIT_DATE: 2026-07-19`, `AUDIT_COMMIT:
2e344f8`, taken on a different branch, `feat/plugin-and-web-distribution`, with
uncommitted working-tree changes). That prior audit's conclusions were **not**
carried over uncritically — every item below was independently re-verified
against the current branch, commit, and working tree.

The single new commit (`9fa8525`) touches four tracked files, all Markdown, no
code: `skills/_parallax/parallax-conventions.md`,
`skills/parallax-halal-screen/SKILL.md`, and their generated `plugin/` copies.
Confirmed directly (not asserted): `git diff --name-only origin/main..HEAD`
returns exactly these four paths, and `git rev-list origin/main..HEAD` returns
exactly one commit (`9fa8525`). The change adds a "gates fail closed on missing
data" convention (an `UNVERIFIED` verdict state for screening/eligibility gates
when upstream tool data is unavailable) and applies it to the halal-screen
skill's instructions. It is prose/instruction-only — no executable code,
scripts, or config changed.

Because the delta is documentation-only, the checklist below evaluates the
full current repo state at `9fa8525` (not diff-scoped), consistent with MCP
project-type full-audit scope. Repo-wide structural facts unchanged from all
prior audits in `docs/security/`: no HTTP API server directory (`api/`,
`server/`, `pages/api/`, `app/api/` absent), no frontend build output
directories (`public/`, `dist/`, `static/`, `build/` absent), no `.env` file
ever tracked (`git ls-files | grep -i '\.env'`: 0 matches), no production
deployment URL documented. The repo is a collection of Claude Code skill
Markdown files plus local, operator-invoked Python CLI tooling (build/eval
scripts) — not a deployed network service.

## Checklist Results

SEC-01 PRIVACY_POSTURE: NA
  evidence: No user-data-collection surface anywhere in the tracked tree. This
  is a skill-instruction repo with no persistence layer, no signup/account
  surface, and no deployed service. `grep -rli "privacy" docs/ README.md`
  matches only prior `docs/security/audit-*.md` files (self-references), not a
  privacy-policy document, which is consistent with there being no user data
  collected to have a policy over.

SEC-02 RLS_COVERAGE: NA
  evidence: No live Supabase/Postgres connection anywhere in the tracked tree.
  `grep -rli "supabase"` matches exactly two files
  (`skills/parallax-judge-house-view/judge.py`,
  `skills/parallax-judge-house-view/cadence.py`), both of which reference a
  Supabase-backed MCP tool as explicitly **deferred / not yet built** ("DEFERRED
  until Supabase managed backend ships") — read directly, not inferred from
  the filename. No RLS surface exists yet because no database exists yet.

SEC-03 AUTH_EDGE_CASES: NA
  evidence: No auth surface anywhere in the tracked tree. The only matches for
  login/signin/signup-shaped strings are false positives — the substring
  "signin" inside "signing" (cryptographic manifest-signing code in
  `skills/_parallax/house-view/`), confirmed by direct inspection of the
  matched lines. No password, session, or account-login code exists.

SEC-04 SERVER_SIDE_VALIDATION: NA
  evidence: No HTTP API server directory exists in this repo
  (`api/`, `server/`, `pages/api/`, `app/api/` all absent). The commit under
  audit changes only Markdown instruction files (no `req.body` /
  `request.json` / form-handling code anywhere touched or newly introduced).
  Not a network-facing input surface.

SEC-05 RATE_LIMITS: NA
  evidence: No rate-limit middleware exists because no served HTTP endpoint
  exists. Repo-wide grep for paid-API keywords (`openai|anthropic|stripe|
  twilio`, case-insensitive) returns matches only in `evals/` — local,
  operator-invoked CLI eval/grading scripts that shell out to the Claude Code
  CLI (`claude -p`) with a hardcoded model allowlist, not a served,
  per-request-billed API route reachable by external users. No spend-cap
  documentation is needed for local dev tooling of this shape; unchanged
  finding across every prior audit in this repo's history.

SEC-06 CORS_ALLOWLIST: NA
  evidence: No HTTP API surface anywhere in the tracked tree. `grep -rliE
  "cors|allowOrigin"` returns 0 matches repo-wide.

SEC-07 CAPTCHA_PUBLIC_FORMS: NA
  evidence: No public-facing forms in the tracked tree — no `.html`, `.tsx`,
  `.jsx`, or `.svelte` UI surface exists at all. All content is Claude Code
  skill Markdown and local Python CLI tooling.

SEC-08 ERROR_RESPONSE_HYGIENE: NA
  evidence: No HTTP API surface. No network response body exists anywhere in
  this repo to leak schema/stack-trace detail into.

SEC-09 NO_API_KEYS_FRONTEND: PASS
  evidence: `grep -rnE "NEXT_PUBLIC_|VITE_|REACT_APP_"` repo-wide (excluding
  `.git`): 0 matches. `grep -rnE "sk_[a-zA-Z0-9]{20,}|pk_live_[a-zA-Z0-9]{20,}
  |Bearer [a-zA-Z0-9]{20,}"` repo-wide: 0 matches. No frontend build surface
  exists to expose keys through in the first place (no `src/`, no bundler).

SEC-10 NO_ENV_IN_FRONTEND: PASS
  evidence: `find public dist static build -name ".env*"` returns nothing
  (none of those directories exist in this repo). `git log --all
  --full-history --diff-filter=A -- "**/.env" "**/*.env"`: 0 commits, ever —
  no `.env` file has ever been committed to this repo's history.
  Credential/secret-shaped literal grep (`password\s*=|api_key\s*=|secret\s*=
  |BEGIN.*PRIVATE KEY`) repo-wide: 0 matches outside the intentionally
  test-only signing fixtures documented and allowlisted in `.gitleaks.toml`
  (with an explicit "test-only, no production trust binding" comment). Ran
  `gitleaks detect` against the full working tree with the repo's own
  `.gitleaks.toml` config: 0 leaks found.

SEC-11 SECURITY_HEADERS: NA
  evidence: No deployed production URL and no HTTP server anywhere in the
  tracked tree.

SEC-12 OWASP_TOP10: PASS
  evidence: 0 Critical-severity findings. Verified directly rather than
  re-asserting the prior audit's conclusion: (1) the single commit under audit
  is Markdown-only — no injection, access-control, or crypto surface touched;
  (2) `gitleaks detect -c .gitleaks.toml`: no leaks; (3) the repo's own
  leak-prevention gate, `scan_tracked_terms.py`, run fresh against all 531
  currently-tracked files: 0 restricted-term hits; (4) separately swept the
  full diff of commit `9fa8525` against the locally-maintained extra-canary
  list (`~/.claude/parallax-canary-extra.txt`, not tracked in this repo, terms
  not reproduced here): 0 matches; (5) rebuilt the plugin bundle from source
  (`build_bundle.py plugin`) and diffed it byte-for-byte against the tracked
  `plugin/` tree and `.claude-plugin/marketplace.json`: identical — the
  tracked bundle is not stale relative to the two source `SKILL.md`/
  `conventions.md` edits, and `git status` after the rebuild shows no
  uncommitted delta. No dependency manifest (`requirements.txt`,
  `package.json`, lockfile) exists in this repo to carry a CVE. No auth,
  access-control, injection, SSRF, or logging surface exists to evaluate
  against the remaining OWASP categories — same structural absence noted
  across every prior audit of this repo.

## Public-Repo Perimeter Check

- Read the full diff of `9fa8525` directly (not filenames alone) for internal
  warehouse/schema identifiers, partner or client names, and proprietary
  methodology internals: 0 hits. The commit's subject matter (a documentation
  convention for how screening gates should behave when tool data is missing,
  and its application to the halal-screen skill) does not reference any
  internal system name, partner, or non-public parameter.
- `python3 skills/_parallax/scripts/scan_tracked_terms.py`: `531 tracked files
  scanned — no restricted terms`.
- Grepped the diff of `9fa8525`, case-insensitively, against the full local
  extra-canary list (13 lines, not reproduced here): 0 matches.
- This audit file itself was checked against the same two term sets
  (`scan_tracked_terms.py`'s built-in list and the local extra-canary list)
  before being left in place: 0 matches under both. No literal internal term,
  partner name, or schema identifier appears anywhere in this document,
  including in "found none" evidence lines.
- Commit message for `9fa8525` (already on the branch, not authored by this
  audit): functional, describes what the code/docs change do, does not
  describe anything as redacted, scrubbed, or held back. Consistent with this
  repo's commit-message-hygiene convention.

## Critical Findings Detail

None. No item failed this cycle; CRITICAL_FAILS=0.

## Deep OWASP Findings (parallel track)

Not invoked — Default Mode only for this run.

SUMMARY: PASS_COUNT=3 FAIL_COUNT=0 NA_COUNT=9 CRITICAL_FAILS=0 AUDIT_DATE=2026-07-31 CHECKLIST_VERSION=1
