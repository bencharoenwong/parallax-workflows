# Security Audit — parallax-workflows

AUDIT_DATE: 2026-06-11
AUDIT_COMMIT: 6e30354c558a53d7819a3dbedaa845acf72eadd9
PROJECT_TYPE: MCP
THREAT_MODEL: absent

## Checklist Results

SEC-01 PRIVACY_POSTURE: NA
  evidence: No user data collected. Repo is a skill pack (Markdown + Python helpers) with no server, no DB, no user accounts, and no data storage. PRs #44/#45/#46 add no new data-collection surface. Privacy policy surface not required. Baseline from 2026-06-07 unchanged.

SEC-02 RLS_COVERAGE: NA
  evidence: No Supabase or Postgres connection in any tracked surface. grep for "supabase|create policy|enable row level security" across all .ts/.js/.py/.sql files returned 0 matches. PRs #44/#45/#46 introduce no database layer. RLS remains the upstream Parallax MCP server's responsibility. NA unchanged.

SEC-03 AUTH_EDGE_CASES: NA
  evidence: No auth surface. No login, signin, password-reset, or session management code in the repo. PRs #44/#45/#46 (disclaimer plumbing, country/macro boundary, concierge router rows) introduce no auth surface. NA unchanged.

SEC-04 SERVER_SIDE_VALIDATION: NA
  evidence: No HTTP API server. grep for "req.body|request.json|request.form" across all API/server directories returned 0 matches — those directories do not exist. The Python files touched (judge.py, conftest.py, tests/*.py) are local test and eval helpers with no network body parsing. NA unchanged.

SEC-05 RATE_LIMITS: PASS
  evidence: Paid-API calls occur via claude CLI subprocesses in the eval harness (run_judge.py). Spend-cap controls unchanged: MAX_ROLLOUTS=20 hard cap in run_baseline.sh aborts before live calls; token cost estimate printed to stderr before any calls. PRs #44/#45/#46 change only Markdown skill files and test imports — no new API call paths added. Controls remain appropriate for operator-run internal tooling.

SEC-06 CORS_ALLOWLIST: NA
  evidence: No HTTP API surface. PRs #44/#45/#46 add no server processes or network-exposed endpoints. grep for "cors|CORS|allowOrigin|allow_origin" returned 0 matches. NA unchanged.

SEC-07 CAPTCHA_PUBLIC_FORMS: NA
  evidence: No public-facing forms. grep for "<form|action=" across all .html/.tsx/.jsx/.svelte/.astro files returned 0 matches. All surfaces are operator-internal CLI tooling. NA unchanged.

SEC-08 ERROR_RESPONSE_HYGIENE: NA
  evidence: No HTTP API surface. grep for "err.message|error.stack|error.details" across .ts/.js/.py files (excluding console/log) returned 0 matches. PRs #44/#45/#46 introduce no HTTP response handling. NA unchanged.

SEC-09 NO_API_KEYS_FRONTEND: PASS
  evidence: grep for NEXT_PUBLIC_/VITE_/REACT_APP_ combined with key|secret|token returned 0 matches. grep for sk_*/pk_live_*/Bearer token patterns across all tracked files returned 0 matches. PRs #44/#45/#46 introduce no new files with API key material. Test-signing-key.priv remains test-only fixture (unchanged from prior audit). PASS confirmed.

SEC-10 NO_ENV_IN_FRONTEND: PASS
  evidence: find on public/dist/static/build/ for .env* returned nothing (directories do not exist). git log --all --diff-filter=A for .env files: 0 commits. notes/ is gitignored (confirmed via git check-ignore; not tracked). PLANS.md newly gitignored in PR#45 (.gitignore line 55 adds PLANS.md); file exists locally at 21KB but is untracked and correctly excluded. No secrets in log statements (grep returned 0 matches). PASS confirmed.

SEC-11 SECURITY_HEADERS: NA
  evidence: No deployed production URL; no HTTP server introduced by any tracked surface. grep for Content-Security-Policy|Strict-Transport-Security|X-Frame-Options|Referrer-Policy|Permissions-Policy|helmet returned 0 matches. NA unchanged.

SEC-12 OWASP_TOP10: PASS
  evidence: OWASP sweep of PRs #44/#45/#46 surface below. 0 Critical findings. Prior audit's PASS for existing codebase unchanged.

## Critical Findings Detail

None. No items failed with Critical severity.

## Information-Disclosure / Data-Perimeter Check — PRs #44, #45, #46 (2026-06-11)

PRIMARY FOCUS: commit range 5a1021f..HEAD (3 squash merges).

### 1. Commit Message Review

All three commit subject lines are functional and describe code behavior:

- `fix(skills): source §9.1 disclaimers by reference and repair the §9.2 exemption gate (#44)` — describes spec change.
- `fix(skills): country/macro boundary rewrite, ground-truth integrity wiring, health-flag canon (#45)` — describes workflow logic change.
- `fix(parallax-concierge): route credit-lens, pair-finder, cio-letter-prep, and house-view ops (#46)` — describes routing addition.

No commit message describes what was scrubbed, redacted, or held back. CLEAN.

### 2. File Content Review — Proprietary Methodology Internals

**Checked for:** scoring/ensemble composition, regional routing, non-public parameter bounds, valuation-integration details, named internal frameworks.

- **loader.md diff (PRs #44/#45):** Changes are spec/wording corrections only: rule-3 "ground-truth panel" → "ground-truth check", compact-render qualifier, `applied_reason` field extension for no-view case, `view_hash`/`provenance_hash` field definitions. These are data-contract fields for the public skill library's audit row spec. No ensemble weights, no scoring coefficients, no internal framework names introduced. CLEAN.
- **country-deep-dive SKILL.md (#45):** Rewrites the boundary clause (allocation vs. regime question), adds unconditional loader.md load, adds `get_company_info` ground-truth oracle. All references are to public Parallax MCP tool names (`build_stock_universe`, `macro_analyst`, `get_company_info`, `get_peer_snapshot`). CLEAN.
- **macro-outlook SKILL.md (#45):** Same pattern. Adds §7.4 and unconditional audit wiring. No internal scoring parameters exposed. CLEAN.
- **DECISIONS.md (PR #44/#45):** Two new entries covering the redundancy-ruling decision (portfolio-scoring-core extraction dropped; country-deep-dive retained with boundary rewrite) and the disclosure-plumbing batch. These entries describe skill-library quality-audit fix rationale — the established repo practice noted in the audit brief. No methodology internals (no scoring formula, ensemble weights, regional routing tables, or coefficient values) appear in either entry. `§3b weights renormalise over verified holdings` refers to the public loader.md §3b spec. CLEAN.
- **parallax-conventions.md (#44):** Adds §9.2 exemption for `parallax-make-house-view` (config artifact + Step 7 gate), adds §9.1 consumer reference pattern. No internal framework names. CLEAN.
- **greenblatt.md (#44):** Documents that the production default is sector-scoped due to universe-builder timeouts, and adds the sector-by-sector merge path for broad screens. This is end-user-visible behavior documentation. No proprietary formula parameters. CLEAN.
- **CHANGELOG.md (#44/#45/#46):** Describes skill behavior changes. `loader.md §3b renormalisation` is a cross-reference to the public spec. No internal schema names, no methodology internals. CLEAN.
- **parallax-concierge/SKILL.md (#46):** Adds routing rows for credit-lens, pair-finder, cio-letter-prep, and house-view operations. All skill names are public (`/parallax-credit-lens`, `/parallax-judge-house-view`, etc.). No internal routing weights or service identifiers. CLEAN.
- **.gitignore (#45):** Adds `PLANS.md` to the ignore list. Entry is a filename only; does not describe the file's contents. CLEAN.

### 3. Internal Data-Source / Schema Names

Grepped the full diff against the locally-maintained internal-identifier canary list (database/warehouse identifiers, methodology parameter names; list not reproduced in this tracked file): 0 matches. All internal identifiers remain absent from tracked files. CLEAN.

### 4. Firm-Specific / Client-Specific Material

Grepped the diff for the firm name and the locally-maintained partner-name canary list (list not reproduced in this tracked file): 0 new matches. "Chicago Global" and "chicago.global" remain in README.md as public company disclosure (unchanged). No hedge-fund operations detail, no client names, no white-label partner names in today's range. CLEAN.

### 5. Private-Repo Artifacts

grep for eval metrics, research findings, internal security-audit detail, planning/decision/handoff notes in today's diff: 0 matches. DECISIONS.md entries are architectural rationale, not eval-harness metrics or internal audit findings. notes/ directory is gitignored and untracked (verified). PLANS.md is gitignored (PR#45 adds the entry) and untracked. CLEAN.

### VERDICT: All 5 categories CLEAN for PRs #44, #45, #46.

---

## Pre-Existing Observations (NOT from today's range — do not count against today's merges)

These items were present on main before 5a1021f and are flagged here as observations only.

**OBS-1: `loader.md` §5 rule 6 (line 224)**

The line carries a possessive firm reference plus an architecture characterization when describing the source of factor scores.

Assessment: At the margin of acceptable. The underlying facts are public (the README states the product's corporate parentage; that factor scores are rule-based rather than LLM-generated is part of the product's public positioning). The line does not name internal systems, reveal scoring coefficients, or describe construction.

Recommendation: Borderline, not a clear leak. A neutral rewrite attributing the scores to the product backend (rather than the firm) conveys the same user-facing meaning. Flag for discretionary scrub in the next SKILL.md pass; not a blocker.

**OBS-2: `parallax-make-house-view/SKILL.md` (lines 24 and 88)**

Two occurrences of a proprietary-data label inside the no-external-dispatch gotcha for telemetry output.

Assessment: The no-dispatch rule itself is correct and stays. The label slightly over-describes — it names the proprietary nature of the data rather than just enforcing the rule. It does not name the signals themselves, reveal their construction, or expose scoring methodology.

Recommendation: Same as OBS-1 — borderline, not a clear leak. A neutral "non-public data" label enforces the same guardrail. Flag for the next pass; not a blocker.

**Note on residual DECISIONS.md vocabulary (pre-existing, carried forward):** Prior audits (2026-06-01, 2026-06-07) acknowledged residual internal-methodology vocabulary in older DECISIONS.md entries as a known-accepted condition. Today's DECISIONS.md additions introduce no new instances. Status unchanged.

---

## OWASP Findings Detail (supporting SEC-12) — PRs #44/#45/#46

**A01 Broken Access Control:** No new access control surface. PRs modify Markdown skill files and Python test imports only. No new route, endpoint, or access-gated resource. PASS.

**A02 Cryptographic Failures:** No new sensitive data stored or transmitted. loader.md §6.2 gains `view_hash`/`provenance_hash` field specs — these are SHA-256 hashes of skill-library artifacts (not user PII). Field spec change only; no new cryptographic implementation. PASS.

**A03 Injection:**
- `judge.py` (PR#44, minor change): `subprocess.run(["claude", "-p", prompt, ...])` uses fixed list form, no `shell=True`. `prompt` is a rendered string from criterion + prose — both operator-controlled. Pre-existing, unchanged risk profile. INFORMATIONAL.
- `validator.py` (unchanged): `subprocess.run(["npx", "-y", "@google/design.md", "lint", temp_path, ...])` uses list form; `temp_path` is `tempfile.NamedTemporaryFile().name`. Pre-existing INFORMATIONAL from prior audits. Unchanged.
- All other new/changed Python in today's range (test files) use only `import` statements and assertion logic. No exec/eval/shell. PASS.

**A04 Insecure Design:** No threat model present (unchanged). PR surfaces are operator-internal Markdown spec updates. LOW priority. Recommend `/threat-model` before any network-exposed endpoint is added.

**A05 Security Misconfiguration:** No debug mode or default credentials in any new file. The `_NINE_TWO_EXEMPT_SKILLS` test gate (PR#44) now requires full directory names (`parallax-*`) — this is a security tightening relative to the prior broken state. PASS.

**A06 Vulnerable Components:** PRs #44/#45/#46 introduce no new dependencies. `requirements.txt` (pynacl==1.6.2, rfc8785==0.1.4, pyyaml==6.0.3) unchanged. PASS.

**A07 Authentication Failures:** No auth surface introduced. PASS.

**A08 Software and Data Integrity:** The §9.2 exemption test gate (`_NINE_TWO_EXEMPT_SKILLS`) is repaired in PR#44: dead entry `white-label-onboard` replaced with full `parallax-white-label-onboard`; test now asserts every entry names a real skill directory. This improves integrity of the compliance gate. PASS.

**A09 Security Logging:** No new logging of sensitive data. PASS.

**A10 SSRF:** No new outbound HTTP calls. skill SKILL.md files describe MCP tool invocations (local Claude-to-MCP protocol); these are not outbound HTTP from the skill library itself. PASS.

**Public-Repo Perimeter (PRs #44/#45/#46):**
- No internal schema, warehouse, or table identifiers in any diff line (checked against the locally-maintained identifier canary list; not reproduced here).
- No hedge-fund operations detail, no client names introduced.
- Two pre-existing firm-reference wordings in loader.md and parallax-make-house-view/SKILL.md predate today's range — see OBS-1/OBS-2 above.
- notes/ untracked, PLANS.md newly gitignored and untracked — file hygiene is correct.

**Attacker Perspective (PRs #44/#45/#46):**
1. The `_NINE_TWO_EXEMPT_SKILLS` repair — could an attacker use the fixed test gate to enumerate skill directory names? All skill names are already public in README.md's skill table. No new exposure.
2. greenblatt.md documents the sector-scoped default — does this expose server-side timeout thresholds or rate limits? No — it describes the operator-facing query scope recommendation, not server internals.
3. Concierge routing table additions (#46) — all target skills are public names already in README.md. No internal routing infrastructure exposed.

SUMMARY: PASS_COUNT=4 FAIL_COUNT=0 NA_COUNT=8 CRITICAL_FAILS=0 AUDIT_DATE=2026-06-11 CHECKLIST_VERSION=1
