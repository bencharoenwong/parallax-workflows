# Security Audit — parallax-workflows

AUDIT_DATE: 2026-08-25
AUDIT_COMMIT: 3854386e1538f1aa74ed67a731f525d199d2aaa4
PROJECT_TYPE: MCP
THREAT_MODEL: absent

## Scope Note

Full 12-item checklist re-run at this pin, not an incremental delta. The
prior audit (`audit-2026-08-17.md`, commit `a9a7e8a`) covered the delta up to
that point; this file confirms those verdicts still hold and independently
re-derives each one from a fresh repo-wide sweep at `3854386` rather than
carrying them forward unverified. `THREAT_MODEL.md` is absent — recommend
`/threat-model` if this repo grows a network-facing or auth-relevant
surface; not needed today given the NA verdicts below.

**What changed since the prior audit (`a9a7e8a..3854386`, 26 commits, 18
files):** CI workflow gained seven new per-root pytest steps (test-suite
coverage fix, no new execution surface); the pre-push commit-message-scan
hook layer gained a fail-closed check on temp-file creation failure and a
corrected merge-base range calculation for new-branch and deletion-only push
cases (both changes make the existing guard stricter, not looser); and the
house-view attribution module gained an exposure-cap enforcement fix
(pure numerical logic — a redistribution algorithm and its post-condition
assertion, no I/O, no external input). The full delta was grepped for
`shell=True`, `os.system`, `eval(`, `__import__`, `pickle`, `yaml.load`,
`subprocess`, `urllib`, `requests.`, `httpx`, `socket.`, and secret-prefix /
private-key-block patterns: zero matches outside the already-reviewed,
list-form `subprocess.run(["git", ...])` calls carried over unchanged from
prior pins.

**Fresh full-repo sweeps run today (not delta-scoped):**
- Hardcoded-secret patterns (`sk_`, `pk_live_`, `Bearer <token>`, PEM private-key
  blocks) across all tracked `.py/.ts/.js/.json/.yaml/.yml/.md`: one hit, a
  test fixture filename/header used to verify the loader *rejects* a
  private-key-looking upload — not a real key, same as prior pins.
- `password=`/`api_key=`/`secret=` literal assignments: zero matches.
- `.env` added in git history, ever: zero matches (`git log --all
  --full-history --diff-filter=A`).
- `.env` under any `public/dist/static/build`: none of those directories
  exist in this repo.
- `shell=True`, `os.system(`, `eval(`, `pickle.load(`, `yaml.load(` in
  non-test `.py`: zero matches (one `ast.literal_eval` hit, which is the
  safe-parsing alternative, not the risky builtin).
- `subprocess` call sites outside tests (8 files): all list-argument form,
  no shell string interpolation; reviewed the ones not covered by a prior
  audit, including `render_stock_report.py`'s headless-Chrome invocation
  (converts a locally-generated HTML report to PDF, no untrusted input in
  the arg list) and `run_judge.py`'s `claude -p` call flagged under SEC-05
  above.
- `pip-audit -r skills/_parallax/house-view/requirements.txt` (the only
  pinned, exact-version dependency manifest in the repo — a deliberate lockfile
  per its own header comment, for replay byte-identity): **0 known
  vulnerabilities** against the 3 pinned packages.
- No `package.json`, no other `requirements*.txt`, no `pyproject.toml`
  tracked — this is a skills library invoked by an agent harness, not a
  deployed service with its own dependency-installed runtime.

## Checklist Results

SEC-01 PRIVACY_POSTURE: NA
  evidence: No user data collection anywhere in the repo; this is a skills
  library that orchestrates calls to an external MCP tool server, it does
  not itself store or transmit end-user data. No production URL to check.

SEC-02 RLS_COVERAGE: NA
  evidence: No Supabase/Postgres surface anywhere in the repo. Case-
  insensitive grep for "supabase" across all `.py/.ts/.js` returns 2 hits,
  both docstring comments in `parallax-judge-house-view` (`cadence.py`,
  `judge.py`) noting a future server-side backend is "DEFERRED until
  Supabase managed backend ships" — no client, no connection string, no
  live surface today.

SEC-03 AUTH_EDGE_CASES: NA
  evidence: No auth surface. The only matches for login/signin/signup/reset
  substrings are false positives on unrelated words (`signing_key_id`,
  cryptographic-signing code in the house-view manifest verifier) — read in
  context, none is an authentication flow.

SEC-04 SERVER_SIDE_VALIDATION: NA
  evidence: No HTTP API server exists in this repo (no `api/`, `server/`,
  `pages/api/`, `app/api/` directories; zero `req.body`/`request.json`/
  `request.form` hits). The closest input-validation-relevant surface is a
  set of operator-invoked CLI scripts (build/scan/install tooling), reviewed
  under SEC-12 rather than scored here, consistent with the treatment in
  every prior audit of this repo.

SEC-05 RATE_LIMITS: NA
  evidence: No paid-API integration. Case-insensitive probe for openai/
  anthropic/stripe/twilio across non-test `.py` returns hits in 4 files: a
  cost-tracking field name (`anthropic_cost_usd`) in the eval harness's
  run-record schema, and `evals/graders/run_judge.py`, which shells out to
  the local `claude -p` CLI (a pinned model, per its own docstring) to
  score eval criteria — the live call is intentionally not exercised in
  CI. This is a local CLI invocation, not a metered third-party API
  requiring a spend cap; NA holds, but the path is worth naming rather
  than omitting. The one outbound network-fetch path in the repo
  (`web_pdf.py`'s URL/PDF extractor) is a destination-validated,
  size-capped, best-effort fetcher, likewise not a paid third-party API.

SEC-06 CORS_ALLOWLIST: NA
  evidence: No HTTP API surface; zero CORS-configuration hits anywhere in
  the repo.

SEC-07 CAPTCHA_PUBLIC_FORMS: NA
  evidence: No public-facing forms. There are zero `<form` tag hits
  anywhere in the repo. A loose substring probe for "form" matches two
  generated, non-interactive local artifacts (a dependency graph HTML
  visualization and an internal planning-doc export), but those hits are
  words like "format" and "transform", not form elements — confirmed by
  targeted `<form` grep on both files, zero matches.

SEC-08 ERROR_RESPONSE_HYGIENE: NA
  evidence: No HTTP API surface, so no response body crosses a network trust
  boundary. CLI-tool failure output (installer, scanners) prints a file path
  or exit code, never a stack trace or internal schema name, consistent with
  prior-audit findings for the same tooling.

SEC-09 NO_API_KEYS_FRONTEND: PASS
  evidence: No frontend build exists in this repo. Full-repo grep for
  secret-prefix, bearer-token, and private-key-block patterns returns one
  hit, a test fixture proving the loader rejects a private-key-shaped
  upload — not a real key. No tracked `.env`, `.pem`, or `.key` file.

SEC-10 NO_ENV_IN_FRONTEND: PASS
  evidence: No `public/`, `dist/`, `static/`, or `build/` directory exists.
  No `.env` file has ever been added in this repo's git history (confirmed
  via `git log --all --full-history --diff-filter=A`). No secret-looking
  value appears in any non-test `print`/`console.log`/logger call.

SEC-11 SECURITY_HEADERS: NA
  evidence: No deployed production URL, no HTTP server anywhere in the
  repo.

SEC-12 OWASP_TOP10: PASS
  evidence: Full pattern sweep of the repo at this pin (see Scope Note
  above) plus targeted review of every new file/hunk since the prior audit.
  0 Critical findings, 0 High findings. `pip-audit` against the one pinned
  dependency manifest returns 0 known CVEs. The public-repo commit-message
  and tracked-file term scanners (this repo's own A08/A09-relevant controls)
  are both installed as a blocking pre-push hook and were themselves
  hardened further in this delta (fail-closed on temp-file-creation
  failure, corrected range calculation for new-branch and deletion-only
  pushes — both changes remove a prior silent-fallback failure mode rather
  than introduce one). No SSRF, injection, path-traversal, or credential-
  handling issue found in this pass.

## Critical Findings Detail

None. No item above has a FAIL verdict.

## Handoff

All non-NA items PASS. No critical or non-critical items are failing.
Push will pass the gate. No `test-architect` dispatch needed (SEC-03 is NA,
not FAIL — there is no auth surface to test).

Recommend `/threat-model` only if this repo later grows a network-facing,
auth, or user-data surface; not warranted today.

SUMMARY: PASS_COUNT=3 FAIL_COUNT=0 NA_COUNT=9 CRITICAL_FAILS=0 AUDIT_DATE=2026-08-25 CHECKLIST_VERSION=1
