# Security Audit — parallax-workflows

AUDIT_DATE: 2026-09-06
AUDIT_COMMIT: c6828adf5cb045c349d41b90aea3d3c8e90b4154
PROJECT_TYPE: MCP
THREAT_MODEL: absent

## Scope Note

Full 12-item checklist re-run at this pin, not an incremental delta — every
verdict below was independently re-derived from a fresh repo-wide sweep at
`c6828ad`, not carried forward from `audit-2026-08-25.md` unverified.
`THREAT_MODEL.md` is absent — recommend `/threat-model` if this repo grows a
network-facing or auth-relevant surface; not needed today given the NA
verdicts below.

**What changed since the prior pin (`3854386..c6828ad`, confirmed a real
ancestor 11 days back): 109 files, +18677/-829.** This is substantially
larger than a same-day feature-branch delta — the range spans everything
merged to `main` since the prior audit plus the current branch's own commits.
Three independent lines of work landed:

- **A client-policy / white-label expansion that did not exist at the pin
  at all.** Zero files under `client-policy/` at `3854386`; 17 at HEAD.
  New non-test modules: `client-policy/adaptation.py` (S0-S2 policy
  adaptation, pure-stdlib + pyyaml), `client-policy/reconcile.py` (an S3
  linear-programming reconciliation solver, fails closed to
  `solver_unavailable` without `scipy`), `white-label/persistence.py`
  (atomic staged-then-`os.replace` config commits with a recovery
  journal), `white-label/extract/folder.py` (deterministic file
  classification for confirmed folder-mode brand ingest), and
  `white-label/rm_consumer.py` (RM-report brand rendering, including a
  logo-URL sanitizer and a client-name markdown-injection guard). All are
  operator-invoked, local-filesystem, no-network code paths — none expose
  an HTTP endpoint.
- **Gate-test / CI-parity hardening.** A new deterministic helper,
  `_parallax/coverage_check.py` (405 lines, pure stdlib, no network —
  cross-checks `check_portfolio_redundancy` sector coverage against
  `analyze_portfolio`), plus two new gate tests
  (`_parallax/scripts/test_ci_root_parity.py`, holding CI's hand-listed
  pytest steps to the same test set the local gate auto-discovers;
  `_parallax/scripts/test_live_capability_contract.py`, structural
  regressions on the runtime tool-contract prose) and updates to
  `run-gate-tests.sh`, `install_git_hooks.sh`, and
  `scan_commit_messages.py`.
- **Six `skills/*/SKILL.md` edits**, unrelated to each other in content:
  `parallax-rebalance/SKILL.md`'s Batch C2 policy-reconciliation split
  into `references/` (a content split, not a git rename —
  `git diff --diff-filter=R` over the range is empty), plus smaller edits
  to `parallax-client-review`, `parallax-desk-call-list`,
  `parallax-load-house-view`, `parallax-scenario-analysis`, and
  `parallax-white-label-onboard`.

**Sweeps run today, full repo at this pin:**
- `shell=True`, `os.system(`, `eval(`, `__import__`, `pickle`, `yaml.load(`/
  `yaml.unsafe_load(`/`yaml.full_load(`, `subprocess`, `urllib`, `requests.`,
  `httpx`, `socket.`, secret-prefix and PEM-block patterns: grepped across
  every non-test `.py`/`.sh` file changed in the delta (43 files) plus the
  full-repo sweep patterns from the prior audit. All `subprocess.run(...)`
  call sites use list-argument form with no shell-string interpolation
  (git plumbing in `build_bundle.py` and `scan_commit_messages.py`; a
  version-check and a design-linter invocation
  behind an availability check in `white-label/validator.py`, pre-existing
  and unchanged in behavior — only its temp-file handling moved from
  `NamedTemporaryFile` to `TemporaryDirectory`, a hardening not a new
  network path). Every `yaml.*load` call site in the changed files is
  `yaml.safe_load`. `socket.`/`urllib.` hits are all inside
  `white-label/extract/web_pdf.py`'s destination-validated, size-capped
  fetcher (pre-existing; the changed hunk in this delta is unrelated
  encoding handling, confirmed by reading the diff, not just grepping it).
- New RM-facing surfaces read for injection risk: `rm_consumer.py`'s logo
  URL validator restricts scheme to `{http, https}`, rejects embedded
  credentials/query/fragment, rejects markdown-link-breaking characters and
  invisible Unicode categories before the URL is interpolated into a
  report; its client-name sanitizer explicitly documents and defends
  against the same markdown/HTML-injection class. `folder.py`'s file
  classification walks an operator-supplied local directory
  (`Path.rglob`), not a remote or untrusted-user path.
- Hardcoded-secret patterns (`sk_`, `pk_live_`, `Bearer <token>`, PEM
  private-key blocks) across all tracked files: one hit, the same test
  fixture as every prior audit — a filename/header used to verify the
  loader *rejects* a private-key-looking upload, not a real key.
  `password=`/`api_key=`/`secret=` literal assignments: zero matches.
  `.env` added in git history, ever: zero matches. No
  `public/`/`dist/`/`static/`/`build/` directory exists.
- Case-insensitive `supabase` grep: still only roadmap-labeled
  documentation and two docstrings noting a managed backend is deferred —
  no client, connection string, or live surface. Case-insensitive
  `openai|anthropic|stripe|twilio` grep outside tests: still only the
  eval harness's cost-metering field name and its allowlisted, pinned
  `claude -p` judge invocation (unchanged from prior audit) — no metered
  third-party API call requiring a spend cap.
- Dependency manifests: four tracked `requirements*.txt` now exist (was
  one at the prior pin) — `house-view/requirements.txt` (exact pins,
  unchanged), and three new ones added by the client-policy/white-label
  work: `client-policy/requirements.txt` (`scipy>=1.10`, `pyyaml>=6`),
  `white-label/requirements.txt` (`jsonschema>=4,<5`, `pyyaml>=6`), and
  `white-label/requirements-test.txt` (exact-pinned test-fixture builders).
  `pip-audit -r` against all four: **0 known vulnerabilities.** CI also
  installs an unpinned `pillow` directly (not via a requirements file, for
  a logo-dimension-gate test path); unpinned means CI always resolves
  pip's then-current release, which sidesteps the CVE history a stale pin
  would carry — but there is no floor version enforced, so a stale local
  cache or offline mirror could silently resolve an old vulnerable build.
  Informational, not a FAIL: this is a CI/test dependency, not a shipped
  runtime component, and no manifest in the repo pins it to a known-bad
  version.
- Dual skill-scanner sweep (`skill-scanner` behavioral + `skillspector`
  static, `--no-llm` per the in-perimeter rule) over `skills/`: 0 Critical
  from `skill-scanner` (8 High, all resolved on inspection — see below);
  `skillspector` static mode flagged 2 Critical / 76 High, all resolved on
  inspection to false positives, consistent with its documented
  over-flagging behavior on large legitimate repos:
  - Both scanners' "Known Vulnerable Dependency: pyyaml/scipy" findings
    name-match a package without reading its pinned version; the actual
    floors (`pyyaml>=6`, `scipy>=1.10`) post-date every cited CVE, and
    version-aware `pip-audit` against the real manifests returns clean.
  - `skill-scanner`'s "Python bytecode without matching source" High
    findings are all local, gitignored, untracked `__pycache__` build
    artifacts from running the test suite locally — confirmed absent from
    `git ls-files` and covered by the repo's `.gitignore`.
  - `skillspector`'s "Env Variable Harvesting" (`os.environ.copy()`),
    "Credential Access" (`/etc/passwd`, `.env` string literals), and
    typosquat flag on `pynacl` are all either test code exercising a
    subprocess's `PYTHONPATH`, a defensive rejection test/docstring naming
    the exact credential-shaped inputs the loader *blocks*, or a
    legitimate, well-known crypto library fuzzy-matched against an
    unrelated package name.
  - One Medium (`skill-scanner`): a `.docx` test fixture flagged as
    "archive file" / extension-content mismatch — `.docx` is a zip
    container by format; this is a pre-existing, tracked, legitimate test
    fixture, not new to this delta.
  - The delta's own gate tests (`test_build_bundle.py`,
    `test_ci_root_parity.py`, `test_live_capability_contract.py`,
    `test_install_git_hooks.py`, `test_scan_commit_messages.py`,
    `test_coverage_check.py`) all pass at this pin: 182 passed, 0 failed.

## Checklist Results

SEC-01 PRIVACY_POSTURE: NA
  evidence: No end-user data collection. The new client-policy/white-label
  modules persist operator-supplied config (client mandate YAML, partner
  branding) to local files under the operator's own filesystem
  (`~/.parallax/...`) — nothing is transmitted off-machine by this repo's
  code. No production URL to check.

SEC-02 RLS_COVERAGE: NA
  evidence: No Supabase/Postgres surface. Fresh case-insensitive grep
  returns only roadmap documentation ("managed backend on the roadmap")
  and two unchanged docstrings noting the same deferred design — no
  client, connection string, or live table access anywhere in the repo.

SEC-03 AUTH_EDGE_CASES: NA
  evidence: No auth surface. No new auth-adjacent code in this delta;
  login/signin/signup/reset substring matches remain the same unrelated
  cryptographic-signing false positives as the prior audit.

SEC-04 SERVER_SIDE_VALIDATION: NA
  evidence: No HTTP API server exists in this repo (no `api/`, `server/`,
  `pages/api/`, `app/api/`; zero `req.body`/`request.json`/`request.form`
  hits). The new client-policy/white-label modules are operator-invoked
  local functions, not request handlers; their input validation (schema
  validation via `jsonschema`, fail-closed `solver_unavailable` on missing
  `scipy`, rejected malformed configs) is reviewed under SEC-12 rather
  than scored here, consistent with every prior audit of this repo.

SEC-05 RATE_LIMITS: NA
  evidence: No paid-API integration added in this delta. Fresh grep for
  openai/anthropic/stripe/twilio outside tests returns the same eval
  harness cost-metering field and the same pinned, allowlist-guarded
  `claude -p` judge invocation as the prior audit — a local CLI call, not
  a metered third-party API requiring a spend cap.

SEC-06 CORS_ALLOWLIST: NA
  evidence: No HTTP API surface; zero CORS-configuration hits anywhere in
  the repo.

SEC-07 CAPTCHA_PUBLIC_FORMS: NA
  evidence: No public-facing forms. Zero `<form` tag hits anywhere in the
  repo, including the new white-label/client-policy modules (which render
  Markdown reports and YAML configs, not HTML forms).

SEC-08 ERROR_RESPONSE_HYGIENE: NA
  evidence: No HTTP API surface, so no response body crosses a network
  trust boundary. The new `reconcile.py` fails closed with a named status
  string (`solver_unavailable`) rather than a stack trace or internal
  schema name; `persistence.py`'s recovery-journal errors likewise surface
  named failure states, not raw exceptions, to the caller.

SEC-09 NO_API_KEYS_FRONTEND: PASS
  evidence: No frontend build exists in this repo. Full-repo grep for
  secret-prefix, bearer-token, and private-key-block patterns returns the
  same one hit as every prior audit — a test fixture proving the loader
  rejects a private-key-shaped upload, not a real key. No tracked `.env`,
  `.pem`, or `.key` file.

SEC-10 NO_ENV_IN_FRONTEND: PASS
  evidence: No `public/`, `dist/`, `static/`, or `build/` directory
  exists. No `.env` file has ever been added in this repo's git history.
  No secret-looking value appears in any non-test `print`/`console.log`/
  logger call in the new modules (`persistence.py`'s recovery journal and
  `rm_consumer.py`'s logging both name only status/failure categories,
  never raw operator paths or config values, per their own docstrings).

SEC-11 SECURITY_HEADERS: NA
  evidence: No deployed production URL, no HTTP server anywhere in the
  repo.

SEC-12 OWASP_TOP10: PASS
  evidence: Full pattern sweep at this pin (see Scope Note) plus targeted
  read-through of every new non-test module since the prior pin
  (`adaptation.py`, `reconcile.py`, `persistence.py`, `folder.py`,
  `rm_consumer.py`, `coverage_check.py`) and the dual skill-scanner sweep.
  0 Critical, 0 High findings after resolving scanner false positives
  (documented above). `pip-audit` against all four dependency manifests:
  0 known CVEs. The public-repo commit-message scanner ran clean over the
  outgoing range (`origin/main..c6828ad`). No SSRF, injection, path-traversal,
  or credential-handling issue found: the new RM logo-URL path enforces
  an `{http, https}` scheme allowlist and rejects markdown/HTML injection
  vectors; the new file-classification path operates only on an
  operator-supplied local directory; all new YAML parsing uses
  `yaml.safe_load`; the new LP solver fails closed rather than degrading
  silently when its dependency is absent.

## Critical Findings Detail

None. No item above has a FAIL verdict.

## Handoff

All non-NA items PASS. No critical or non-critical items are failing.
Push will pass the gate. No `test-architect` dispatch needed (SEC-03 is
NA, not FAIL — there is no auth surface to test).

Recommend `/threat-model` only if this repo later grows a network-facing,
auth, or user-data surface; not warranted today. The client-policy/
white-label modules materially expanded this delta relative to the
task-briefing's description of a documentation-only sweep — worth a
`/decision-log` note if that gap in framing recurs, but it does not change
any verdict above: none of the new surface is network-facing.

SUMMARY: PASS_COUNT=3 FAIL_COUNT=0 NA_COUNT=9 CRITICAL_FAILS=0 AUDIT_DATE=2026-09-06 CHECKLIST_VERSION=1
