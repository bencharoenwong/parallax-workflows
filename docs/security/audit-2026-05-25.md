# Security Audit — parallax-workflows

AUDIT_DATE: 2026-05-25
AUDIT_COMMIT: a4acee1786e91993f510982a9cca4965f392c3a4
PROJECT_TYPE: MCP
THREAT_MODEL: absent

## Checklist Results

SEC-01 PRIVACY_POSTURE: NA
  evidence: No user data collected. This is a client-side skill pack (Markdown + Python helpers) with no server, no DB, no user accounts, and no data storage. No privacy policy surface required. Data-location documentation is not applicable.

SEC-02 RLS_COVERAGE: NA
  evidence: No Supabase or Postgres connection present. grep for "supabase|create policy|enable row level security" across all .ts/.js/.py/.sql files returned 0 matches (one mention in MCP_FIELD_INVENTORY.md is a comment referencing the upstream Parallax server's storage URL, not a local DB connection). RLS is the responsibility of the upstream Parallax MCP server, outside this repo's scope.

SEC-03 AUTH_EDGE_CASES: NA
  evidence: No auth surface. This repo has no login, signup, password-reset, or session management code. grep for "login|signin|password.*reset|signup" returned 0 matches in .ts/.tsx/.js/.jsx/.py files. Auth edge-case tests are not applicable.

SEC-04 SERVER_SIDE_VALIDATION: NA
  evidence: No HTTP API server. grep for "req.body|request.json|request.form" across api/, server/, pages/api/, app/api/ returned 0 matches — those directories do not exist. The Python helpers (loader.py, validator.py) process only local YAML files read from ~/.parallax/client-branding/config.yaml. Input handling uses yaml.safe_load (no unsafe yaml.load calls found).

SEC-05 RATE_LIMITS: NA
  evidence: No paid-API calls originate from this repo. The skills orchestrate the upstream Parallax MCP server via Claude Code's tool dispatch; they do not make direct API calls to OpenAI, Anthropic, Stripe, or Twilio. One test-only reference to "rate_limit" in test_stress.py is a mock response pattern. Rate limiting is the Parallax server's responsibility.

SEC-06 CORS_ALLOWLIST: NA
  evidence: No HTTP API surface. No CORS configuration exists or is needed — this repo has no server process, no HTTP listener, and no network-exposed endpoint. grep for "cors|CORS|allowOrigin|allow_origin" returned 0 matches.

SEC-07 CAPTCHA_PUBLIC_FORMS: NA
  evidence: No public-facing forms. This is an internal-only Claude Code skill pack. grep for "<form|action=" across all .html/.tsx/.jsx/.svelte/.astro files returned 0 matches. No web frontend exists.

SEC-08 ERROR_RESPONSE_HYGIENE: NA
  evidence: No HTTP API surface. The Python helpers surface errors only through return dict fields (e.g., result["error"] = "config_not_found") consumed by Claude Code internally — they are never serialized into an HTTP response body. No api/ or server/ directories exist.

SEC-09 NO_API_KEYS_FRONTEND: PASS
  evidence: grep for NEXT_PUBLIC_/VITE_/REACT_APP_ combined with key|secret|token returned 0 matches. grep for sk_*/pk_live_*/Bearer token patterns returned 0 matches. grep for sk-/sk-ant-/sk-proj-/ghp_/gho_/AKIA patterns returned 0 matches across all .py/.md/.yaml/.yml/.json/.toml files. Trusted_keys.json in skills/_parallax/house-view/signing/ contains only a public Ed25519 verification key (pubkey_b64) explicitly labeled "test-only" — no private key material present. No private key files (.pem, .key) found in the repository.

SEC-10 NO_ENV_IN_FRONTEND: PASS
  evidence: find on public/dist/static/build/ for .env* returned 0 results (those directories do not exist). git log --all --diff-filter=A for .env files returned 0 commits. grep for console.log/print/logger with key|secret|token|password patterns in api/server/ returned 0 matches (those directories do not exist). No .env files present anywhere in the repository tree.

SEC-11 SECURITY_HEADERS: NA
  evidence: No deployed production URL and no HTTP server. grep for Content-Security-Policy|Strict-Transport-Security|X-Frame-Options|Referrer-Policy|Permissions-Policy|helmet across all .ts/.js/.py/.toml/.yaml/.yml files returned 0 matches, consistent with no server process. Not applicable.

SEC-12 OWASP_TOP10: PASS
  evidence: No Critical OWASP findings. Detailed sweep results below. All subprocess calls use array form (no shell=True). yaml.safe_load used exclusively (no unsafe yaml.load). No SQL queries, no XSS surface, no CSRF surface, no redirect handling. ast.literal_eval in translate-chinese-finance/references/load_skill.py:118 is safe (literal_eval cannot execute arbitrary code). Logo path resolver in loader.py enforces an extension allowlist (_LOGO_ALLOWED_EXTS) and calls Path.resolve() + Path.expanduser() before checking is_file() — no path traversal possible. URL fetching in web_pdf.py is operator-initiated (branding extraction from a client-supplied URL) with no server-side SSRF risk (no server exists). 0 Critical findings.

## Critical Findings Detail

None. No items failed with Critical severity.

## OWASP Findings Detail (supporting SEC-12)

**A01 Broken Access Control:** No access control surface exists — no endpoints, no routes, no user roles. PASS.

**A02 Cryptographic Failures:** No sensitive data stored. Ed25519 public keys only in trusted_keys.json (test-only, labeled). No deprecated hash algorithms found. PASS.

**A03 Injection:**
- SQL injection: no SQL queries anywhere in the codebase. PASS.
- Command injection: subprocess calls in validator.py (lines 472, 497) and web_pdf.py (line 646) use list-form arguments with no user-supplied data interpolated into the command. run_diff_harness.py (line 79) uses list-form git command with no user input. PASS.
- YAML injection: yaml.safe_load used at loader.py:294. No yaml.load() found. PASS.
- ast.literal_eval at translate-chinese-finance/references/load_skill.py:118: parses Python literals extracted from a Claude-generated response (not raw user input). literal_eval cannot execute arbitrary code. INFORMATIONAL — not a security finding in this context.

**A04 Insecure Design:** No threat model present. Recommend `/threat-model` for the white-label onboarding flow (external URL fetching, partner config parsing) before expanding that surface. LOW priority given no deployed attack surface today.

**A05 Security Misconfiguration:** No server to misconfigure. No debug mode, no default credentials surface. PASS.

**A06 Vulnerable Components:** No package.json or requirements.txt with pinned dependencies found at repo root. Python helpers import yaml, jsonschema, PIL, matplotlib, pdfplumber, scrapling — these are operator-installed, not bundled. No lock files to audit. INFORMATIONAL — dependency audit is the operator's responsibility at install time.

**A07 Authentication Failures:** No auth surface. PASS.

**A08 Software and Data Integrity:** No CI/CD pipeline (.github/workflows/ absent, consistent with no-mistakes gate). No webhook handlers. PASS.

**A09 Security Logging:** Python helpers use Python's standard logging module with level-appropriate calls (logger.warning, logger.debug). No secrets are logged — logo paths are intentionally omitted from log messages (loader.py:349: "the raw path is intentionally omitted from both the error string and the WARNING log"). PASS.

**A10 SSRF:** web_pdf.py fetches operator-supplied URLs (client brand website) using urlopen with a 15s timeout and a 5 MB read cap. No server process means no server-side SSRF risk — the fetch runs in the operator's local Claude Code process, not behind a network boundary. INFORMATIONAL — acceptable risk for a local CLI skill.

**Attacker Perspective:**
1. Most sensitive data: partner branding configs in ~/.parallax/client-branding/config.yaml (client names, logo paths). Protected by filesystem permissions on the operator's machine — outside this repo's control.
2. Logo path resolver: correctly restricts to image extensions and requires is_file() — cannot be used to read /etc/passwd or .env files. Clean.
3. No replay, no webhooks, no payment flows in scope.

SUMMARY: PASS_COUNT=2 FAIL_COUNT=0 NA_COUNT=10 CRITICAL_FAILS=0 AUDIT_DATE=2026-05-25 CHECKLIST_VERSION=1
