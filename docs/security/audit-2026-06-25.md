# Security Audit — parallax-workflows

AUDIT_DATE: 2026-06-25
AUDIT_COMMIT: 36d45bb85ff9f4906447139ff77998833cda0862
PROJECT_TYPE: MCP
THREAT_MODEL: absent

## Checklist Results

SEC-01 PRIVACY_POSTURE: NA
  evidence: No user data collected. Repo is a skill pack (Markdown + Python helpers) with no server, no DB, no user accounts, and no data storage. The white-label-stock-report renderer (new since 2026-06-11) reads local JSON/YAML and writes HTML/PDF locally — no external data store, no user PII surface. Privacy policy not required.

SEC-02 RLS_COVERAGE: NA
  evidence: No Supabase or Postgres connection in any tracked surface. grep for "supabase|create policy|enable row level security" across all .ts/.js/.py/.sql returned 0 matches. No database layer introduced by any new commit since the 2026-06-11 audit. RLS responsibility remains upstream with the Parallax MCP server.

SEC-03 AUTH_EDGE_CASES: NA
  evidence: No auth surface. grep for "login|signin|password.*reset|signup" across all .ts/.tsx/.js/.jsx/.py returned 0 matches of user authentication code. Ed25519 signing references in manifest_cache.py and chain_emit.py are audit-trail signing (not user auth). No auth surface introduced in the white-label-stock-report, render_gate.py, or eval specs.

SEC-04 SERVER_SIDE_VALIDATION: NA
  evidence: No HTTP API server. grep for "req.body|request.json|request.form" across api/, server/, pages/api/, app/api/ returned 0 matches — those directories do not exist. render_stock_report.py is a local CLI tool reading operator-supplied files (argparse + json.loads + yaml.safe_load); no network body parsing. NA unchanged.

SEC-05 RATE_LIMITS: PASS
  evidence: Paid-API calls occur via claude CLI subprocesses in the eval harness (evals/graders/run_judge.py). Spend-cap controls: MAX_ROLLOUTS=20 hard cap in evals/baseline/run_baseline.sh aborts before live calls with a clear error message; token cost estimate printed to stderr before any calls. New eval specs (6 portfolio-skill configs added in PR #59) follow the same harness — no new unbounded API call path introduced. Appropriate for operator-run internal tooling.

SEC-06 CORS_ALLOWLIST: NA
  evidence: No HTTP API surface. grep for "cors|CORS|allowOrigin|allow_origin" across all .ts/.js/.py returned 0 matches. No server process or network-exposed endpoint in any tracked file.

SEC-07 CAPTCHA_PUBLIC_FORMS: NA
  evidence: No public-facing forms. grep for "<form|action=" across all .html/.tsx/.jsx/.svelte/.astro returned 0 matches. All interactive surfaces are operator-internal CLI tooling.

SEC-08 ERROR_RESPONSE_HYGIENE: NA
  evidence: No HTTP API surface. Error output in render_stock_report.py uses sys.stderr exclusively (e.g., Chrome-exit warnings, PDF-fallback notices) — never a response body. grep for "err.message|error.stack|error.details" across .ts/.js/.py excluding console/log returned 0 matches. NA unchanged.

SEC-09 NO_API_KEYS_FRONTEND: PASS
  evidence: grep for NEXT_PUBLIC_/VITE_/REACT_APP_ combined with key|secret|token returned 0 matches. grep for sk_*/pk_live_*/Bearer token patterns across all tracked files returned 0 matches. The test signing key in skills/_parallax/house-view/signing/ is a labeled test-only fixture (trust binding is per-kid; production deployments exclude this kid). render_stock_report.py and render_gate.py introduce no API key material. PASS confirmed.

SEC-10 NO_ENV_IN_FRONTEND: PASS
  evidence: find on public/dist/static/build/ for .env* returned nothing (directories do not exist). git log --all --diff-filter=A for .env files: 0 commits. evals/results/ is gitignored (verified via git check-ignore) and contains 0 tracked files. No secrets in log statements (grep for key|secret|token|password in console.log/print/logger across .ts/.js/.py returned 0 matches). PASS confirmed.

SEC-11 SECURITY_HEADERS: NA
  evidence: No deployed production URL and no HTTP server in any tracked surface. grep for Content-Security-Policy|Strict-Transport-Security|X-Frame-Options|Referrer-Policy|Permissions-Policy|helmet returned 0 matches. NA unchanged.

SEC-12 OWASP_TOP10: PASS
  evidence: OWASP sweep of commits since 2026-06-11 (a3443d3..36d45bb: render gate, eval specs, white-label-stock-report skill). 0 Critical findings. Detail in OWASP Findings section below.

## Critical Findings Detail

None. No items failed with Critical severity.

## OWASP Findings Detail (supporting SEC-12) — commits since 2026-06-11

**Scope:** render_gate.py (shared), parallax-white-label-stock-report/render_stock_report.py, 6 eval spec configs, .no-mistakes.yaml, DECISIONS.md, CHANGELOG.md, README.md, SKILL.md additions.

**A01 Broken Access Control:** No new access control surface. render_stock_report.py reads local operator-supplied files only. render_gate.py is a pure stdin/stdout filter. No new route or endpoint. PASS.

**A02 Cryptographic Failures:** No new sensitive data stored or transmitted. CSS injection mitigated by _hex() (validates hex color literals against an allow-set) and _safe_font() (strips CSS-breaking chars from font-family values). No new cryptographic implementation. PASS.

**A03 Injection:**
- render_stock_report.py HTML output: all user-data fields pass through html.escape() via esc() helper. No raw f-string HTML construction from untrusted input. PASS.
- render_stock_report.py CSS: color values gated by _hex(); font values gated by _safe_font() stripping ;}{}'"<> — CSS escape-out prevented. PASS.
- render_stock_report.py subprocess: `subprocess.run([CHROME, ...], ...)` uses hardcoded Chrome binary path in a list form (no shell=True). pdf_path is operator-chosen output path, not user-supplied in a web context. INFORMATIONAL (pre-existing operator-trust pattern).
- yaml.safe_load() used for brand config (no arbitrary Python execution). PASS.
- render_gate.py: pure string regex matching, no eval/exec. PASS.

**A04 Insecure Design:** No threat model present (unchanged from prior audits). New surfaces are local CLI tools; threat model is low urgency at this tier. Recommend /threat-model before any network-exposed endpoint is added. LOW.

**A05 Security Misconfiguration:** No debug mode or default credentials in any new file. sample-acme.synthetic.json is clearly labeled as a synthetic rendering fixture (fictional company, placeholder prose). .gitignore for the new skill excludes __pycache__/, .pytest_cache/, *.pyc. PASS.

**A06 Vulnerable Components:** render_stock_report.py has no new mandatory dependencies beyond stdlib + pyyaml (already tracked in requirements.txt). Optional deps (pymupdf/fitz, headless Chrome) are best-effort with silent fallback on ImportError/absence. No new CVEs introduced. PASS.

**A07 Authentication Failures:** No auth surface introduced. PASS.

**A08 Software and Data Integrity:** subprocess calls use hardcoded binary paths in list form. No webhook receivers. No CI/CD pipeline in repo (.github/workflows/ absent). PASS.

**A09 Security Logging:** render_stock_report.py error output to sys.stderr only. No PII or secrets logged. eval grader subprocess stderr is captured and used only for timeout/error diagnostics. PASS.

**A10 SSRF:** No new outbound HTTP calls from render_stock_report.py or render_gate.py. web_pdf.py (white-label brand extractor) uses urlopen for operator-supplied brand URLs — pre-existing informational from prior audits, no change. INFORMATIONAL.

**Public-Repo Perimeter Check (commits a3443d3..36d45bb):**
- Commit messages are functional throughout (describe code behavior; no scrubbing descriptions).
- DECISIONS.md additions describe architectural rationale for the shared render gate, agent switch, and test coverage fix. No proprietary methodology internals, no internal schema names, no scoring coefficients or ensemble weights.
- render_gate.py: pure stdlib Python regex/string logic. No internal identifiers.
- render_stock_report.py: inlines CGC/MAS disclosure boilerplate (publicly available regulatory text). No internal schema or warehouse names.
- sample-acme.synthetic.json: fictional company fixture, clearly labeled. No real Parallax MCP output.
- Canary-term grep (against locally maintained set of internal identifier and partner-name canaries — list not reproduced in this tracked file): 0 matches in any new tracked file or commit message. CLEAN.
- evals/results/ gitignored and untracked (0 tracked files confirmed). CLEAN.

**Attacker Perspective:**
1. render_stock_report.py reads an operator-supplied JSON file and YAML config. An attacker with write access to those files could inject arbitrary HTML through AI prose fields (investment_thesis etc.) — but these fields are served by Parallax MCP, not user-controlled in a web context. The brand-token scan in full_white_label mode provides a secondary signal if prose contains unexpected brand references. Operator-trust boundary is appropriate for a local CLI tool.
2. full_white_label mode requires client_disclaimers — the ValueError guard prevents an empty disclosures section on a regulated research document. Correct.
3. No replay/idempotency surface (no webhooks, no payment endpoints). Not applicable.
4. No privilege escalation surface (no role fields, no auth). Not applicable.
5. Large-input resilience: render_stock_report.py has no file-size limit on report_json. For operator-internal use this is acceptable; would need a guard if exposed to untrusted input. INFORMATIONAL.

SUMMARY: PASS_COUNT=4 FAIL_COUNT=0 NA_COUNT=8 CRITICAL_FAILS=0 AUDIT_DATE=2026-06-25 CHECKLIST_VERSION=1
