# Decision Log

This file captures the *why* behind each shipping milestone — alternatives that were rejected, flip conditions that would reverse the decision, verification status. For the *what* (one-line summary of what landed), see [CHANGELOG.md](CHANGELOG.md).

Conventions: each entry leads with **Why**, **Impact**, and **Alternatives**. `[DROP]` tags rejected alternatives. **Flip conditions** name the future state in which the decision should be revisited. Long entries are intentional — readers should be able to reconstruct the call without external context.

## 2026-06-11: Disclosure plumbing batch — §9.1 goes by-reference, the §9.2 exemption set is repaired and extended, and three spec-vs-practice gaps close on the spec side

**Why:** Six owner rulings from the 2026-06-10 library audit, all sharing one shape: the spec and the implementations had drifted, and in each case the implementation was right (or right enough) that the fix belonged on the spec/documentation side. (1) The standard §9.1 disclaimer was inlined verbatim in 25 SKILL.md files — every future counsel-driven wording change would need a full sweep, the exact drift class §9.2 already solved with its by-reference pattern. (2) The `_NINE_TWO_EXEMPT_SKILLS` test-gate entry `white-label-onboard` never matched the actual directory name `parallax-white-label-onboard`, so the exemption was dead code and the gate failed on main. (3) loader.md §5 rule 3 demanded an always-on ground-truth panel while every consumer (client-review, morning-brief, scenario-analysis) correctly renders it only on mismatch. (4) make-house-view's `generate` audit row carries `view_hash` / `provenance_hash` / `parent_version_id`, which §6.2 did not define for that action — formally a §6.3 violation. (5) halal-screen's Usage advertised a "build halal portfolio" mode that no workflow branch implements. (6) greenblatt.md described the default universe as mid-to-large-cap US equities while the dispatcher deliberately defaults to a sector-scoped query because broad universe-builder queries time out in production.

**Impact:**
- 25 SKILL.md files now render the standard disclaimer by reference (`Render the standard disclaimer verbatim from parallax-conventions.md §9.1.`); the 14 view-aware skills keep their two-branch structure with the reference folded into the `Otherwise:` clause. Conventions §9.1 gains a "Consumer skill reference pattern" paragraph mirroring §9.2's. Two new drift tests: no SKILL.md may inline the canonical sentence, and every exemption entry must name a real skill directory. `_ANALYSIS_DISCLAIMER_RE` extended to accept the by-reference form. Bespoke disclaimers (pair-finder's liquidity caveat, scenario-analysis's scenario wording, concierge's router wording) stay inline by design — the skill owns non-standard wording.
- `_NINE_TWO_EXEMPT_SKILLS` entries are now full directory names; `parallax-make-house-view` is added with rationale in conventions §9.2 (config artifacts + the Step 7 operator gate; downstream consumers render §9.2 themselves). `parallax-judge-house-view` is explicitly ruled NOT exempt — it emits an LLM-as-judge analysis report read directly by a person, so its `report.md` now renders §9.2.
- loader.md §5 rule 3 reworded: the ground-truth *check* is unconditional per scored holding; the integrity *table* renders only when a mismatch is detected (covering all scored holdings when it does). Mismatches are never silently dropped.
- loader.md §6.2 defines `view_hash` / `provenance_hash` for `action == "generate"` and extends `parent_version_id` to it — the fields make-house-view already emits are now spec-legal. No code changed.
- halal-screen drops the build example; Usage now states construction routes to `/parallax-portfolio-builder` followed by screening here.
- greenblatt.md documents the actual production default (sector-scoped "US large-cap consumer staples", timeout rationale, sector-by-sector merge path for broad screens) and drops the stale "Task 4 anchor test will establish a default" note. No dispatcher behavior change.

**Alternatives:** [DROP] **Enforce the always-on ground-truth table** — rejected; it adds zero signal when all names match and would regress three correct consumers to satisfy a spec sentence. [DROP] **Widen the greenblatt default to match the spec's mid-to-large-cap description** — rejected as production-breaking; broad `build_stock_universe` queries have confirmed timeouts, which is why the dispatcher scoped the default in the first place. The spec was the stale artifact. [DROP] **Implement a halal build branch** — rejected as premature; portfolio construction is portfolio-builder's job, and a real build mode needs universe integration plus the §5 rule 4 divergence assertion — a deliberate future addition, not a Usage-line patch. [DROP] **Exempt judge-house-view from §9.2 alongside make-house-view** — rejected; it fails the exemption's load-bearing condition (it emits analysis, not gated config). [DROP] **Change maker.py to emit a paired generate+save row protocol now** — deferred, not dropped; that is a code+test change on the audit-integrity path and ships in its own PR with the spec already in place after this batch.

**Flip conditions:** (a) Counsel finalizes the §9.1/§9.2 wording → edit conventions once; the by-reference conversion is what makes that a one-file change. (b) The universe builder's broad-query timeouts are fixed server-side → revisit the greenblatt default breadth (and the thematic-screen §5 rule 4 mitigation) in the same pass. (c) Real demand for a halal build mode → new workflow branch with `build_stock_universe` + divergence assertion, plus restored Usage example. (d) The deferred generate/save audit-row decision lands → if two-row wins, update §6.1's `generate` enum text and maker.py + test_maker.py together.

---

## 2026-06-10: "Compare two house views" ships as `--compare` on make-house-view — no new skill, house-view-diff untouched

**Why:** The ask was "compare two *different* house views" (e.g. two ingested firm views). Because the house-view family already carries three comparison surfaces — `house-view-diff` (portfolio output with vs. without the active view), `judge-house-view` (active view vs. live Parallax signals), and `make-house-view --shadow-diff` (fresh synthesis vs. active bank view) — this was treated as a redundancy question before a build question: an 8-agent read-only capability audit mapped the ask onto three candidate targets and source-verified each. Two were already covered: same-firm cross-version compare ships as `load-house-view --version-history` (per-version `version_diff` chain), and portfolio-output-under-two-views is `house-view-diff` with both legs pointed at non-empty bundle dirs (in-contract diff-without-verdict mode). The one genuine gap: view-level compare of two *arbitrary saved* views — `--shadow-diff` hardwires its right operand to the fresh synthesis, `judge`'s right operand is live signals, and `version_diff` only works within one view family. That gap is a rendering fold-in on `make-house-view`'s already-symmetric `diff_views` core, not a new skill.

**Impact:**
- `shadow_diff.py` gains `render_compare` + `diff_excludes` (+ helpers); **`diff_views` and `render_shadow_diff` are unchanged** — compare re-reads the existing diff buckets with neutral semantics (`added` = right-only, `bank_only` = left-only) and adds an excludes-list diff the shadow path never needed.
- `maker.py` gains module-level `run_compare(left, right)` + `_load_view_bundle` (accepts a bundle dir or a bare `view.yaml`; validates `tilts` is a dict; non-dict `metadata` degrades gracefully). `execute_synthesis` / `MakerOptions` / `MakerResult` untouched — compare is not synthesis.
- `--compare` needs nothing from Parallax at runtime: pure local file diff (verified — no MCP/network/synthesis reachable from the call path).
- Output renders tilts + excludes divergence only; the optional pillar-confidence block stays out of cross-view output.
- 22 new tests (`tests/test_compare.py`) + a second fixture view calibrated against the real anchor fixture; SKILL.md fixes the mis-routed "per-cell diff → house-view-diff" bullet (house-view-diff compares portfolio *outputs*, not views); README command-table row.

**Alternatives:** [DROP] **New `parallax-compare-house-views` skill** — rejected; two of its three would-be modes are already shipped surfaces, and the third is one rendering function on an existing symmetric core. [DROP] **Extend `house-view-diff`** — rejected as a category error: that skill is the portfolio-*output* comparator; view-level tilt compare belongs with the view-diff core in make-house-view. [DROP] **Ride `execute_synthesis` with a dummy MCP runner** (the original blueprint shape) — rejected; compare is not synthesis, and a standalone function avoids threading a fake runner through options/result types. [DROP] **Graded PASS/WEAK/KILL verdict for two real views** — deferred, not built; the existing bands mean "shift vs. an empty baseline," and a two-real-views divergence verdict needs new calibration that nothing currently demands.

**Flip conditions:** (a) An operator actually asks for a graded divergence verdict between two firms' views → that is a new calibration decision; ship its own DECISIONS entry. (b) The snapshot-store work lands with arbitrary version-pair selection → revisit whether non-adjacent same-family pairs route to `--compare` or stay in `--version-history`. (c) Cross-view output ever needs the pillar-confidence block → deliberate perimeter decision per the 2026-05-25 entry's pattern, not a silent addition.

---

## 2026-06-07: Spec-validator friction on ported skills resolved with an explicit `--normalize` build flag, not a softer validator

**Why:** The agentskills.io spec validator (PR #35) is a hard build stop, and skills ported from client conventions carry frontmatter the spec rejects (`user-invocable`, `argument-hint`, `negative-triggers`). Hand-fixing each ported skill repeats the same mechanical edits and risks silently dropping negative-trigger content. A one-command normalizer keeps the validator strict while making the port path cheap.

**Impact:** New `_parallax/scripts/spec-normalize.py` rewrites SKILL.md in place: drops `user-invocable`/`argument-hint`, moves `negative-triggers` items into a `## When not to use` body section (created after the H1 if absent; missing items appended if the section exists — content is never silently dropped). Idempotent: spec-clean files are left byte-identical. 7 tests. `build-skills.sh` gains a proper flag loop (`--no-lint`, `--normalize`), a shared `all_skill_dirs` helper, and an actionable hint on spec-validate failure pointing at `--normalize`. Source files remain the spec-clean single source of truth.

**Alternatives:** [DROP] **Keep hand-editing each ported skill at the validator stop** — rejected; recurring mechanical work with a silent-content-loss failure mode. [DROP] **Soften the validator to warnings for client-convention keys** — rejected; forfeits the spec-clean guarantee the validator exists to enforce. [DROP] **Normalize automatically inside every build** — rejected; an in-place source rewrite should be an explicit operator action, not a silent build side effect.

**Flip conditions:** (a) Client-convention ports become routine → wire `--normalize` into the port pipeline rather than leaving it manual. (b) The agentskills spec grows native support for negative-trigger metadata → update the normalizer mapping (or retire the relocation) in the same change that adopts the new spec field.

---

## 2026-06-07: agentskills.io spec compliance — directory IS the skill name, frontmatter spec-clean, validator gates the build

**Why:** Two coupled defects made the repo non-portable as a skills collection. (1) Repo directories were short aliases (`AI-buffett`, `should-i-buy`) registered under prefixed symlinks, violating the spec rule that a skill's name equals its parent directory name — and the alias↔slug indirection was the root enabler of the slug-casing mismatch class fixed in PR #33. (2) Ten skills carried house-convention frontmatter (`negative-triggers`, `gotchas` as YAML lists holding markdown prose) that was unparseable YAML, so body fragments surfaced as live skill descriptions. The decision (PR #35): make a clone of this repo a valid skills collection as-is — rename every directory to its skill name, restrict frontmatter to spec keys with the house-convention content moved verbatim into `## When not to use` / `## Gotchas` body sections, and gate the build on a spec validator so neither defect can silently return.

**Impact:** 126-file rename pass (`parallax-ai-buffett`, `parallax-should-i-buy`, …); `install.sh` and `check-registration.sh` drop the prefixing logic; new `_parallax/scripts/spec-validate.py` (allowed keys, name/directory match, description and compatibility length, metadata shape) wired into `build-skills.sh` ahead of coverage-lint as a hard stop; `coverage-lint.sh` forbids the old top-level keys and requires the body sections; `skill-structure-conventions.md` documents the rules. Follow-on: ported skills arriving with client-convention frontmatter are handled by `--normalize` (see the entry above) rather than by weakening the validator.

**Alternatives:** [DROP] **Keep alias directories + prefixing install logic** — rejected; a clone was not a valid collection, and the name/dir indirection is what let frontmatter names drift from invocable slugs in the first place. [DROP] **Teach the validator to accept the house-convention keys** — rejected; those keys are what broke YAML parsing, and the spec key-set is the portability contract for cross-runtime ports. Content was relocated verbatim, never dropped. [DROP] **Validator as warning-only** — rejected; warnings had already failed to keep frontmatter parseable.

**Flip conditions:** (a) The agentskills spec changes its allowed key-set → update `spec-validate.py` in the same change that adopts the new spec. (b) A runtime emerges that requires non-spec frontmatter keys at the source level (not foldable at port time) → revisit whether source stays spec-clean with a build-time transform, mirroring the `--normalize` direction.

---

## 2026-06-04: The 2026-06-01 library-audit fix program lands as one structural batch — guards before content fixes

**Why:** A 35-skill convention audit (2026-06-01, one reader agent per skill) traced every P0 to a single systemic cause: registered copies drifting from repo source, compounded by a name-case mismatch between frontmatter (`parallax-AI-*`) and the invocable lowercase slugs — concierge had 6 routes dispatching to nothing, and the live AI-profile skills were serving a stale pre-disclosure version. The audit's recommended order was deliberately structural-first: ship the two build guards that would have caught the drift (a JIT-load directive lint and a registration fork detector), then land the content fixes under their protection, so the same class of defect cannot accumulate silently again.

**Impact:** PR #33 — lowercase slug normalization across the 6 AI-* skills, concierge routing, thematic-screen, and shared references; `build-skills.sh` JIT-directive lint (hard-fails dangling `references/` paths, resolves local and cross-skill forms — the cross-skill form is explicitly handled because the audit's own pre-flight grep false-positived on it); new `check-registration.sh` fork detector; stale §-citation batch; AI-consensus profile count 4→5; morning-brief 5th health flag (macro misalignment) surfaced; translate-thai doubled-word detection fixed for no-space forms; credit-lens reference renderer marked test-only. PR #34 — README AI-profile table brought into line (lowercase slugs, PTJ row, "all 5 profiles").

**Alternatives:** [DROP] **Content fixes without the guards** — rejected; the audit showed the guards spec'd in `skill-structure-conventions.md` had simply never shipped, which is why the drift stayed invisible. Fixing content alone reverts to the same blind state. [DROP] **Fix the registered copies in place instead of re-pointing them at the repo** — rejected; editing copies entrenches the fork topology that caused the P0s. The registration side moves to symlinks so repo edits propagate.

**Flip conditions:** (a) `check-registration.sh` flags a fork again → treat as a registration-topology incident first, content bug second. (b) The directory-name spec work (PR #35) changes what "correctly registered" means → keep the detector's definition in sync (it was updated in #35 to drop prefixing).

---

## 2026-05-31: v2 eval substrate — spec-driven skill-agnostic engine, two-tier grading, should-i-buy two-lens upgrade, AI-buffett momentum correction

**Why:** Skill-quality regressions on SKILL.md edits are invisible to the v1 latency harness (it times wall-clock, not output correctness), and the runtime caches SKILL.md at session start so unit tests don't catch them either. The goal was a *trustworthy noise-floor baseline* — a pass-rate a real skill change can be measured against — rather than a harness that rubber-stamps passing scores. Three coupled decisions landed together:
- **Engine vs. criteria split.** Two options for the grader: (a) one monolithic `should-i-buy` checker, (b) a skill-agnostic engine where each skill ships an `EvalSpec` declaring its own required sections / check IDs / rubric criteria. Option (b) was selected so the grading engine is shared across skills and each skill declares its own contract rather than rewriting the grader per skill.
- **Two tiers, hard Tier-1 gate.** Tier-1 is deterministic structural checks (a hard gate); Tier-2 is an LLM-as-judge rubric. The judge is allowlist-guarded to Anthropic models — non-Anthropic models abort — so unpublished/proprietary skill output never leaves the perimeter via an eval rollout.
- **should-i-buy two-lens upgrade.** Splitting output into Fundamentals and Technicals lenses (new parallel `get_technical_analysis` call, +5 tokens) was chosen over folding price action into Financial Health, so the price-action read stays evidentially distinct from the factor/financial read and divergences surface rather than averaging into a blended verdict.

**Impact:**
- New `evals/` subtree: `graders/` (shared engine + CI-safe pure-function tests), `skills/<skill>/eval_config.py` (per-skill `EvalSpec`), `tasks/<skill>/core.jsonl`, `fixtures/<skill>/` (golden + broken transcripts). `should-i-buy` Tier-1 checks calibrated against real live output.
- CI runs only `cd evals && python3 -m pytest graders -q` — pure functions, never a live (token-spending) rollout. Live rollouts are manual.
- `should-i-buy` SKILL.md restructured into Fundamentals/Technicals lenses with a Technicals fallback (Momentum-factor proxy on tool timeout — the lens never silently disappears); token budget ~24 → ~29.
- `AI-buffett`: momentum mischaracterization corrected (BKP-2018 document no meaningful momentum tilt; the `<=6` criterion is a design screen, not a documented loading) across the profile, output template, and SKILL example; output discipline tightened (Steps 1–6 silent; Synthesis section now REQUIRED for every verdict including `match`).
- `portfolio-checkup` eval spec committed as **DRAFT — never run**: a spec stub, not an active eval.

**Forward integration:** New skill evals follow the `evals/README.md` "Adding a new skill eval" recipe — copy the nearest-family spec, prune `required_sections`/`check_ids`/`tier2_criteria`, add tasks and fixtures. The design doc lives in `notes/` (gitignored, local-only per the repo's handoff convention).

**Alternatives:** [DROP] **Monolithic should-i-buy-only checker** — rejected; would have to be rewritten per skill instead of reusing the shared engine. [DROP] **Allow any judge model for Tier-2** — rejected on data-perimeter grounds; the allowlist guard is load-bearing for keeping proprietary/unpublished skill output off external models. [DROP] **Fold technicals into Financial Health** — rejected; collapses the price-action vs. fundamentals distinction the two-lens split exists to preserve. [DROP] **Auto-run live rollouts in CI** — rejected; rollouts spend Parallax tokens and depend on live MCP server load (noise), so they stay manual.

**Flip conditions:** (a) A CI budget for metered live rollouts becomes available → revisit the manual-only rollout rule. (b) `portfolio-checkup` gets a real task set and is run → drop its DRAFT tag. (c) The Tier-1 checks on `should-i-buy` start mis-firing on valid output → recalibrate the matchers (they were tuned against real live output) before trusting a regression signal.

---

## 2026-05-25: Vocabulary scrub of partner-visible files — neutral finance terminology in prose, identifiers preserved, generic commit messages going forward

**Why:** A post-merge audit of PR #28 surfaced that partner-visible spec docs, skill descriptions, and the gate-confirmation display string still carried proprietary internal vocabulary (Greek-letter pillar names, framework branding) inherited from internal-roadmap drafts. The scrub was framed as a vocabulary-consistency pass rather than as proprietary-content removal: the public-facing files now describe the four house-view components in neutral finance terms (macro backdrop, valuation, market state, sentiment) and drop upstream-infrastructure attribution from the white-label onboarding README. **Scope was deliberate, not exhaustive.** Two options were considered for how to handle the discovery: (a) rewrite git history on the leak-revealing prior commits, (b) scrub forward, leave history alone, adopt a hygiene rule that future sensitive cleanups ship with generic commit subjects. Option (a) was rejected — history rewrites on a pushed branch break clone hashes for any downstream consumer of the gate's forwarded `origin` and don't actually un-publish content that was already pulled; the leak window is what it is. Option (b) was selected.
**Impact:**
- **Files scrubbed (partner-visible prose, comments, display strings):** `skills/_parallax/house-view/schema.yaml`, `skills/_parallax/house-view/loader.md`, `skills/_parallax/house-view/gate_present.py` (only the human-readable section header and four labels in `_render_gate_display`), `skills/load-house-view/SKILL.md`, `skills/make-house-view/SKILL.md`, `skills/thematic-screen/SKILL.md`, `skills/white-label-onboard/README.md` (License & Attribution section). Companion spec docs `skills/_parallax/house-view/stress-test-design.md`, `skills/_parallax/house-view/MCP_FIELD_INVENTORY.md`, and the rule description in `skills/stress-house-view/internal_rules.yaml` carried the same residual vocabulary and were scrubbed in the same pass for consistency.
- **Files deliberately not touched:** (1) `DECISIONS.md` historical entries (they describe a state of the world at decision time; rewriting them is dishonest and the leak window already closed). (2) Python identifiers — function names (`compute_omega` / `compute_phi` / `compute_xi` / `compute_psi`), class names, variable names, module names, audit-row keys — in `maker.py`, `pillar_formulas.py`, `pillar_compose.py`, etc. (data-contract stability; renaming would cascade into tests, audit log readers, and external integrations). (3) The yaml rule ID `pillar_omega_risk_on` in `stress-house-view/internal_rules.yaml` (referenced by test code at `_parallax/house-view/tests/test_stress.py:226,240` — rename would require coordinated test edit, out of scope for a doc pass). (4) Internal-only paths under `notes/` (not partner-shipped).
- **Going-forward hygiene rule:** sensitive cleanups ship with neutral commit subjects (e.g., "vocabulary consistency pass", "doc cleanup", "spec docs polish") rather than subjects that name the proprietary vocabulary being removed. The objective is to keep the *what* and *why* in the changelog + decision-log (where readers expect detail) without re-broadcasting the leak content through the commit-subject feed that mirrors anywhere a clone or fork lands. Commits a79802d and 28a0a7a follow this rule.
**Forward integration:** New skill / spec drafts pull vocabulary from the scrubbed surface (schema.yaml prose, loader.md §3 "Framework components", gate_present.py display labels) as the canonical reference. If a future feature legitimately needs to re-introduce proprietary vocabulary on a partner-visible surface — for example, a deployment where a bank licenses the upstream methodology branding — that is a deliberate decision and should ship its own DECISIONS entry, not slide in via a stale draft.
**Alternatives:** [DROP] **Rewrite git history on the leak-revealing prior commits** — rejected; doesn't recover already-pulled content, breaks downstream clone hashes (gate-forwarded `origin`), and the social signal of a rewrite is itself attention-grabbing. The honest framing is "scrub forward, accept that the leak window is what it is, change hygiene from here." [DROP] **Also rename Python identifiers and audit-row keys (full surface scrub)** — rejected; would cascade into ~10+ files of test code, audit-log readers, and a schema-version bump for the audit row format, scope-exploding a vocabulary cleanup into a data-contract migration. Field identifiers stay; only surrounding prose, comments, and display labels change. [DROP] **Scrub the white-label-onboard README more aggressively (remove all upstream provenance)** — rejected; the README still credits the upstream skill correctly under a "white-label onboarding skill for the Parallax equity research platform" framing, just without the "Part of the Chicago Global intelligence infrastructure" tagline that was inappropriate for a partner-deployable file. [DROP] **Block the scrub on closing the test-coupled `pillar_omega_risk_on` ID rename** — rejected; the description prose carrying the proprietary vocabulary is the primary partner-visible surface and was scrubbed in this pass. The ID rename can ship separately when the test edit is in scope.
**Flip conditions:** (a) A future audit finds a residual partner-visible surface still carrying the proprietary vocabulary → scrub it in a follow-up vocabulary-consistency pass, update this entry with the additional file. (b) A test refactor brings the `pillar_omega_risk_on` rule ID and its test reference into scope → rename the ID at the same time, drop the "deliberately left" note for it. (c) A licensed-methodology deployment legitimately re-introduces the upstream vocabulary on a partner-visible surface → ship a new DECISIONS entry naming the deployment, do not silently revert this scrub.

---

## 2026-05-25: Thematic-screen macro-tag annotation runs concurrently with C2/C3 — extracted as Concurrent Annotation Pattern in conventions §3.1

**Why:** The trade-idea expansion of `/parallax-thematic-screen` added a `macro_tag ∈ {with-regime, against-regime, orthogonal}` annotation derived from `get_company_info.country` against per-market `macro_analyst` tactical output. Initial draft considered three placements for the annotation step: (a) before C2 (gate `export_peer_comparison` on macro-tagged rank), (b) after C3 (annotate the rendered table), (c) concurrent with C2/C3 as a parallel step. Option (a) was the implicit default in the first plan and is the anti-pattern: it implies annotation can influence what C2/C3 consume, which would re-rank by annotation and break auditability. Option (b) serializes unnecessarily — C2 and C3 only need the rank C1 produced, which is known the moment C1 completes. Option (c) was selected. Once the same shape was identified as recurring (any skill that ranks then annotates faces the same fork), the rule was extracted into `parallax-conventions.md §3.1` as the Concurrent Annotation Pattern with thematic-screen Phase C1.5 named as the reference implementation. **Impact:**
- `thematic-screen` SKILL.md Phase C1.5 documents the pattern inline and links to the convention; the convention documents the non-negotiable constraints (no rank change, no membership change, no downstream filter-by-annotation, parallel row-preservation contract on consumers).
- Convention §3.1 is the citable home for the rule. Future skills with the same shape (rank → annotate → consume rank elsewhere) cite §3.1 and follow the reference implementation rather than re-deriving the structure.
**Forward integration:** If a future skill needs an annotation that DOES influence rank (e.g., "drop all against-regime rows from the top 5"), that is NOT this pattern — it is a re-rank or filter step and must be modeled as such, with auditability implications surfaced explicitly. Concurrent Annotation is specifically for the case where annotation enriches the output without altering selection.
**Alternatives:** [DROP] **Gate C2/C3 on macro tag (annotation-as-filter)** — rejected; inverts the rank-vs-annotation contract and breaks the composite-driven auditability that the loader.md §3 rule depends on. [DROP] **Serialize annotation after C2/C3** — rejected; pointless latency cost, and the dependency direction is wrong (annotation depends on C1 only). [DROP] **Inline the rule in thematic-screen alone, do not extract** — rejected; the shape recurs across any rank-then-annotate skill and the documented anti-pattern (re-ranking by annotation) is easy to slide into without an explicit convention. **Flip conditions:** (a) A skill genuinely needs annotation-driven re-rank → that skill is OUT of this pattern and must surface auditability implications explicitly. (b) Convention text drifts from thematic-screen reference implementation → resync, with thematic-screen as the source of truth.

---

## 2026-05-25: Description / Trigger Completeness rule is forward-only — 10+ existing flag-mode skills are accepted debt

**Why:** Two specific operator-vocabulary discoverability misses motivated the rule: (1) AI-soros basket mode (no ticker) does the same regime/reflexivity macro workflow as single-ticker mode but operators typing "trade ideas based on current macro regime" were not surfacing AI-soros — the description framed both modes around bottom-up tickering. (2) portfolio-builder `--augment-silent` flag fills house-view gaps from Parallax data per-portfolio without saved-view mutation, but the description used only flag-internal vocabulary; operators typing "fill house view gaps from Parallax data for this portfolio" did not match. A retroactive audit found 10+ additional skills with flag/mode surfaces lacking trigger phrases (judge-house-view, load-house-view, make-house-view, others). Two options were considered for scope: (a) close the entire backlog in this branch, (b) forward-only with accepted debt. Option (a) was rejected as scope explosion that would balloon the branch and dilute the focused fixes. Option (b) was selected with the backlog documented as accepted debt; new skills (2026-05-25 onward) must comply, existing skills are exempt until separately scoped.
**Impact:**
- `skill-structure-conventions.md` gains a "Description / Trigger Completeness" section with the rule, scope statement, backlog disclosure, and a proprietary-vocabulary guard (trigger phrases ship to white-label deployments via the Codex matcher — no proprietary pillar/factor/signal names in triggers).
- Two skills fixed in this branch under the new rule: AI-soros (basket-mode triggers added) + portfolio-builder (`--augment-silent` trigger added).
- Enforcement is author discipline + PR review until `build-skills.sh` grows a lint that flags `--`-prefixed Usage flags lacking trigger-keyword presence in description.
**Forward integration:** The build-time lint sketch in the convention's JIT-load check section is the implementation template when the trigger lint ships. Until then, PR reviewers should grep new-flag PRs for description coverage.
**Alternatives:** [DROP] **Close the entire 10+ skill backlog in this branch** — rejected as scope explosion; would balloon the focused tech-debt closeout and dilute reviewer attention. [DROP] **Make the rule retroactive without closing the backlog** — rejected; retroactive-without-cleanup is a soft signal that breaks the "all skills compliant" reading of the convention without actually fixing anything. Forward-only with explicit accepted-debt disclosure is the honest framing. [DROP] **Ship the build-time lint in this branch** — rejected; the lint is non-trivial (parse Usage flags, cross-reference description) and the rule itself is the load-bearing piece. Lint follows. **Flip conditions:** (a) A separately scoped pass cleans up the backlog → update the convention's scope statement and backlog count. (b) Build-time lint ships → flip the rule from author-discipline to mechanically enforced; remove "no scheduled cleanup" disclosure if the lint catches the existing backlog too.

---

## 2026-05-25: Halal-screen concentration sanity-check gated on N≥8 compliant holdings

**Why:** `portfolio-builder` Step 4 documents a `check_portfolio_redundancy` silent-failure mode where the tool returns `"well-diversified"` with `sector_concentration: {}` empty even when the portfolio has >60% single-sector concentration. The sanity-check gate (compute concentration client-side, flag the tool bug) was reused verbatim in `halal-screen` Step 2 for portfolio screening. Problem: halal screening structurally reduces the universe — typically excludes 3+ sectors (financials, alcohol/gambling, weapons/tobacco) — so small compliant subsets concentrating in 1-2 residual sectors is the expected screening outcome, not a tool defect. Firing the sanity-check on N≤7 produces a false-positive "tool bug" flag every time the screen narrowly compliant book hits its natural concentration floor. **Cutoff reasoning:** at N=8 a >60% single-sector share requires ≥5 holdings clustered in one sector, which is unlikely from natural Shariah filtering (which typically excludes ~3 sectors broadly, leaving the residual spread across ≥4–5 sectors); at N≤7 the same share can result from 4 holdings and is consistent with screening outcome, not a tool defect. **Impact:** halal-screen Step 2 sanity-check now applies only when `N≥8` compliant holdings. For N<8 the gate is skipped entirely; the natural concentration is treated as a screening outcome and not flagged as a tool bug. Portfolio-builder Step 4 + Step 6 keep the unconditional gate — portfolio-builder universes are not structurally filtered the way Shariah screens are.
**Forward integration:** If production data shows the false-positive rate diverging from the assumption (e.g., real Shariah books with N=10-12 still hitting natural concentration), retune the cutoff. The cutoff is a heuristic tied to AAOIFI/DJIM sector-exclusion shape; if Parallax's compliance logic changes which sectors get excluded, the residual-spread reasoning must be revisited.
**Alternatives:** [DROP] **Keep the sanity-check unconditional** — rejected; produces a false "tool bug" flag on every narrowly compliant halal book hitting natural concentration. [DROP] **Drop the sanity-check entirely from halal-screen** — rejected; the tool genuinely does have the silent-failure mode and large compliant books (N≥8) can hit it for real. [DROP] **Cutoff at N=5 or N=10** — rejected as too low / too high respectively. N=8 sits at the residual-sector-spread inflection point per the reasoning above. **Flip conditions:** (a) Production data shows the false-positive rate diverging from the assumption → retune the cutoff. (b) AAOIFI/DJIM sector-exclusion logic in Parallax changes → revisit the residual-spread reasoning that anchored N=8. (c) `check_portfolio_redundancy` is fixed upstream and no longer silently returns empty `sector_concentration` → drop the sanity-check entirely from both skills.

---

## 2026-05-25: Portfolio-builder Step 1 timeout fallback drops the `check_portfolio_redundancy` placeholder call

**Why:** Step 1's prior fallback instruction read "If still times out, use `check_portfolio_redundancy` placeholder and flag output." Audit found this would either fail outright or return nonsense: `check_portfolio_redundancy` requires a `holdings` parameter, and the timeout state means no candidate universe exists from which to construct one. The "placeholder" framing was a copy-paste artifact from an earlier skill draft, never executable. Two options were considered: (a) construct a synthetic holdings list (e.g., from prior portfolio context or random universe) and call the tool with that, (b) abort the screen with an explicit empty-universe state and skip Steps 3-6. Option (a) was rejected as fabricating data for compliance-relevant validation; the placeholder rationale was specifically to keep the audit trail honest. Option (b) was selected and codified as a REQUIRED empty-universe gate at the top of Phase B. **Impact:**
- Step 1 fallback sets `universe = []` and `universe_status = "unavailable"`, continues to Phase B with the empty candidate list, and flags the unavailable state in Output Format.
- Phase B opens with a REQUIRED empty-universe gate: if `universe_status = "unavailable"`, the skill skips Steps 3-6 (rank/redundancy/optimize/analyze) entirely, renders Output Format with `Universe Built` flagged as "unavailable" and no `Selected Holdings` table, and appends an audit log entry noting the abort.
- Step 6 fallback path gains a sanity-check mirror for `check_portfolio_redundancy` (matching Step 4) so the gate fires on the fallback path as well, not only on the primary call. Step 6 also documents the scope of what the fallback does NOT recover (rolling metrics, drawdown analysis, contribution attribution, performance time series — flagged "unavailable in fallback path") and the symbol-mapping caveat for `quick_portfolio_scores` on the fallback path.
**Forward integration:** The empty-universe gate is the skill-level realization of the broader "fabrication vs honest abort" rule for portfolio workflows. Other portfolio-construction skills (rebalance, scenario-analysis) should grow similar gates if their universe-build steps ever fail-open.
**Alternatives:** [DROP] **Construct synthetic holdings for the placeholder call** — rejected; fabricates data on a compliance-relevant validation surface and the audit trail would carry tool output for a portfolio that never existed. [DROP] **Retry the universe build N more times** — rejected; the second-retry-failed branch is the trigger, and the operator already absorbed one retry. Adding more retries hides the root cause. [DROP] **Surface a hard error and refuse to render any output** — rejected; the operator wants to know the screen ran and that the universe failed, with the rest of the skeleton intact for resubmission with a narrower thesis. **Flip conditions:** (a) `check_portfolio_redundancy` gains a holdings-free signature that returns a default-universe redundancy view → could re-introduce a placeholder call. (b) Upstream `build_stock_universe` stabilizes such that two-retry timeouts effectively never happen → the empty-universe gate becomes vestigial but harmless; can stay for defense in depth.

---

## 2026-05-25: AI-soros basket-mode trigger uses disjoint vocabulary; telemetry basket-membership is best-effort

**Why:** Two related fixes addressed in the same SKILL.md change. **(1) Trigger vocabulary:** AI-soros has two modes — basket (no ticker, surfaces regime themes and ranked trade ideas) and single-ticker dual-channel (industry exposure AND telemetry basket theme). Pre-fix, both modes shared the same description framing centered on bottom-up ticker analysis; operators typing "trade ideas based on current macro regime" / "regime-driven stock picks" / "reflexivity-lens stock ideas" did not surface AI-soros. The fix added three operator-vocabulary trigger phrases targeting basket-mode invocation specifically. Vocabulary is deliberately disjoint from single-ticker phrasing (no overlap with "should I buy <ticker>", "analyze <ticker>", etc.) so the matcher correctly distinguishes the two modes. **(2) Telemetry basket-membership caveat:** Channel B in single-ticker mode resolves ticker → basket assignment by name-match against `get_telemetry`'s `divergences[*].basket_name`. Per-ticker basket lookup is NOT a documented field in the get_telemetry schema; the current resolution is best-effort and may miss when a ticker is exposed to a basket theme but its name doesn't string-match. The two paths could be reconciled at the schema layer (Parallax MCP exposes per-ticker basket membership) or at the skill layer (document the limitation, treat Channel B matches as best-effort, do not silently fail-open). Schema-layer fix was deferred — requires upstream Parallax MCP work and the documented convention has not yet landed in parallax-conventions.md. Skill-layer documentation was selected as the v0 fix. **Impact:** AI-soros `description` gains the three trigger phrases per the Trigger Completeness rule. New gotcha documents the telemetry basket-membership best-effort caveat and points to the deferred convention work.
**Forward integration:** When Parallax MCP grows a per-ticker basket-membership field (or per-ticker telemetry exposure scoring), update Channel B in AI-soros to consume the new field directly and remove the best-effort caveat. Document the convention in `parallax-conventions.md` and reference it from this skill's gotcha.
**Alternatives:** [DROP] **Add per-ticker basket inference via LLM judgement on basket descriptions** — rejected; would introduce a fabricated signal where the data layer is silent. The honest framing is "best-effort name-match, treat as partial". [DROP] **Remove Channel B entirely until schema lands** — rejected; the partial signal is genuinely useful when the name-match hits, and the dual-channel framing is load-bearing for single-ticker mode's auditability. [DROP] **Fix at the schema layer in this branch** — rejected as out of scope for the tech-debt closeout; requires upstream MCP work. **Flip conditions:** (a) Parallax MCP exposes per-ticker basket membership → swap name-match for direct field access; remove the caveat gotcha; document the convention. (b) Channel B name-match false-negative rate diverges materially from acceptable → revisit the basket-mode framing or remove Channel B until the schema lands.

---

## 2026-05-24: Maker `calibration_status` is hardcoded `heuristic_phase0` — v2 plan §10 retention #3 is wrong for the maker path

**Why:** E2E calibration tests (`tests/test_e2e_calibration.py`, added 2026-05-24) revealed that the v2 plan's claim "calibration_status passthrough requires zero code change" is only true for the `load-house-view` skill (LLM-orchestrated, reads `manifest_cache.load_manifest()` at runtime per SKILL.md Step 4 6b). The `make-house-view` skill is Python code and currently hardcodes `"heuristic_phase0"` in both view.yaml (`maker.py:470`) and audit row (`maker.py:655`). Documented as intentional in `make-house-view/SKILL.md:124`. Therefore: when calibration ships and the manifest is signed, `/parallax-load-house-view` views auto-flip to `empirical_phase1`, but `/parallax-make-house-view` views will continue shipping `heuristic_phase0` until `maker.py` is wired to consult `manifest_cache`. **Impact:**
- `tests/test_e2e_calibration.py::test_e2e_mocked_empirical_manifest_flips_save_to_phase1` is wrapped in `pytest.mark.xfail(strict=True)` as a tripwire — the day `maker.py` is wired, the xfail becomes XPASS-as-failure and forces conversion to a positive assertion. Equivalent of a `// TODO` that the test runner enforces.
- Until wired, generated views in `empirical_phase1` deployment scenarios will mis-disclose: regulatory disclosure banner per `parallax-conventions.md §9.2` reads "heuristic" when the calibration manifest is actually signed.
**Forward integration:** Maker needs (1) import of `manifest_cache`, (2) call to `load_manifest()` at save time, (3) same `methodology_section != "GUESS" AND backtest_ref is non-null` check that load-house-view's prose uses, (4) write the resulting status into view.yaml and audit row instead of the hardcode at `maker.py:470,655`. Estimated 1-2 hours of work; gated on the calibration manifest itself being available (currently roadmap). **Out of scope (this entry):** Wiring change deferred until calibration ships.
**Alternatives:** [DROP] **Fix maker.py now to read manifest_cache** — rejected today; no calibrated manifest exists yet, so the wire-up would be unverifiable end-to-end and the hardcode is the honest current state. [DROP] **Remove the tripwire xfail and accept the gap silently** — rejected; xfail-strict ensures the gap is noticed at the right time (when the manifest ships) rather than missed. **Flip conditions:** (a) Calibration manifest is signed and available → wire maker.py to manifest_cache + remove the xfail. (b) Calibration shipping is cancelled → keep the hardcode permanently and remove the xfail with a one-line DECISIONS amendment noting the cancellation.

---

## 2026-05-24: Judge bundle-name nonce — silent report overwrite under sub-second concurrent runs

**Why:** E2E concurrency tests (`tests/test_e2e_concurrency.py`, added 2026-05-24) surfaced that two judge runs against the same view within one wall-clock second silently overwrote each other's report bundle. The bundle name at `judge.py:614` was `{judged_version_id}-{seconds-precision timestamp}` — colliding for sub-second back-to-back runs. Audit chain was safe (`audit_chain.append_entry` uses `fcntl.flock(LOCK_EX)` per audit_chain.py:193) so audit.jsonl preserved all rows, but the bundle directory (which is the ONLY home for `report.md`, `report.json`, `mcp_responses.jsonl`, `audit_entry.json`) silently overwrote on the second write. Production manifestation: cron-fired auto-on-load drift check completing in the same second as an operator-triggered `/parallax-judge-house-view` → operator opens what they think is their report and gets the cron's, with no warning anywhere.
**Impact:** `judge.py:614` bundle_name now appends a 6-char `uuid.uuid4().hex[:6]` nonce: `{version_id}-{timestamp}-{nonce}`. Probability of nonce collision per pair of same-second runs ≈ 6e-8 (16.7M possibilities; was 1.5e-5 at 4 chars before code-reviewer flag — initial DECISIONS draft said 1.5e-9 which was mathematically wrong). Operationally negligible for production traffic shape (a few judge runs per day per client; even at 100/sec the birthday-bound collision probability stays under 1e-4 per same-second cohort). Test `test_e2e_serial_judge_runs_produce_distinct_audit_rows_and_reports` upgraded from xfail tripwire to passing assertion. Total tests: 329 passing + 1 remaining xfail (maker calibration tripwire above).
**Forward integration:** None — fix is self-contained. Bundle path consumers (none today; the path is opaque to callers via `JudgeResult.report_dir`) get a slightly longer path with no semantic change.
**Out of scope:** Same nonce pattern could be added to the maker's view-archive directory if we ever observe a similar collision there — not surfaced by current tests, defer.
**Alternatives:** [DROP] **4-char nonce (initial fix)** — rejected after code-reviewer flagged the math: 4-char = 65,536 possibilities → ~1.5e-5 collision per pair → not operationally safe given `render_judge.write_report_bundle` uses `mkdir(exist_ok=True)` (silent overwrite on collision). [DROP] **4-char nonce + collision-retry guard** — rejected; cleaner to widen the keyspace than add retry logic and an `if exists` branch. [DROP] **Switch to microsecond-precision timestamps without a nonce** — rejected; microseconds still race under heavy load, and the nonce gives stronger collision protection at negligible cost. [DROP] **Add file locking around bundle directory creation** — rejected; locks the wrong abstraction; the issue is name collision not write race. [DROP] **Refuse to start a judge run if a bundle dir with the same name exists** — rejected; would require pre-flight stat and surfaces as a user-visible error for what should be transparent disambiguation. **Flip conditions:** (a) Collision rate observed in production telemetry > 1 in 10⁶ → bump nonce to 8 hex chars. (b) Operator UX needs sortable bundle names → revisit naming scheme entirely (probably keep millisecond timestamp first, then nonce). Audit context: discovered by `test_e2e_concurrent_judge_runs_preserve_chain_integrity` in the concurrency test batch; nonce width tuned after code-reviewer review (4 → 6 chars).

---

## 2026-05-24: Make + judge as paired peer skills in the house-view family (`/parallax-make-house-view` + `/parallax-judge-house-view`)

**Why:** A bank-anchored Parallax MCP client asked for two things: (1) monitor whether their in-house view is going stale and recommend updates with reasoning, and (2) eventually generate views from scratch when the bank's CIO is between cycles. Initial framing tried to ship the monitor (`judge`) first as the narrow wedge and defer the generator (`maker`); user override picked the generator as the wedge with the monitor as a peer. Architect + adversarial review confirmed the two share enough infrastructure (cross-country aggregator, pillar formulas, audit chain, manifest gate, confirmation gate) that building them as peers — rather than as a primary + extension — keeps the methodology inspectable and the paired uninstall semantics clean. Both skills route through the same `load-house-view` Step 4 save path (maker) or read-only stress-test path (judge), reusing audit_chain / chain_emit / manifest_cache / view_status untouched. **The fundamental architectural choice was to keep methodology client-side (in the `make-house-view` skill) rather than wrap it behind a server-side MCP tool, so bank model-validation teams can inspect the cross-country reconciliation rules and pillar composition formulas directly.**

**Impact:**
- **Two new skills shipped:** `skills/make-house-view/` (8 files: SKILL.md, maker.py, cross_country.py, pillar_compose.py, pillar_formulas.py, prose_synth.py, shadow_diff.py, tests/; 55 tests) and `skills/judge-house-view/` (7 files: SKILL.md, judge.py, drift_classify.py, recommendation.py, render_judge.py, cadence.py, tests/; 46 tests). Cross-linked SKILL.md headers. `judge.py` imports maker's `cross_country` / `pillar_compose` / `pillar_formulas` via the `sys.path` injection pattern stress.py established for shared infra (hyphenated skill dirs aren't directly importable as Python packages).
- **Shared infra extended** in `skills/_parallax/house-view/`: new `gate_present.py` (extracts load-house-view's Step 3 confirmation gate into `GateContext` + `run_gate_loop` so maker can reuse without code duplication — preserved structural-identical audit output for the ingest path via 24-test regression coverage), new `provenance_classes.py` (6-class registry + `validate_provenance_entry(mode="read"|"write")` enforcing `parallax_data_fill` deprecation), new `aggregator_weights.yaml` (v0 MSCI ACWI shares aligned to 14 MCP-covered markets, sum normalized to 1.0), new `MCP_FIELD_INVENTORY.md` (Phase A0 artifact documenting actual MCP surface).
- **Pillar formulas use prose extraction, not structured fields.** Phase A0 live MCP smoke calls verified `telemetry.signals` does NOT carry `valuation_z`, `vix_percentile_12m`, `breadth_z`, or `ig_spread_z` as discrete fields (architect review's HIGH-severity concern was empirically confirmed). But valuation_state and market_entropy values ARE embedded in `macro_analyst.content` prose for some markets (US prose says "Valuation metrics at -1.00 and entropy at 0.00"; Japan/China silent). `pillar_formulas.compute_phi/xi` therefore extract via regex from prose, gated by the 60% coverage rule in `cross_country.aggregate`. Pillar functions return `(value, confidence, missing_inputs)`; when `missing_inputs` is non-empty, confidence is capped at `LOW_CONFIDENCE_THRESHOLD - 0.05 = 0.35` (strictly below `gap_detect.LOW_CONFIDENCE_THRESHOLD = 0.4`) so the gate's low-confidence block surfaces them.
- **`stress.py` extended backward-compatibly.** `build_recommended_deltas` gained `include_fresh: bool = False` (default preserves DIVERGENT_STALE-only behavior; judge passes `True` to also emit DIVERGENT_FRESH deltas with `kind="informational_fresh"`). `validate_recommended_deltas` allowlist extended from `{"informational", "global"}` to include the new kind. All 73 pre-existing stress tests still pass.
- **`loader.md §6.1/§6.2` extended.** Action enum gained `generate` (carried by maker on save) and `judge` (carried by judge on every report, `applied=false` always). Conditional field tables added. `composition_formula` / `aggregator_weights_ref` / `source_snippets` live in `provenance.yaml` only — never on audit rows — per §6.3 "no skill-specific custom keys" rule.
- **Consumer integration narrowed to 3 skills.** `portfolio-builder/`, `rebalance/`, `thematic-screen/` JIT-load `_parallax/house-view/auto-on-load-judge-pattern.md` for a dry-run drift check on view_age > 30d (banner only on drift_material). `morning-brief/` got a conditional one-liner pointing at `/parallax-judge-house-view` when its existing Batch B alignment check detects ≥3 misaligned holdings — NOT an auto-invocation because the brief already does its own macro_analyst fan-out per Batch A and a second one would duplicate work. `client-review/` (compliance risk in client-facing output), `explain-portfolio/` (replaying past decisions), and `should-i-buy/` + `deep-dive/` (no portfolio math) explicitly EXCLUDED.
- **Backward-compat locked in by Phase G uninstall test.** `test_disable_maker_leaves_judge_functional` + `test_disable_judge_leaves_maker_functional_and_consumers_skip_gracefully` + `test_disable_both_yields_structural_audit_parity` (the last asserts ingest output structurally identical to a committed golden baseline, excluding `ts`/`prev_entry_hash`/`entry_hash`/`synthesis_timestamp`/uuid-and-hash fields, with a separate `audit_chain.verify_chain()` integrity assertion — reframed from "byte-identical" because audit timestamps make true byte-identity unachievable without time injection through audit_chain).
- **Calibration ships as `heuristic_phase0`.** Per existing `manifest_cache.load_manifest` logic, both maker-generated views and judge reports carry `calibration_status="heuristic_phase0"` until the calibration manifest's `methodology_section != "GUESS"` AND `backtest_ref != null`. Required disclosure surfaces automatically via the existing `parallax-conventions.md §9.2` wiring (the `generator_synthesis` provenance class is treated as "AI-assisted output" the same as `prose_extraction` — narrative is LLM-generated, structured tilts come from deterministic cross-country aggregation + prose-extracted pillar formulas; the §9.2 banner correctly characterizes both layers).
- **Total tests added:** 308 across the house-view family (was ~150 pre-make/judge). Three integration tests in `tests/test_integration_make_judge.py` exercise maker→save→judge end-to-end with mocked MCP; three uninstall tests in `tests/test_uninstall.py` lock in plug-and-play semantics.

**Forward integration:**
- **Phase D (server-side `house_view_judge` MCP tool) CANCELLED, not deferred** (decision locked 2026-05-24). The judge runs client-side permanently. Bank clients schedule their own cron against `/parallax-judge-house-view --json` on their side. Rationale: methodology transparency for bank model-validation review (same reason maker is client-side), zero cross-tenant blast radius from a broken judge run, no need for cross-org auth plumbing to access bank-local view directories. Do NOT resurrect the framing without a new architectural decision.
- **Calibration roadmap** (long pole): empirical_phase1 unlock requires (a) pillar formula weights calibrated against backtested forward equity outcomes, (b) aggregator market weights refit from rolling-window beta-to-global-equity, (c) signed manifest with non-GUESS methodology_section and non-null backtest_ref. Backtest harness depends on Parallax MCP exposing historical telemetry snapshots (today's `get_telemetry` is current-state only) — single biggest server-side prerequisite.
- **Untested MCP components** (6 of 9 still uninspected per `MCP_FIELD_INVENTORY.md §6`): `currency`, `fixed_income`, `liquidity`, `news`, `sector_positioning`, country-level `factors`. Should be smoke-tested before any pillar formula extensions reference them. CI smoke test (`tests/integration/test_mcp_field_inventory.py`) catches MCP surface drift monthly without flagging additive changes.
- **Judge LLM model identity not yet captured in reasoning chain envelope.** `chain_emit` pins `manifest_ref` + `skill_version` but not the LLM model used for psychological_wavelength scoring or judge recommendations. Acceptable for Phase 0; needs addressing before empirical_phase1 (model identity is a load-bearing input to reproducibility).

**Out of scope (follow-up):**
- **External-model breadth review still pending.** Plan went through adversarial-reviewer (Claude-only depth) but NOT plan-swarm (needs SendMessage which wasn't loaded this session). Paste-ready prompt for a fresh session lives at `notes/2026-05-24-house-view-swarm-prompt.md` — public-repo authorization is already given.
- **`shadow_diff` CLI flag** for `/parallax-make-house-view` shipped as part of B1 but client-side bank-view-diff UX may need refinement after first real bank engagement.
- **Maker's `--basis "<hint>"` flag** routes hint text into the macro_analyst orchestration as a bias prompt; behavior is heuristic in v0 and may need empirical tuning.

**Alternatives:**
- [DROP] **Generate as a flag on the monitor (judge --generate)** — rejected; collapses the methodology surface and forces operators to pick "monitor my view" or "build me a view" through the same CLI when the two have meaningfully different semantics (read-only vs write).
- [DROP] **Server-side `house_view_make` and `house_view_judge` MCP tools** with maker fully behind an API — rejected; hides cross-country reconciliation + pillar formulas from bank model-validation review, which is the single highest-value transparency surface for the institutional client. Judge still gets a future server-side endpoint (Phase D deferred) because cron use is the bank's actual workflow.
- [DROP] **Auto-on-load judge banner in 6 consumer skills** (original plan) — narrowed to 3 after architect review flagged: (a) morning-brief duplicates the brief's existing macro_analyst fan-out, (b) client-review compliance risk from drift banners in client-facing output, (c) explain-portfolio replays past decisions where drift-since-then is expected.
- [DROP] **Pillar formulas use structured `telemetry.signals` fields** (v1 plan assumption) — rejected after Phase A0 verified those fields don't exist. Switched to prose-extraction with 60% coverage gate; `MCP_FIELD_INVENTORY.md` documents the surface for future maintainers.
- [DROP] **Maker as "whole-view gap-fill applied to an empty draft"** using `gap_detect`/`gap_suggest` — rejected after architect verified `gap_detect.detect_gaps` returns `[]` on blank drafts and `gap_suggest`'s 4 hard-coded regime rows can't synthesize a view. Maker uses explicit 8-step orchestration; `gap_suggest` keeps its intended finishing-pass role on populated drafts.
- [DROP] **Phase G byte-identical audit assertion** — rejected because audit `ts` and `prev_entry_hash` make byte-identity unachievable without time injection through `audit_chain.append_entry`. Reframed to structural-field parity (exclude time-varying fields) plus separate chain-integrity check.
- [DROP] **`kind="informational_fresh"` overloaded onto existing kind field without validator update** — caught by adversarial review (would fail `validate_recommended_deltas` on every judge call). Extended allowlist instead.

**Flip conditions:**
- (a) Bank model-validation team requests methodology behind an opaque API instead of inspectable code → re-evaluate maker server-side wrapper; weigh transparency vs. IP-leak concerns.
- (b) ~~Supabase managed backend ships → activate Phase D server-side `house_view_judge`~~ — **REMOVED 2026-05-24.** Phase D was cancelled, not deferred. A future managed-service offering for the judge would need to be a separate architectural decision with a different multi-tenant auth story; do not resurrect the old framing as a flip path.
- (c) Backtest harness shipped + manifest signed → calibration_status auto-flips from `heuristic_phase0` to `empirical_phase1` per existing `manifest_cache.load_manifest` logic; no code changes in make/judge.
- (d) Parallax MCP exposes structured `valuation_z`/`vix_percentile_12m`/`breadth_z` fields → pillar formulas valuation_state/market_entropy can swap prose extraction for direct field access; gate the swap behind a new `pillar_formulas.compose_phi@v2` etc. with provenance `composition_formula` field as the replay anchor.
- (e) Bank-specific UX overrides needed (custom column ordering, branded report headers) → judge `--format [default|bank_model_validation]` flag mirroring the white-label integration pattern.
- (f) Plan-swarm external-model breadth surfaces an architectural concern not caught by Claude-only review → re-evaluate the relevant section of v2 plan; track via DECISIONS.md amendment.

Audit context: 4-pass review trail (Plan v1 → feature-dev:code-architect → Plan v2 → adversarial-reviewer Hunter/Skeptic/Referee), 7 verified bugs all fixed (1 critical + 6 medium; 4 from adversarial review, 2 discovered during Gemini-dispatched Phase E integration tests, 1 additional latent bug in stress.py r['dim'] bare access from code-reviewer pass), 3 build phases (E/F/G) dispatched via `/gemini-implement` to Gemini 3 Pro Preview with results validated against the test suite.

---

## 2026-05-18: AI-interaction disclosure §9.2 wired to all analysis-producing consumer skills

**Why:** Technical-debt audit (2026-05-18) identified the §5 rule 6 banner as missing from 17/17 wired SKILL.md files — a regulatory output gap under HKMA/SFC GenAI Nov 2024, MAS FEAT, and EU AI Act Art 13/50. Original placeholder banner in `house-view/loader.md` §5 rule 6 was technically incorrect: it claimed "factor scores...were produced by large-language-model analysis," but Parallax factor scores come from CG's deterministic pipeline, not from an LLM. Conflating deterministic scoring with LLM synthesis would mislead clients on a regulatory disclosure and weaken the SR 11-7 conceptual-soundness story.
**Impact:**
- **`_parallax/parallax-conventions.md` §9** restructured: §9.1 (existing standard disclaimer, unchanged wording) + new §9.2 (AI-interaction disclosure, single source of truth). Banner text: *"AI-assisted output. Structured data — factor scores, financials, peer mappings, technicals — is from Parallax's deterministic pipelines; see the Provenance footer for per-source attribution and version, and the trace ID for end-to-end output attribution. Narrative, synthesis, and recommendations are LLM-generated; verify any specific statement before acting."* Reads correctly today (trace IDs surface in Provenance footers) and reads correctly after Vincent's attribution-API endpoint ships (clients know what to do with the trace ID) — no re-edit across 17 skills needed at endpoint launch.
- **`_parallax/house-view/loader.md` §5 rule 6** now references `§9.2` rather than carrying inline banner text. COUNSEL-TBD marker narrowed from "wording + URL pending" to "final wording subject to counsel sign-off; no methodology URL until approved." The earlier placeholder text is explicitly retired with a note explaining why.
- **16 SKILL.md files** updated with one-line directive immediately above each Disclaimer block: `Render parallax-conventions.md §9.2 immediately above the disclaimer below.` No inline banner text — single source of truth in §9.2. deep-dive's Assessment section retains its contextual-proximity AI disclosure (Perplexity-backed content) in addition to the document-level banner.
- **`white-label-onboard` exempted** from §9.2 because it emits YAML config and audit artifacts, not LLM-generated client-facing analysis. Exemption is encoded in the test gate (`_NINE_TWO_EXEMPT_SKILLS`) so the decision is greppable in code.
- **New test gate** in `_parallax/white-label/tests/test_integration_pattern_referenced.py`: `test_wired_skill_renders_ai_disclosure` asserts every wired SKILL.md that carries the standard analysis disclaimer also references `parallax-conventions.md §9.2`. Unit-level coverage for the §9.2 reference regex included. New SKILL.md files producing analysis output without the §9.2 reference fail at PR review.
**Forward integration:** Attribution-API endpoint is in progress (owner: Vincent). Endpoint URL is deliberately NOT in the banner — when it ships, the one-line addition to §9.2 ("Resolve a trace ID at <url>") propagates to all 17 wired skills automatically via the single source of truth. The banner reads correctly today (trace ID exists in Provenance footer) and reads correctly after launch.
**Out of scope (follow-up):** ~~Three unwired client-facing skills (`watchlist-monitor`, `score-explainer`, `earnings-quality`) and the AI-persona meta-skills (`AI-buffett`, `AI-soros`, `AI-klarman`, `AI-greenblatt`, `AI-consensus`, plus `credit-lens` and `halal-screen`) carry the standard disclaimer but lack the white-label sentinel.~~ **Resolved 2026-05-25** — §9.2 reference now wired into all 11 previously-unwired analysis skills (10 via commit 947c833; `AI-ptj` via commit a4acee1) using option (b): §9.2 reference without the white-label sentinel. Test-gate scope unchanged (wired-skill set).
**Alternatives:** [DROP] **Inline the banner in each SKILL.md** — rejected; creates the same drift surface that drove §9.2 centralization. [DROP] **Anticipatory wording mentioning "attribution API (rolling out)"** — rejected; v2a phrasing makes a timeline commitment that ages badly if any slip occurs, and counsel reviews forward-looking language twice. [DROP] **Wait for counsel sign-off before shipping any banner** — rejected; the gap is a live regulatory issue and the working banner is materially more accurate than the prior placeholder; counsel review on the *final* wording can run in parallel with the working banner in production. [DROP] **Generic "AI-generated output" banner** — rejected; conflates deterministic scoring with LLM synthesis and misrepresents which parts of the output are AI-generated. **Flip conditions:** (a) Counsel approves final wording → edit §9.2 once; all 17 skills propagate; (b) Vincent's attribution API ships → append "Resolve a trace ID at <url>" to §9.2; all 17 skills propagate; (c) follow-up sweep wires the 8 unwired client-facing skills → extend `_NINE_TWO_EXEMPT_SKILLS` only for genuine config-artifact skills; (d) regulator requires model identification ("Claude" vs "large language model") → §9.2 single edit. Audit context: technical-debt-manager 2026-05-18 finding "Critical Item 1."

---

## 2026-05-07: Discoverability layer over new endpoints — response to external 14-item improvement memo
**Why:** External integrator submitted a 14-item Parallax capability memo (P0/P1/P2/Docs tiers, plus an architectural recommendation that every skill ship a parallel REST endpoint). Council (5 advisors + 3 peer reviewers + chairman, MCP domain, Standard scope) converged on the Feasibility Analyst's repo inventory: ~6-7 of the "missing" items already ship as production skills the integrator hadn't discovered (`scenario-analysis` = stress book, `explain-portfolio` = drawdown attribution, `AI-consensus` exists, `get_score_analysis` returns a `weeks` history window, `analyze_portfolio` already covers concentration / redundancy proxies). A 5-holding `analyze_portfolio` schema probe confirmed the residual ~4-5 are genuinely net-new. Adversarial review then corrected execution details: sequencing (provenance audit must precede substantive reply), reply framing (lead with REST acknowledgment, not "you missed skills"), and the optimizer disposition (split full-engine REJECT from constrained rebalance-helper OPEN).
**Impact:** (1) **§7 Capability Index** added to `skills/_parallax/parallax-conventions.md` — JIT-loaded by every skill; answers "I want to do X, which skill/tool?" for the 6-7 capabilities the integrator missed, plus an explicit "Known gaps" subsection so future integrators don't re-litigate them. (2) **`skills/_parallax/response-schemas.md`** — new shared reference documenting nested structures of `rolling_metrics`, `drawdown_analysis`, `concentration_metrics`, `company_contribution`, `portfolio_scores`, `latest_holdings`, `data_quality`. Source for upstream OpenAPI nested-schema docs. (3) **PM/RIA vocabulary aliases** added to `scenario-analysis/SKILL.md` ("stress book", "scenario suite", "stress test") and `explain-portfolio/SKILL.md` ("drawdown attribution", "performance attribution") — eliminates the search-by-vocabulary failure mode without touching skill logic. (4) **Cross-repo follow-ups** (symbol-table aliasing for Bloomberg-style class shares like `BRK.B`, fuzzy-search suggestions wrap on `data_quality.missing_rics`, OpenAPI nested-schema paste, risk-weighted Effective N field-add) deferred to the upstream API repo; logged locally outside this tracked file. **Reject:** every-skill-ships-parallel-REST as a blanket architectural mandate (multi-profile orchestration with judgment fusion does not translate to a stable REST contract without freezing the ensemble); full portfolio optimizer with alpha model and committed covariance contract (high switching cost, established incumbents, methodology-evolution drag); lot-level tax-aware rebalance (regulatory exposure, category mismatch). **Defer pending audit:** portfolio-level score-history rollup endpoint — must complete a score-history exposure review before scoping. **Defer pending pilot signal:** constrained rebalance-weights helper (distinguished from full optimizer); benchmark recommender; ADV/liquidity field family; pairwise correlation matrix.
**Alternatives:** [DROP] **Ship 14 net-new endpoints in priority order** — rejected on Feasibility's denominator finding: ~half the list duplicates existing skills, so prioritizing the original list optimizes the wrong space. [DROP] **Reject the entire memo and run a 30-day institutional discovery sprint before any action** — Skeptic's recommendation, dismissed because the inventory probe answers the cheap question (already-shipped vs. genuinely-new) in hours, while the discovery sprint solves a different question downstream. [DROP] **Lead with portfolio-level score-history endpoint as the institutional demo artifact** — three of five advisors recommended this; rejected after peer review surfaced that the endpoint shape requires an exposure audit before scoping; deferred pending audit completion. [DROP] **Ship a constrained rebalance helper this quarter** — distinguished from the full-optimizer rejection but kept on hold pending pilot-signal evidence rather than preemptive build. **Flip conditions:** (a) score-history exposure audit completes with a controlled access design → ship a `score_history` endpoint with universe caps + rate limits as the consolidation surface; (b) two paid pilots independently surface "constrained rebalance weights" as a deal-qualification ask → promote that helper from defer to scope, with explicit "not a recommendation" framing; (c) integrator population shifts from individual power users to platform-OEM buyers at the retail-brokerage scale → revisit the every-skill-REST architectural framing under platform-OEM economics rather than judgment-layer-moat economics. Full council deliberation, schema probe results, and adversarial review held locally outside this tracked file.

## Index

- [2026-05-06 (later) — white-label-onboard restructure: extract.py package split first, SKILL.md split conditional](#2026-05-06-later-white-label-onboard-restructure-extractpy-package-split-first-skillmd-split-conditional)
- [2026-05-06 — Extend white-label-onboard with PPTX/DOCX + voice; reject new client-brand-ingest skill](#2026-05-06-extend-white-label-onboard-with-pptxdocx--voice-reject-new-client-brand-ingest-skill)
- [2026-04-27 — Ingest stays pure; augmentation moves to consumer-skill JIT](#2026-04-27-house-view-ingest-stays-pure-data-augmentation-moves-to-consumer-skill-jit)
- [2026-04-26 — Gap-fill: suggest-with-per-field-accept](#2026-04-26-house-view-ingest-gap-fill--suggest-with-per-field-accept)
- [2026-04-25 — Calibration manifest client + chain emitter shipped (Batch 3+3.5+4)](#2026-04-25-batch-3354-shipped-calibration-manifest-client--chain-emitter--implementation-milestone)
- [2026-04-25 — Pre-Batch-2 security-auditor hardening](#2026-04-25-security-auditor-pass-pre-batch-2-hardening-of-calibration-manifest-spec-triad)
- [2026-04-25 — Calibration manifest spec triad v1 LOCKED](#2026-04-25-later-calibration-manifest-spec-triad-v1-locked--endpoint--schema--reasoning-chain)
- [2026-04-25 — Tier 2 reframe: compliance-first sequence](#2026-04-25-tier-2-reframe--compliance-first-sequence-one-pager--hash-chain-original-tier-2a2b-not-the-right-next-batch)
- [2026-04-24 (eve) — XAI deep research validates Phase 0.5, +1 gap](#2026-04-24-evening-xai-compliance-posture--deep-research-validates-phase-05-architecture-surfaces-1-new-gap)
- [2026-04-24 — Single-stock §7 split + render_view_conflict() seam](#2026-04-24-late-morning-house-view-single-stock-7-split--render_view_conflict-seam)
- [2026-04-24 — Extraction auditing: defer per-tilt provenance, ship --why on-demand](#2026-04-24-midday-house-view-extraction-auditing--defer-per-tilt-provenance-ship---why-on-demand)
- [2026-04-24 — §7 compliance memo as parallel track](#2026-04-24-midday-7-compliance-memo--parallel-track-not-blocking)
- [2026-04-24 — Calibration architecture Option C](#2026-04-24-pm-calibration-architecture--option-c-hybrid-signed-manifest-d-compatible)
- [2026-04-24 — Corpus publishing budget](#2026-04-24-corpus-publishing-budget--100-public-labeled-tilts-2-bundles-per-view-family-no-pillar-integers)
- [2026-04-23 — Diff harness as spec-only, internals local](#2026-04-23-pm-house-view-diff-harness-shipped-as-spec-only-internal-artifacts-stay-local)
- [2026-04-23 — Don't build /hv/compile as IP-protection](#2026-04-23-dont-build-hvcompile-as-an-ip-protection-move)
- [2026-04-22 — Next increment = portfolio_diff_harness](#2026-04-22-next-increment--portfolio_diff_harness-not-more-schema)
- [2026-04-21 — Phase 0 iteration path](#2026-04-21-phase-0-iteration-path--schema-first-week-1-loader-rewrite-next-sprint)

---

## 2026-05-06 (later): white-label-onboard restructure: extract.py package split first, SKILL.md split conditional
**Why:** SKILL.md (969 lines) and extract.py (909 lines) are repo outliers — next-largest skill SKILL.md is 340 lines. Council (5 advisors + 3 peer reviewers; unanimous on Advisor 5's plan) chose phased restructure to minimize blast radius rather than a single bundled refactor.
**Impact:** Phase 1 ships first — `extract.py` → `_parallax/white-label/extract/` package with `__init__.py` re-export shim that preserves `from extract import (...)` verbatim, gated by 102 passing tests; precedent exists in `_parallax/house-view/`. Phase 2 (SKILL.md split into ~150-line orchestrator + 2-3 references/) is **deferred**, gated on (a) a JIT-load compliance transcript audit confirming Claude actually loads referenced files when directed, AND (b) a `parallax-conventions.md` amendment so the pattern propagates to the other 16+ skills rather than drifting. Pre-flight checks (DECISIONS.md anchor grep, `importlib`/`__file__` test grep, transcript audit) precede any code moves. Framework citations (Lago/Rezvani/Genesys) → `references/voice-frameworks.md` (universal advisor agreement).
**Alternatives:** [DROP] Skip restructure — rejected; SKILL.md token cost at session start is real and measurable. [DROP] Bundle extract.py split + SKILL.md split in one commit — rejected; triples failure surface, harder to revert. [DROP] Add INDEX.md / NAVIGATION.md — rejected by 4/5 advisors; third source of truth competing with SKILL.md and DECISIONS.md, will drift. [DROP] Build full "client-config ingestion platform" abstraction (Advisor 3) — deferred; sibling skills (client-watchlist-ingest, client-policy-ingest) don't exist yet. Revisit if/when a second consumer materializes. Full council session record at `notes/2026-05-06-1023-council-white-label-restructure.md`.

---

## 2026-05-06: Extend white-label-onboard with PPTX/DOCX + voice; reject new client-brand-ingest skill
**Why:** Operator brainstorm started with "let's build a `client-brand-ingest` skill that extracts brand colors and voice from a folder of client collateral." Office-hours forcing-question pass surfaced a paying customer and reframed the risk from extraction precision → downstream-skill integration. Premise check then revealed the real issue: `parallax-white-label-onboard` already does ~70% of the proposed work — extracts colors/logos/fonts from URL or PDF, has WCAG validation, confirmation gate, hash-chained audit, and is wired into three downstream consumer skills (client-review, due-diligence, deep-dive). Building a new skill would have duplicated production-grade infrastructure, broken three downstream wirings, and produced two competing brand-config artifacts that diverge over time.
**Impact:**
- `parallax-white-label-onboard` extended additively: PPTX (`extract_from_pptx`) and DOCX (`extract_from_docx`) input modes parse OOXML theme XML directly (`ppt/theme/theme1.xml`, `word/theme/theme1.xml`) for high-confidence (0.9) colors and fonts; body text is aggregated for voice corpus.
- Folder mode iterates `.pptx`/`.docx`/`.pdf` files. Multi-source mode accepts any combination of URL + docs + folders.
- New `merge_drafts()` and `cross_validate_visual()` helpers — multi-source mismatches are flagged at the confirmation gate, never silently auto-resolved (PM/CIO decides which version of their brand is canonical).
- New `voice` section in schema.yaml: Lago 7-section template (positioning / tone / core_rules / anti_filler / audience_adaptation / channel_notes / drafted_vs_sent / company_context / disclaimers) plus Rezvani-style tone matrix. Voice extraction is LLM-driven via SKILL.md prompting (Step 1.5), not regex — Python helpers only assemble the corpus.
- New `VoiceValidator` enforces corpus floors (≥500 words minimum, ≥2000 recommended) and section-completeness gates (positioning, tone.register, ≥3 primary attributes, ≥2 core rules, ≥3 anti-filler patterns).
- Two consumer classes documented: visual consumers (existing PDF skills, ignore voice) and voice consumers (letter-writers, newsletters, meeting-prep — read voice block when enabled).
- 24 new tests added; all 91 tests pass; coverage-lint gate passes.
**Verification status — honest read:** Module-level functional test executed against synthetic PPTX/DOCX fixtures works end-to-end (theme extraction, voice corpus, cross-validation, merging, voice validator). The Step 1.5 voice extraction prompt has not been runtime-validated against a real partner's collateral — it requires a live session with partner material to confirm the LLM produces specific (vs generic asset-management boilerplate) `core_rules` and `anti_filler` lists. The "drafted-vs-sent feedback loop" is documented in SKILL but not implemented (deferred until at least one voice consumer skill is wired up).
**Flip conditions:** (a) more than three white-label clients onboard with significantly different brand requirements that don't fit the unified schema → split visual and voice into separate config files (visual.yaml + voice.yaml); (b) voice extraction quality on real client corpora is consistently weak → revisit either the corpus floor or the prompt structure (Lago's framework was calibrated to one founder's voice, not institutional asset-manager prose); (c) downstream voice consumers proliferate (≥4) → consider a `load_voice_pack()` helper in `_parallax/white-label/loader.py` to centralize the voice-style-block construction currently inlined in SKILL.md.
**Alternatives:** [DROP] Build a new `client-brand-ingest` skill from scratch — rejected; would duplicate ~70% of working infrastructure (audit chain, confirmation gate, validators) and require rewiring three downstream PDF skills against a new artifact location. [DROP] Build a sibling `client-voice-ingest` skill writing to the same `~/.parallax/client-branding/` directory under a separate `voice.yaml` — rejected; clean separation but creates two sources of truth that can drift (visual.yaml says client X, voice.yaml says client Y), and forces voice consumers to perform a two-file load. [DROP] Voice-only minimal scope (defer PPTX/DOCX visual extraction to later) — rejected; visual fidelity is table-stakes for branded outputs and PPTX theme XML is already the most precise extraction surface available. The chosen path adds both in one diff because they share the same OOXML theme parser.

---

## 2026-04-27: House-view ingest stays pure; data augmentation moves to consumer-skill JIT
**Why:** End-to-end validation against real CIO docs surfaced two problems with auto-gap-fill at ingest. **(1) Functional:** the fold helpers were written against assumed response shapes; the live API nests differently and returns sector positioning as narrative prose, not structured maps. Result: zero usable suggestions on live data despite passing unit tests against synthetic fixtures. **(2) Architectural:** even with helpers fixed, silently injecting current-data values into the saved view broke the trust contract — when the saved view mixes "what the bank said" with "what current data said today," compliance can no longer answer "is this our bank's view or a derived view?" cleanly. The pivot: **saved house view stays pure (bank's view + uploader confirmation only); current-data augmentation happens at consumer-skill use time, with provenance recorded per consuming artifact, never folded back into the saved view.**
**Impact:**
- Gap-fill detection + suggestion modules ship as pure functions (`gap_detect`, `gap_suggest`) with hash-chained audit + compliance-export modules. Live-API-shape regression tests pin behavior against captured response fixtures — caught the unit-tests-passed-but-production-failed gap.
- `/parallax-load-house-view` ingest no longer carries a gap-fill review step. Save flow writes `view.yaml` + `prose.md` + `provenance.yaml` + `audit.jsonl` only; current-data overlays never appear in the saved view.
- `/parallax-portfolio-builder` is the first consumer wired for JIT augmentation: opt-in `--augment-silent` flag triggers `gap_detect` + `gap_suggest` against silent thesis-relevant dimensions; output table carries a per-row Tilt Source column tagging each multiplier as bank-view-sourced, current-data-sourced (with tool + as-of date), or neutral; audit log records the flag value + augmented dimensions + skipped dimensions.
- Five other consumer skills (rebalance, thematic-screen, morning-brief, client-review, explain-portfolio) follow the same pattern in fast-follow PRs.
- Worked-example artifact (`examples/worked-example.md` + `.py`) is reproducible and demonstrates all three source-tag types. JIT operational policies (staleness, flag persistence) are documented in `examples/jit-policies.md`. Operator verification posture is in `examples/testing-posture.md`.
**Verification status — honest read:** Module tests pass. SKILL.md changes (consumer-skill instructions executed by Claude at runtime) need a fresh-session walkthrough to runtime-verify, since the runtime caches SKILL.md content at session start. The pre-go-live protocol is two fresh-session invocations: ingest a CIO doc and confirm the saved-view artifacts land correctly; then run a portfolio-builder invocation with `--augment-silent` and confirm the per-row Tilt Source column appears, the saved view stays unchanged, and the audit chain extends with the augmentation provenance.
**Flip conditions:** (a) live-session verification surfaces a SKILL.md gap → follow-up commit before external demo; (b) regulators require the augmentation provenance to live on the saved view rather than per-artifact → revisit with a sibling overlay file design; (c) the JIT flag becomes the de-facto operator default in observed usage → consider flipping the default and adding an explicit-pure flag.
**Alternatives:** [DROP] Keep gap-fill at ingest with the fold-helper fix only — rejected; the trust-contract problem persists even with functional fixes. [DROP] Move to a post-hoc augment surface as the only path — rejected; the operator instinct is to augment at the point of need, not after-the-fact against the saved view. [DROP] A view-side consistency-warning surface — largely obsoleted by the pivot since the saved view no longer mixes sources. [DROP] Wire all six consumer skills in one milestone — rejected as scope creep; the first consumer is the highest-leverage validation case.

## 2026-04-26: House-view ingest gap-fill — suggest-with-per-field-accept
**Why:** External CIO macro reports are often partial (sectors only, silent on regions; macro regime only, silent on factor tilts). The existing `schema.yaml` "omitted tilt defaults to neutral" rule conflates "uploader didn't take a view" with "uploader was deliberately neutral." Added an optional ingest step that detects silent dimensions, queries the data-driven macro view for candidate values, and presents them to the uploader for per-field accept/reject — preserving the uploader-confirms-every-value invariant rather than auto-merging.
**Impact:** New Step 2.5 in `/parallax-load-house-view` runs between extraction and the confirmation gate when the source is a document (skipped for wizard mode). New `--fill-gaps` operational mode re-runs the same flow against an already-saved view as post-hoc enrichment. Eligible dimensions are `macro_regime`, `factors`, `regions`, `sectors`; the existing Phase 0 boundary on pillars (encoding-only) and the existing CIO-judgment dimensions (themes, styles, excludes) is preserved end-to-end via an allowlist guard tested at planner level. New provenance classification `parallax_data_fill` added to `provenance.yaml` taxonomy in schema.yaml; carries source tool + call args + verbatim snippet + `data_as_of` so `--why <tilt-path>` can surface the data origin and warn that the value reflects live data captured at ingest (may drift). Three new audit actions in loader.md §6.1: `gap_fill_suggested` / `gap_fill_accepted` / `gap_fill_rejected`, each appended through the existing hash-chained `audit_chain.append_entry()`. Detection uses word-boundary regex against prose + extraction_notes (avoids false positives like "us" matching inside "cautious") and a small alias map for common surface forms. Suggestion sourcing is a pure module; the skill runtime makes the MCP calls and feeds responses back. Per-field UX is a single multi-select prompt with a batch review table (sequential prompts would be unusable at the 27-region schema scale). Security pass surfaced two file-permission gaps (audit.jsonl + provenance.yaml at-rest perms) — both fixed with explicit 0600 enforcement in `audit_chain` and the SKILL.md write step. Code-cleaner pass surfaced two dead imports — removed. **74/76 tests pass** in the house-view subtree (43 net new across unit + smoke; 2 pre-existing failures unrelated). **Flip conditions:** (a) data-driven values age stale faster than uploaders notice → ship `--refresh-data-fills` (uses `data_as_of` recorded today); (b) stronger epistemic separation between discretionary view and derived signal becomes desirable → fall back to overlay-only by promoting accepted suggestions to a sibling `data_view.yaml` instead of folding into `view.yaml` (per-field accept UX stays, only storage location changes); (c) macro coverage expands beyond today's market list → extend the region map; (d) low-confidence override threshold (0.4) needs tuning → single constant in `gap_detect.LOW_CONFIDENCE_THRESHOLD`; (e) telemetry exposes structured per-factor regime scores → swap the regime-derived factor mapper for a direct one.
**Alternatives:** [DROP] Auto-merge / harmonize into one unified view.yaml — most ergonomic but blurs CIO opinion vs derived signal and crosses the existing "encoding-only, not auto-applied" guardrail. [DROP] Overlay-only via a separate `data_view.yaml` — preserved as flip-condition (b); rejected for v1 because pushes work to every downstream consumer and the per-field provenance class already preserves separation. [DROP] Coverage report only (emit "silent on X, Y" and defer fill to a separate skill invocation + manual re-ingest) — defers the hard problem and generates no audit-trail provenance for the data-driven layer. [DROP] Sequential per-gap prompting — unusable at 27-region scale. [DROP] Treat all 0/null as gaps without prose-mention check — would flood the uploader with prompts on intentionally-neutral fields; absence ≠ gap is the load-bearing distinction. [DROP] Auto-apply when extractor confidence is very low without uploader confirmation — would break the uploader-confirms-every-value invariant. Full reasoning + per-finding security audit detail kept locally.

## 2026-04-25 (Batch 3+3.5+4 shipped): Calibration manifest client + chain emitter — implementation milestone
**Why:** Spec triad locked earlier in the day. Security-auditor + code-reviewer cleared the spec. Pivoted from the original "full client surface in one go" plan (4 modules + lock+range deps + 8 tests) to a **minimal-vertical-slice-first** approach after running `/office-hours` on the layout decision. Office-hours killed the premise that we needed the full client surface in v1 — verifier-first proves the architecture (Ed25519 + JCS round-trip, trusted_keys schema, anti-collision design) before committing to multi-file structure. The slice plan: ship the verifier alone (Batch 3), then layer cache (Batch 3.5), then layer chain emission + retention CLI (Batch 4). Each batch independently testable, each layered on the verified primitive below it. Goal: turn the locked spec triad into runnable, tested code that consumer skills can call, without waiting on the Batch 2 server endpoint (different repo, longer timeline).
**Impact:** (1) Batch 3 — `manifest_verify.py` (329 LOC): pure function, takes a manifest dict + `signing/trusted_keys.json` path, returns parsed dict + chain-anchor hash on valid Ed25519+JCS signature, raises typed errors (`KeyIdUnknown`, `SignatureInvalid`, `ManifestSignatureMissing`, etc.). Implements anti-collision guard from chain spec §3.2 (security audit A-1). 5 tests pass (round-trip, tampered values, unknown kid, empty sig, synthetic anti-collision trip). (2) Batch 3.5 — `manifest_cache.py` (472 LOC): `ManifestCache` class persists verified manifests to `~/.parallax/calibration/<org_id>_<calibration_version>.json` with atomic `0600`/`0700` writes (security audit B-1) and emits `cache_permissions_unsafe` warnings on read drift. Includes strict expiry boundary (`manifest_expires_at <= now()`), monotonicity rollback rejection, and the **A-3 dead-state guard** (fresh manifest with unknown kid + cached manifest expired or corrupt → `DeadStateNoFallback`, schema spec §6.2 step 2). `load_manifest()` orchestrator composes verify + expiry + monotonicity + cache fallback. 10 tests pass. (3) Batch 4 — `chain_emit.py` (422 LOC) + `chain_prune.py` (213 LOC): writes structured chain envelopes per chain spec §2-§4 to `~/.parallax/reasoning-chains/<YYYY-MM>/<run_id>.yaml` with atomic `0600`/`0700`. Phase-0 helper records the bundled-values transition state (chain spec §3.2). `sections_touched` auto-suppresses anchors with `active: false` (chain spec §7 PRISM-routing privacy). Refuses to overwrite existing run_id files. Defensive anti-collision check at emit time. `parallax chain prune --before <date>` retention CLI per security audit B-2: dry-run default, `--confirm` to delete, refuses symlinked chain_dir, declines files modified after process start. 11 tests pass. (4) Code-reviewer pass surfaced 2 findings (1 High: `CacheCorrupt` from cache re-verification was leaking instead of translating to `DeadStateNoFallback` in the A-3 path — runbooks branch on `key_id_unknown_no_fallback`, not `cache_corrupt`, so a key-rotation cold start would route to the wrong alert; 1 Medium: `chain_prune._parse_before` docstring claimed "END of day" but code returns midnight UTC start-of-day — code correct, doc misleading for companion-tool authors). Both fixed; 1 regression test added. (5) **27/27 tests pass** across the entire house-view subtree (1 view_hash + 5 verify + 10 cache + 11 chain). (6) All 5 security-audit findings (A-1, A-3, A-7, B-1, B-2) plus NEW-4 strict expiry are now **enforced in tested runnable code**, not just in spec. (7) Resolved deps committed as exact pins per A-7 lockfile rule: `pynacl==1.6.2`, `rfc8785==0.1.4`, `pyyaml==6.0.3`. (8) Test signing key + signed test fixture + fixture generator are **deliberately public** (test-only kid labeled in `trusted_keys.json`, kid-bound trust binding means production deployments without this kid are unaffected) — matches existing `test_view_hash.py` precedent and lets auditors run round-trip on a fresh clone. **Flip conditions:** (a) Batch 2 server endpoint generates real production keys → add to `signing/trusted_keys.json`; test-only kid stays for self-tests but production manifests don't carry it; (b) `rfc8785` library is unmaintained or has a CVE → exact-pin lockfile rule needs a pin-bump procedure (regenerate fixtures + re-run tests on the new version + document cutover in skill_version changelog); (c) cache concurrent-write race ever bites in practice (currently rename-atomic but not lock-protected) → add `fcntl.flock` for inter-process serialization; (d) replay tooling needs to verify a chain from a `skill_version` we no longer ship → versioned `trusted_keys.json` per chain spec §8.2 long-horizon replay anchor must already cover this, but verify before any 2034-replay drill; (e) `parallax chain prune` CLI accidentally deletes archived chains → add a third confirmation step (compliance-store-acknowledgment).
**Alternatives:** [DROP] Original full-client-surface layout (`client/{verify,cache,errors,manifest_client}.py` + lock+range deps + 8+ tests as one batch) — rejected after office-hours; commits to multi-file structure before knowing if pynacl/rfc8785 actually round-trip in our env, 4× the surface area to debug, lock+range split was scope creep. [DROP] Server-first (Batch 2 before Batch 3) — rejected because Batch 3 doesn't need a live endpoint to test (file-based fixture is sufficient and matches what auditor-replay tooling will do anyway); chose to parallelize. [DROP] Defer cache + chain to "Batch 3 includes everything" — rejected because monolithic batches make code-review more expensive and bisection harder when bugs surface; small layered batches each with its own commit + tests is the cheaper unit. [DROP] Make the test signing key private (gitignored) — rejected because then tests don't run on a fresh clone, and the key is labeled test-only in trusted_keys.json so production deployments without that kid are unaffected. [DROP] Make the test fixture private — same reason; deliberately public so auditors can verify their environment against a known-good round-trip. [DROP] Skip the `parallax chain prune` CLI in v1 — rejected; security audit B-2 names it as a required artifact for chain retention, and operators on shared workstations need a non-destructive (dry-run-default, `--confirm`-required) cleanup path or chains accumulate to ~12GB over 7 years. [DROP] Wire load-house-view skill to call the new modules in this same milestone — deferred to Batch 5 because skill markdown integration is structurally different work (instructions for the model, not Python imports) and bundling it would risk the implementation milestone's quality. **Test infrastructure note:** tests run via a temp venv at `/tmp/parallax-batch3-venv/` because PEP 668 blocks Homebrew Python's pip. Future contributors should set up their own venv from `requirements.txt`; the README documents this.

## 2026-04-25 (security-auditor pass): Pre-Batch-2 hardening of calibration manifest spec triad
**Why:** Before starting Batch 2 (server-side endpoint in the Parallax MCP server repository), ran `security-auditor` (Claude-only, read-only) over the locked spec triad to verify the signing/replay design is wired correctly and to surface any storage/data-retention concerns the user (Ben) explicitly asked about ("are we storing any information?"). Auditor produced 9 findings (4 Medium: A-3, B-1, A-1, B-2; 3 Low: A-7, B-3, B-4; 2 informational/verified-closed: A-5 90-day rotation worst-case bound + C-2 cache-substitution attack closed by existing controls) and a verdict that the design is "architecturally correct and ready for Batch 2 implementation with three spec-level patches." Five patches applied to spec triad before any server code is written (4 Medium + A-7 Low); closing them now is cheap, closing them later requires re-implementing client logic against shifted assumptions. Sixth and seventh findings (B-3 audit-log access control, B-4 dev fixture cloud-sync warning) are operator-discipline concerns not blocking Batch 2; deferred to ops runbook. Code-reviewer pass on the 5 patches surfaced two follow-on edits (schema §7.2 write-time permission instruction, this entry's severity count, endpoint-spec changelog symmetry) — all applied before commit.
**Impact:** (1) Schema spec §6.2 step 2 (A-3 Med) — silent dead-state closed: when cached manifest is expired AND `key_id_unknown` triggers on the fresh manifest (e.g., outdated skill outlives both cache and trusted-key bundle), client now hard-errors `key_id_unknown_no_fallback` and refuses to run; eliminates the case where an outdated skill silently used an expired cache or accepted an unverified fresh manifest. (2) Schema spec §7.1 (B-1 Med) — cache file/dir permissions now mandatory: `0600` on cache files, `0700` on `~/.parallax/calibration/` directory on Unix-like systems, equivalent ACL on Windows; loader verifies on every read with `cache_permissions_unsafe` warning; rationale is shared workstations (regulated entity desktops) make `$HOME` paths broadly readable by default. (3) Chain spec §3.2 (A-1 Med) — normative anti-collision guard: emitter MUST compute both `manifest_ref.hash` and signing-payload hash and assert they differ before emission; `manifest_signature_missing` hard-error if they collide; catches buggy signing libraries that reuse one hash for both, which would silently break the dual-hash trust-anchor design. (4) Chain spec §5 (A-7 Low) — JCS pinning tightened from range (`rfc8785>=0.1.0,<0.2.0`) to exact lockfile pin (`rfc8785==<x.y.z>`); replay tools refuse if resolved version differs from pin at the chain's `skill_version` tag; rationale is 7yr replay horizon needs determinism, and a single-byte canonicalization change between patch versions breaks chain hash byte-identity. (5) Chain spec §4.3 (B-2 Med) — added required `parallax chain prune --before <YYYY-MM-DD>` CLI with `--confirm` gate, dry-run default, summary line, symlink-following + concurrent-write guards; documented `0700` directory permission rule for `~/.parallax/reasoning-chains/`, encrypted-volume requirement, cloud-sync exclusion guidance for alpha-sensitive content. (6) C-2 (rollback-in-different-clothing attack via cache substitution) verified closed by existing combined controls (signature + monotonicity + org_id binding); no spec change needed. (7) A-5 (90-day worst-case for emergency-rotation propagation to offline clients) confirmed bounded; v2 detection mechanism remains a candidate. (8) Confidence after audit: Batch 2 server implementation is unblocked. **Flip conditions:** (a) any first prospect requires multi-user-shared workstation deployment without per-user `$HOME` isolation → A-3 dead-state error message needs operator-facing remediation playbook before deploy; (b) `rfc8785` library is unmaintained or has a CVE → exact-pin lockfile rule needs a pin-bump procedure, not just the rule; (c) `parallax chain prune` CLI accidentally deletes archived chains → CLI requires a third confirmation step (compliance-store-acknowledgment). **Two findings deferred:** B-3 Low (server-side audit log sensitivity classification) → ops runbook, not spec; B-4 Low (dev test-fixture cloud-sync warning) → developer onboarding doc, not spec.
**Alternatives:** [DROP] Defer all 5 patches until Batch 2 implementation surfaces the gaps — rejected; spec edits are minutes, post-code refactors are hours, and the dead-state in particular (A-3) is a class of bug the user can ship without ever noticing if the rotation window doesn't coincide with a cache expiry. [DROP] Apply only the Critical findings (none in this audit) and defer Mediums — rejected; 3 Mediums (A-3, B-1, B-2) all close concrete attack surfaces; A-1 normative assertion is a 6-line spec edit that prevents an entire class of silent emitter bugs. [DROP] Tighten JCS pin in `pyproject.toml` install-range — rejected; install-range stays a range for normal pip resolution, the lockfile pin is what governs determinism. [DROP] Make `parallax chain prune` deletion the default mode (no `--confirm`) — rejected; alpha-sensitive content + financial-records retention demand explicit operator action. [DROP] Skip the directory permission rule for chains since the calibration cache rule covers most paths — rejected; chains contain CIO views + portfolio weights, a different sensitivity class from calibration values; both need explicit hardening. [DROP] Address B-3 (audit log sensitivity) in spec — preserved as deferred-to-ops-runbook; spec scope is the wire/file artifacts, not server-side log infrastructure. **Audit report:** captured in agent output; not persisted to notes/ — the substantive content is in the spec changelog entries and this DECISIONS.md item.

## 2026-04-25 (later): Calibration manifest spec triad v1 LOCKED — endpoint + schema + reasoning chain
**Why:** Phase-1 PRISM-additions stash (`stash@{0}`) is held server-side-pending; 12 hunks of framework evolution (canonical 5-factor set, 27-country regions, Ω/Φ/Ξ/Ψ pillar vocabulary (renamed to econometrics_phase/valuation_state/market_entropy/psychological_wavelength in fix/house-view-glyph-scrub), conviction tracking) blocked on calibration manifest endpoint landing per DECISIONS.md 2026-04-24 PM Option C. Three v1 DRAFTs ([internal design sessions], [internal design sessions], [internal design sessions]) were each self-scored 9/10 individually but had cross-spec inconsistencies (chain WARN multiplier with no manifest source; chain `signature` envelope vs §3.6 optionality conflict; manifest §5 sub-numbering 5.1 → 5.3 → 5.2; §11 decision log placed after §13) plus three structurally-decidable open questions (URL path; regime vocabulary location; pillar-routing dormancy mechanism) and one new spec-decision surfaced by stash audit (where does the stash's `growth × inflation → econometrics_phase` derivation table live?). Locking the triad before any code lands prevents downstream Batch 2 (server) and Batch 3 (client) from being written against a moving target.
**Impact:** (1) Manifest schema v1: regime keys (both growth-axis outer keys AND inflation-axis `pillar_defaults` sub-keys) MUST be a subset of the skill-shipped vocabulary in `schema.yaml`; client validates on load with hard-error message naming the offending key; server publishes only against current skill vocabulary (forces coordinated skill+manifest releases). (2) `sec-pillar-to-factor-routing.active: false` v1 default; clients suppress this anchor in chain `manifest_ref.sections_touched` while dormant, reserved for a future activation milestone. (3) Stash@{0}'s `growth × inflation → econometrics_phase` derivation table folded into existing `sec-macro-regime-deltas.pillar_defaults` — keeps manifest section count low; values are H2a-recalibratable (correct location, since cell values move with calibration). (4) `sec-sensitivity-bands.warn_multiplier` global field added — closes the chain spec §3.5 hanging reference (WARN behavior was a magic constant with no manifest source); default value defined in the manifest spec; per-family override deferred to v2 if any family ever needs different multiplier. (5) Manifest examples updated to canonical factor names + `trading_signals` (matches stash@{0} canonical 5-factor set; `quality`/`defensive` are skill-side aliases only, never appear in manifest). (6) Endpoint URL locked at `/v1/calibration/manifest` (was Q10.1 open). (7) Reasoning chain `signature` field clarified as **always present** with `alg` carrying the signed-or-not state (`alg: "none"` → `value: null` v1 default); resolves envelope-vs-§3.6 inconsistency without breaking forward-compat for `alg: "ed25519"` v2. (8) WARN/KILL/PASS thresholds now fully recoverable from `manifest_ref.hash` during chain replay — no hardcoded skill-side magic numbers left in the band-check path. (9) Stash@{0} can apply cleanly post-Batch-3 (client-side fetch + dormant routing baked in); not before — applying earlier ships pillar vocabulary publicly without the "values live in manifest" story shipped, which is messy though not strictly leaky given memory note that pillar vocabulary already leaked. (10) Three batches now unblocked for sequential execution: **Batch 2** server-side endpoint in the Parallax MCP server repository, **Batch 3** client-side fetch/cache/verify in this repo, **Batch 4** reasoning-chain emitter wired into consuming skills. **Flip conditions:** (a) any first prospect requires hash-pinned bundle audit reproducibility → flips C→D per Option C entry, manifest schema needs `bundle_hash_manifest` cross-section field; (b) per-org calibration values land before v2 → schema breaks (current `org_id: "parallax-default"` invariant relaxes); revisit `pillar_defaults` per-org; (c) regime vocabulary needs to evolve on a faster cadence than skill releases → schema enforcement softens to "subset OR alias" with deprecation aliases like `deprecated_aliases` in §5.2; (d) WARN multiplier needs per-family override before v2 → `sec-sensitivity-bands.<family>.warn_multiplier` overrides global; (e) `trusted_keys.json` rotation cadence becomes faster than skill release cadence → key-fetch endpoint added (currently bootstrap-via-skill-pin per endpoint §6.4).
**Alternatives:** [DROP] Keep all three specs as DRAFT and start Batch 2 server work immediately — rejected; cross-spec inconsistencies (WARN multiplier, signature optionality, sub-numbering) would propagate into code; cheap to fix now, expensive once code is referencing the spec. [DROP] Add a separate manifest section `sec-pillar-derivation-defaults` for the stash derivation table — rejected; section count discipline favors fold-in to existing `sec-macro-regime-deltas`; the table IS a regime-conditional default, semantically the same family. [DROP] Hardcode the WARN multiplier value in skill code rather than manifest — rejected; this is a calibration parameter (recovery can shift it as part of H2a empirical recalibration) and replay needs determinism from `manifest_ref.hash` alone, not from skill version + magic constants. [DROP] Make `signature` field genuinely optional (envelope MAY omit) — rejected; chain tooling never having to handle missing fields is cheaper than the "saves 1 line of YAML when unsigned" benefit; `alg: "none"` is the right v1 unsigned encoding. [DROP] Lock URL path differently (`/v1/house-view/calibration` etc) — rejected; `/v1/calibration/manifest` is more general (calibration may serve future non-house-view artifacts under same path). [DROP] Apply stash@{0} pre-Batch-3 to unblock those 12 hunks immediately — rejected; ships pillar vocabulary into public repo without the manifest-resident-values context, creating a "where do the values come from?" gap visible to anyone reading the public schema; cleaner to apply post-Batch-3 when the gap is structurally answered. [DROP] Run `/council` or `plan-swarm` on the locked specs before committing — rejected; data-perimeter constraint blocks dispatching CG/PRISM-internal payloads to external models; depth review via `adversarial-reviewer` (Claude-only) is the right tool if a review-pass is wanted before code lands. **Spec triad lives in `notes/` (gitignored) so the lock itself isn't a committable artifact in this repo; this DECISIONS.md entry is the public-facing record of the lock event.**

**Adversarial-reviewer second pass (2026-04-25 post-lock):** ran `adversarial-reviewer` (Claude-only, 3-phase Hunter/Skeptic/Referee, per data-perimeter) over the locked triad before commit. Reviewer surfaced 27 Hunter findings → 12 disproved by Skeptic (false-positive rate 44%, all Skeptic disproves confirmed by Referee) → **15 verified bugs accepted: 1 Critical, 2 High, 5 Medium, 7 Low**. All 15 were spec defects (3 self-introduced during lock pass, 12 pre-existing in the v1 DRAFTs). Critical: BUG-003 — `warn_multiplier` comment had inverted WARN band semantics (said "exceeds upper-bound × multiplier" when correct is "between upper-bound and upper-bound × multiplier"); a Batch 3 implementer reading only the schema comment would have implemented WARN incorrectly with silent miscategorization. Highs: BUG-010 emergency key rotation described as "server-side state change" on a signed field (cryptographically impossible — rewrote as re-sign-under-new-key with explicit upgraded-vs-not client behavior); BUG-025 long-horizon replay trust anchor (manifest retention 7yr but key retention ~1yr → auditor in 2034 replaying 2027 chain finds key absent from current `trusted_keys.json`; spec now requires versioned `trusted_keys.json` from the pinned `skill_version`). All 15 fixes applied to the spec triad on disk before this commit; per-bug fix details captured in each spec's changelog under "post-review pass" entry.

**E2E quality test pass (2026-04-25 post-fix-pass):** ran a second `adversarial-reviewer` invocation as an end-to-end quality test, with three independent tests: (1) cross-reference resolution audit across all three specs (18 cross-refs found, 15 resolved correctly, 2 mismatched, 1 unverifiable); (2) worked-example construction (concrete manifest JSON + chain YAML + per-ticker trace through determinism contract); (3) scenario regression on 5 realistic scenarios (normal session, calibration revocation, schema migration, 7-year audit replay, emergency key rotation). Verdict: **FIXES-NEEDED** with 6 new issues (2 Medium, 4 Low) plus 2 of the original 15 fixes confirmed only partially-effective. Scenarios A pass; B/C/D/E SOFT pre-fix. **All 6 NEW issues fixed and 2 partial-effective bugs cleaned up:** NEW-1 Med (chain §8.2 dangling cross-ref to non-existent "schema spec §9 dependency 2" — corrected to endpoint spec §9 item 2 only); NEW-2 Med (endpoint §6.2 wrong target — chain §3.2 → chain §8.2 for `trusted_keys.json` reference); NEW-3 Med (revocation propagation gap — pull-only architecture means 410 cannot reach unexpired-cache clients without live request; v1 accepts the bound, documents in new endpoint §6.5 + §4 410 row update, defers proactive detection to v2 with three candidate mechanisms listed); NEW-4 Low (expiry boundary explicit `manifest_expires_at <= now()` — was ambiguous, now boundary-inclusive so emergency-rotation re-signs with `manifest_expires_at = now()` force immediate re-fetch); NEW-5 Low (chain §5 determinism contract names `loader.md` at pinned `skill_version` as normative dependency — earlier text claimed byte-identity replay without naming the application-logic source); NEW-6 Low (endpoint §6.3 corrected the "already-expired cached copies" claim — only true for clients who fetched POST-rotation; PRE-rotation cached clients carry original `manifest_expires_at` (~90 days) and constitute the dangerous case requiring out-of-band operator outreach in v1). Confidence in spec stability after both review rounds: **9.7/10** (the 0.3 deficit: composite formula and per-factor weight derivation live in `loader.md` outside the triad — referenced normatively now but not inlined; an auditor still needs `loader.md` to fully replay; acceptable since `loader.md` ships in the public skill repo). Total: 21 spec issues (15 + 6) addressed before code lands; this DECISIONS.md entry is the public-facing record.

## 2026-04-25: Tier 2 reframe — compliance-first sequence (one-pager → hash-chain), original Tier 2.A+2.B not the right next batch
**Why:** `/office-hours` demand-test on the queued Tier 2.A+2.B bundle (persisted `provenance.yaml` at ingest + `source_type` suffix in `render_view_conflict()`, ~1.5–2 hours of spec work) exposed two compounding errors. (1) Original "internal tool for self" framing was wrong — when forced through Q1/Q2, the asserted ~50 `--why` invocations turned out to be **projected, not actual**, and the customer is **institutional prospects in vendor-risk-approval workflows**, not the developer. (2) Once the customer was correctly named, Q3 (B2B variant) collapsed onto a single gating event: **an institutional vendor-risk approval workflow**. Tier 2.A as scoped (plaintext flat YAML alongside view.yaml) is **user-grade scaffolding being asked to do blocker-grade work** — neither user-grade enough to be loved nor blocker-grade enough to clear vendor risk. Q4 (platform variant) surfaced the actual artifact stack a COO sign-off requires: vendor-risk one-pager (what a COO reads), hash-chained `audit.jsonl` (tamper-evidence), `provenance.yaml` co-saved + hashed into chain (Tier 2.A re-targeted), `audit-export <view_id>` CLI (regulator-grade bundle). User confirmed "we have time" → verdict **reframe**, not build-as-scoped.
**Impact:** (1) HANDOFF.md Tier 2 section restructured: **Approach A — Vendor-Risk One-Pager** (S, 4-6 hours, this week, [internal design sessions]) becomes new top priority; **Approach B — Hash-Chained Audit + audit-export CLI** (M, 1-2 weeks of spec work) becomes #2; original Tier 2.A's `provenance.yaml` work survives as **data layer of Approach B**, not as standalone goal. (2) Tier 2.B (`source_type` suffix in render_view_conflict) and Tier 2.D (tilt-attribution audit fields) **deprioritized** to Phase 1 user-grade polish backlog — both are user/architectural niceties that don't move COO sign-off; revisit when there are real users (not projected). (3) Tier 2.C (structured `## Drivers` block) kept but lowered to between-batches polish task; doesn't depend on A or B. (4) Calibration disclosure text confirmed staying **strong** (`"do not use for regulatory capital, fiduciary-grade portfolio construction, or client-facing recommendations"`) — office-hours reframe makes the case that strict self-limits *help* the COO sign-off case rather than hurting it; softening would weaken compliance posture. (5) Original 5 open decisions in HANDOFF.md §3 reduced to 3 (resolved: Tier 2.D fan-out moot under deprioritization; Drivers block placement moot under deprioritization; calibration disclosure strength resolved as keep-strong). New open decision: whether to `--re-pair` the active sample view to v1 before Approach A so the one-pager can use it as a clean worked example (likely yes, 15 min). (6) Approach B's signing-key story deferred — v1 ships with hash-chain integrity only; cryptographic signing waits for first prospect that explicitly requires it. (7) Active sample view's `audit.jsonl` becomes pre-chain content; chain begins at next save (or `--re-pair`); no retroactive backfill. **Flip conditions:** (a) the active pursuit accelerates faster than expected → Approach A's timeline compresses (could ship in 1 day if pursuit-blocking); (b) the active RFP closes without Approach A in the response → reduces urgency on A, B becomes equal priority; (c) compliance review explicitly demands cryptographic signing → Approach B's signing-key story moves up from "v2 if needed" to "v1 blocker"; (d) a third prospect surfaces with a different regulatory regime than the current targets (e.g. EU AI Act high-risk classification) → Approach A revised to add jurisdiction-specific section; (e) any move to Phase 1 multi-tenant Supabase deployment → Approach B's hash-chain becomes a Supabase trigger schema, not a JSONL field.
**Alternatives:** [DROP] Build Tier 2.A+2.B as originally scoped — rejected; user-grade scaffolding for a blocker-grade requirement; ships work that doesn't move COO sign-off. [DROP] Tier 2.C alone (the original "alternative if short on time" in HANDOFF.md) — rejected as next batch; pure UX polish on a surface that's already good enough; doesn't move the active pursuit. [DROP] Approach C (re-scope all of Tier 2 as "Compliance MVP" in one bundle) — preserved as the *retrospective* shape of A + B together, not as a separate execution path; A's decoupling is the value. [DROP] Skip Approach A and build Approach B directly — rejected; the one-pager is what an institutional COO actually *reads*; technical machinery without an explainer document is engineering for engineers. [DROP] Soften the heuristic-stage disclosure to "use with caution" — rejected; office-hours reframe established that strict limits help the COO sign-off case. [DROP] Add cryptographic signing to Approach B v1 — deferred; hash-chain integrity sufficient for v1, signing waits for explicit prospect demand. **Flip conditions** captured in Impact item (7).

## 2026-04-24 (evening): XAI compliance posture — deep research validates Phase 0.5 architecture, surfaces 1 new gap
**Why:** Internal self-assessment against XAI frameworks (EU AI Act, MAS FEAT, NIST AI RMF, SR 11-7, FCA, IOSCO, OECD, HKMA/SFC) produced a B+ composite for State A (local-only) and C+ for State C (client-facing), with four known gaps. Deep research dispatched to external tool validated the four gaps but graded them stricter in two ways and surfaced a fifth: (a) SR 11-7 "conceptual soundness" graded FAIL on both internal + client use because the "GUESS/HEURISTIC" label on multiplier values is a documented admission of unsoundness — a documented intent to validate later provides zero regulatory shielding; (b) HKMA/SFC Nov 2024 GenAI Circular mandate of "human-in-the-loop review before AI outputs are deemed reliable and actioned upon" applies at every downstream propagation step, not just at the ingest confirmation gate — our six portfolio-layer skills auto-propagate tilts without secondary gates; (c) **Gap 5 (new):** single-stock §7.1/§7.2/§7.3 flags themselves require per-flag contextual AI disclosure tooltips (not just the bottom-of-output AI-interaction banner) under OECD Principle 1.3 + HKMA/SFC. Industry floor observed via FactSet/Bloomberg/BlackRock Aladdin is higher than our self-assessment estimated — FactSet does sentence-level auditable links; Aladdin's AI-Auto-Commentary is explicitly restricted to synthesizing existing rigorously-calculated data, never generating new advice.
**Impact:** (1) Phase 0.5a/0.5b architecture unchallenged — nothing shipped is invalidated; the research validates direction and sharpens the deferred list. (2) Priority reshuffled per research: calibration (Gap 3) now **#1** (not attribution) — SHAP/LIME on arbitrary multipliers is precise measurement of arbitrary numbers; calibration is the foundation. (3) Attribution (Gap 1) now **#2** — research recommends SHAP for the *composite re-rank step only*, not for the LLM extraction step (LLMs aren't differentiable in the sense SHAP requires — research conflated). (4) **Gap 5 added to backlog** — per-flag hover-state AI disclosure tooltips; requires a UI layer we don't have yet (terminal-only currently); revisit when Phase 1 client surface ships. (5) Jurisdictional exposure confirmed: **HK SFC is the binding constraint** the moment we touch SFC-licensed distribution; EU AI Act Article 13 is binding for any EU-deployed client surface; SR 11-7 is technically scoped to FR/OCC-supervised banks but sets the model-risk benchmark institutional prospects will apply. (6) Research's recommendation to migrate audit.jsonl → Neo4j/graph-DB rejected for Phase 0.5 — overkill for State A; the `--why` on-demand helper handles stacked provenance at query time adequately; reconsider at Phase 1 Supabase cutover. (7) Deferred list before research: {Layer 4 applied-tilts, Phase 1 Layer 1 per-tilt provenance, compliance memo, fresh E2E test}. After research: {Gap 3 calibration (**was already planned as H2a**), Gap 1 SHAP attribution (Layer 4 upgrade), Gap 2 structured drivers (new roadmap item), Gap 4 graph-provenance (Phase 1 only), Gap 5 per-flag tooltips (UI-layer work)}.
**Alternatives:** [DROP] Block Phase 0.5a/0.5b on compliance memo (per earlier adversarial review's modify on Council verdict — still holds; research did not reverse that conclusion). [DROP] Graph-DB migration in Phase 0.5b (research recommendation) — overkill for State A; `--why` suffices. [DROP] Run SHAP on the LLM extraction step (research recommendation, but conflates differentiable composite re-rank with non-differentiable LLM extraction). Kept in reserve: steelman case that current governance is over-engineered for State A — credible, but research confirms the argument collapses at the State A→B boundary and every roadmap item assumes State B/C will arrive. **Flip conditions:** (a) move toward Phase 1 multi-tenant or any SFC-licensed distribution → Gap 5 (UI tooltips) + Gap 4 (graph provenance) become blocking; (b) any US bank-affiliate pilot → SR 11-7 conceptual soundness becomes binding, Gap 3 calibration must be empirical before deployment; (c) any EU B2B pilot → EU AI Act Article 13 "transparency by design" forces Gap 2 (structured drivers) before ship.

## 2026-04-24 (late morning): House-view single-stock §7 split + `render_view_conflict()` seam
**Why:** E2E test on `/parallax-should-i-buy NVDA.O` against the active sample view produced a data body byte-identical to the raw (view-less) pull — Parallax's peer-suggest returned MU.O (higher momentum) while the view was UW momentum, with zero conflict flag. Council (MCP domain, Standard scope, 5 advisors + 3 peer reviewers + chairman) + adversarial-reviewer confirmed: §7's "no rescoring" ceiling is correct, but its single closing-note floor was too weak — silent pass-through of a conflicting peer-suggest is a real gap at the decision surface.
**Impact:** (1) Loader.md §7 split into §7.1 (blanket House View Note, **relocated to after The Scores / Factor Profile**, not end-of-report), §7.2 (inline peer-suggest conflict token when peer's sector is view-UW≤-1 or on excludes — **flag, do not filter**), §7.3 (inline score-vs-view tension banner gated on Parallax total ≥7 AND stock sector tilt ≤-1, to prevent alert fatigue on moderate-score names). (2) New `skills/_parallax/house-view/render_helpers.md` defines canonical `render_view_conflict(kind, fields) -> str` with three kinds (blanket / peer_suggest / score_tension) — single seam so the Phase 1 provenance suffix is a one-file change; consumers MUST route through it. (3) `should-i-buy` + `deep-dive` SKILL.md updated: gotchas cite new sub-sections, Output Format reorders with House View Note right after scores and inline tokens at their surfaces. (4) Loader.md §2 step 6 gains a null-arithmetic guard for the both-null (`effective_date` + `valid_through`) case — fails closed with explicit banner. §7 "no rescoring, no rerank, no peer filtering" invariant preserved throughout. Ships as `d2ff193` on `phase-1-prep`.
**Alternatives:** [DROP] Blanket end-note only (status quo) — rejected; peer-suggest silent disagreement is the specific gap. [DROP] Rescore or re-rank single-stock output (Advisor 3 steelman, Platform Thinker's annotation primitive) — rejected as §7 violation; Layer 3 "read-only, no rescoring" is the contract that makes the Parallax score meaningful. Platform Thinker's structured `view_annotations` primitive deferred to a later phase — current single-seam renderer covers the lift when it lands. [DROP] Filter the peer-suggest out of the table — rejected; filter pressure collapses into de-facto rescoring (Ship-It Engineer's flagged risk). Flag-not-filter preserves both surfaces. Adversarial review overturned two Council framings: deep-dive was NOT a correctness bug (it already injects the view into `get_assessment`'s prose), and footer repositioning was NOT compliance-gated (relocating already-shipped content, not adding new content).

## 2026-04-24 (midday): House-view extraction auditing — defer per-tilt provenance, ship `--why` on-demand
**Why:** Proposed 5-layer auditing design centered on Layer 1 (per-tilt `extraction.provenance.{group}.{field} = {confidence, source_span, source_page, rationale}`). Adversarial review found two critical unresolved issues: (a) stacked provenance is live in the current data — the sample view's `profitability: 2` is prose-derived (+1 from "defensive characteristics" / "quality bonds") STACKED with rule-derived (+1 from macro_regime `slowing + sticky` per loader.md §3) — and the proposed scalar-per-field schema cannot express this; (b) provenance sits outside `view_hash` scope, so a span correction wouldn't bump `version_id` (forensic gap). Reviewer also noted no concrete State A failure scenario justifies the schema weight when `extraction_notes` already covers the live view's ambiguities.
**Impact:** (1) Layer 1 (per-tilt provenance) **deferred to Phase 1** — where re-extraction runs anyway, stacked-provenance can be designed against real query patterns, and the hash-scope question can be resolved against multi-tenant audit needs. (2) Shipped what stayed ROI-positive: Layer 2 (pre-edit snapshot at confirmation gate → `.archive/<version>/pre_edit.yaml` with optional `edit_notes.md`), Layer 3 (`version_diff` block on save when `parent_version_id` non-null, flat diff of `tilts`+`excludes` subtrees, cap 40 entries), Layer 5 **folded** into `audit.jsonl` as new `action="extraction_attempt"` enum value (not a separate log file — single source of truth preserved for Phase 1 Supabase migration). (3) New `--why <tilt-path>` operational mode — zero-schema-cost alternative to Layer 1: reads `view.yaml` + `prose.md` + `extraction_notes` + loader.md §3 at query time, returns a one-paragraph answer with quoted supporting spans, notes when the tilt is rule-derived vs prose-derived. Handles the stacked-provenance case structurally (prose scan + rule lookup are naturally additive). Also added `--version-history` to render the save chain + diffs from audit.jsonl. (4) Current sample view **NOT re-extracted** — `extraction_notes` is sufficient provenance record for State A local-only; Phase 1 re-extract runs against a new model anyway, so backfilling now is throwaway work. Loader.md §6.1 action enum + §6.2 conditional fields (`parent_version_id`, `version_diff`, `disposition`, `draft_yaml_hash`, `extraction_duration_ms`, `hint`) + §6.4 examples all updated. Ships as `a89acc0` on `phase-1-prep`.
**Alternatives:** [DROP] Ship Layer 1 per-tilt provenance now (original flagship) — rejected until stacked-provenance schema shape + hash-scope question resolved. [DROP] Layer 5 as separate `extraction_log.jsonl` — rejected; fragments audit trail for Phase 1. [DROP] Re-extract current sample view under new schema — rejected; re-extraction would rely on a different model's span anchoring and could silently shift tilts without a clean uploader re-confirmation path. [DROP] Wrap `render_view_conflict()` in a structured `view_annotations` primitive now (Platform Thinker at the rendering decision, applicable here too) — not dropped, just deferred: the single-seam string renderer absorbs the future swap as a one-file change when Layer 1 lands.

## 2026-04-24 (midday): §7 compliance memo — parallel track, not blocking
**Why:** Council verdict put compliance-memo-on-§7-pattern as a **blocking gate** on Phase 0.5a rendering changes, driven by peer reviewers' concern that inline `⚠` flags on single-name reports might constitute solicited advice under private-bank suitability regimes. Adversarial review (Q7) found the sequencing inverted the actual risk: §7's main-stock closing flag (`"Active view is UW [sector]; evaluate against your active view before acting"`) already ships in production-equivalent form without counsel clearance; §7.2 peer-suggest token is structurally identical — same flag pattern, same advisory posture, not a new legal surface. Meanwhile the pre-0.5a state (silent pass-through of a view-conflicting peer-suggest) was arguably the higher liability today.
**Impact:** (1) Phase 0.5a ships immediately without counsel pre-clearance (committed as `d2ff193`). (2) Compliance memo scoped to the **§7 pattern as a whole** (existing main-stock flag + new peer-suggest + tension banner) before any **Phase 1** external-facing deployment — not before Phase 0 / 0.5 engineering. If counsel returns an objection, the response is a rollback of the token rendering (single-seam `render_view_conflict()` returns empty string), not a re-architect. (3) Deployment surface confirmed as State A local-only — MIT-licensed skills, operator reads output in their own Claude Code session, no client-facing egress in the current codebase. State determined by checking repo structure (no email/PDF/web export in any skill), not by asking counsel. (4) This decision applies only while State A holds; any move to State B (partner installs generating client-facing output) or State C (Phase 1 multi-tenant Supabase deployment) re-triggers the memo as blocking.
**Alternatives:** [DROP] Block Phase 0.5a on compliance memo (Council verdict as originally framed) — rejected; inverts the risk direction and leaves the higher-liability silent-disagreement state live during the memo turnaround. [DROP] Assume compliance will approve and ship without ever writing the memo — rejected; §7 pattern becomes real-stakes at any Phase 1 cutover or B2B institutional deployment, and we want the memo in hand before that, not after. [DROP] Scope the memo narrowly to §7.2/§7.3 additions only — rejected; the adversarial review's observation was that §7.1/§7.2/§7.3 are structurally the same pattern. Memo covers the pattern, not the increment. **Flip conditions:** (a) any move toward Phase 1 Supabase multi-tenant; (b) partner firm installs skills and uses output in a client-facing workflow; (c) an advisory surface is added (email export, PDF, web render) — each re-triggers the memo as blocking for its specific deployment.

## 2026-04-24 (PM): Calibration architecture — Option C (hybrid signed manifest), D-compatible
**Why:** Decision session walked §7 open questions from [internal design sessions] sequentially (compliance → org-scoping → distribution → cost/timing). Q1 killed B through chain-leak: if server returns applied-multipliers so clients can render a compliant chain of reasoning, every org's run-log recovers the multiplier grid faster than probing would — B's "zero values exposed" property dies through normal operations. Q2 ruled per-org values premature in v1 while base calibration is still GUESS/HEURISTIC. Q3 collapsed to public skill repo + authenticated manifest fetch given existing public OSS distribution. Q4 showed infra cost (1-2 weeks) is not close to IP cost of a second public leak of recalibrated multipliers — public regression corpus already partially constrains calibration; further public exposure would erode that buffer.
**Impact:** (1) Calibration values live in signed JSON manifest cached locally at `~/.parallax/calibration/<version>.json`; fetched via new authenticated endpoint `GET /calibration/manifest?version=<v>` at Parallax MCP. (2) Skill code stays OSS MIT, public GitHub distribution unchanged. (3) Manifest schema includes `org_id` from v1 but values are uniform across orgs in v1; per-org values unlock in v2 without schema break. (4) Every skill run emits `reasoning_chain.yaml` (5-item: manifest_ref with section anchors, view verbatim, base_scores pinned via response_hash + response_blob for MCP-non-determinism replay, final_portfolio, signature) — no parameter duplication into the chain (chain references manifest, doesn't inline values). (5) Chain retention local-only v1; server-side hash-receipt ledger deferred to v2 with no schema change. (6) C infrastructure built in parallel with H2a recalibration; **gate: no Phase-1 recalibrated values ship until endpoint live and signing verified end-to-end.** (7) Sensitivity bands + kill criterion + pillar-to-factor routing all live in manifest's private sections. (8) Unblocks H2a and the 12 parked stash hunks in `phase-1-prep PRISM additions` stash. **Flip conditions:** (a) jurisdiction compliance lookup returns all-permissive AND infra cost escalates — revisits B on cost grounds (still probably C via chain-leak problem); (b) any target jurisdiction requires hash-pinned bundle audit reproducibility — flips C→D; (c) authenticated-adversary leak becomes an active problem (customer leaking manifest to a competitor) — adds per-org noise injection or accelerates to D.
**Alternatives:** [DROP] Option A (status quo bundled) — incompatible with Phase-1 recalibration shipping without re-leaking the moat; second leak terminal for calibration IP. [DROP] Option B (server-only apply) — chain-of-reasoning requirement forces parameter return, which leaks calibration through normal operations faster than adversarial probing; "partial leak" in memo §2 upgraded to "kills B" in decision session. [DROP] Option D (compile-at-install per org) — over-engineered for v1; hash-pinned reproducibility not required by any known compliance constraint; support-matrix cost compounds per §4 item 3. Kept in reserve: C's manifest format designed so D migration is distribution-layer only, not schema. [DROP] Per-org calibration values in v1 — premature while base is GUESS/HEURISTIC; ships noise as signal. Schema hooks retained for v2. [DROP] Build endpoint sequentially before H2a starts — delays H2a for no benefit; parallel-build converges at ship time. [DROP] Running another council — memo §8 explicitly instructed against it; self-critique instead surfaced 3 real holes (chain-vs-manifest duplication, closed-by-design compliance question, over-granular 7-item chain) which were fixed directly.

## 2026-04-24: Corpus publishing budget — ≤100 public labeled tilts, ≤2 bundles per view family, no pillar integers
**Why:** Council step 3 from 2026-04-23 required a disclosure budget before any Phase-1 additions to the public regression corpus. Count of current public surface: 73 labeled integer tilts across 5 bundles in `2026-04-21/` (corpus is underdetermined for recovering multiplier values but near the boundary of safe disclosure for the extraction rule — macro-regime classifier and sector-sign recoverable, multipliers still opaque). Phase-1 additions without a cap would cross that boundary silently.
**Impact:** (1) Public regression corpus capped at ≤100 labeled tilts, additive to today's 73 — leaves room for ~27 more tilts ≈ one additional bundle. (2) ≤2 bundles per view family (no triple-repetition in any family — regression power scales with family-repetition). (3) Hard no-fly zones: no pillar integer scores public, no `sensitivity_bands.yaml`-adjacent artifacts, no derivation prose in `extraction_notes`, no per-company factor scores (already gitignored). (4) Unpublished families (sector-rotation, rates-duration, style-box-pure) — may add ONE bundle from any of these three; the other two stay private. **Flip conditions:** (a) Phase-1 recalibration (IP_AUDIT A6) lands with new multipliers — at that point 04-21 teaches the old rule, cap re-evaluated; (b) fork-activity signal suggests an adversary is already regressing on public corpus — cap drops to zero; (c) compliance returns "chain-of-reasoning must be disclosed" — budget becomes cap on *published* examples, private corpus can grow freely.
**Alternatives:** [DROP] No cap / publish-aggressively-as-RFC (Platform Thinker's council reframe) — rejected until corpus-leak analysis shows current public surface is already sufficient for fast-follower cloning; today's count (73) is near-boundary, not past it. [DROP] Cap at zero / freeze 04-21 and publish nothing else — rejected as over-correction; one additional bundle worth of new-family coverage has real product-credibility value and stays inside the leakage envelope.

## 2026-04-23 (PM): House-view diff harness shipped as spec-only; internal artifacts stay local
**Why:** Review of shipped artifacts found the original plan's public spec referenced internal implementation files (dangling pointers) and embedded an internal threshold literal. Internal-artifact audit came back benign (one existing fork is a bot snapshot pre-dating the house-view work; contains no house-view files). User directive mid-execution: protect additional internal material going forward.
**Impact:** (1) `/parallax-house-view-diff` skill rebranded as interface spec only — describes the dual-leg diff protocol shape, no executable invocation. (2) `tests/portfolio_diff_harness.md` + `tests/README.md` sanitized to remove calibration literals and internal-artifact name-drops. (3) Internal test fixtures, reference runner, calibration manifest, second regression baseline set, and universe candidate fixtures (which contain Parallax MCP output) all kept local via `.gitignore`. (4) Morning entry's core verdict (don't build `/hv/compile` as primary IP move) stands; this amendment clarifies execution shape. (5) License = MIT, no CLA — future artifact releases may need dual-license or private-sibling treatment. Pending: internal methodology skeleton, public-corpus budget count before further additions, future skill-vs-API-endpoint architecture decision for the house-view engine.
**Alternatives:** [DROP] Publishing scrubbed regression baselines + candidate fixtures — rejected on internal-exposure + scoring-corpus-slice grounds. [DROP] Shipping reference runner + calibration + methodology as one coherent public commit — rejected under user's maximalist-private directive. [DROP] Reframe as publish-everything-as-RFC (platform play) — deferred, not dropped; revisit if corpus-leak analysis shows current public surface already enables reverse-engineering. **Flip conditions:** (a) fork activity resumes or adversarial fork appears; (b) MIT grant becomes problematic as new artifacts ship (→ dual-license or private sibling); (c) corpus-leak analysis finds public surface is already sufficient for fast-follower cloning (→ reconsider RFC-publish stance). Full execution log in internal notes.

## 2026-04-23: Don't build `/hv/compile` as an IP-protection move
**Why:** Council + adversarial stress-test (Hunter/Skeptic/Referee, ruled B — stands with amendment). Repo already public on GitHub with 8 commits of loader.md history; the horse has bolted on v1 opacity. Stress-test Hunter #1 was decisive: the original verdict's step 3 (build `compile.py` in-skill) was solving a non-determinism problem not documented in any artifact — T3 e2e findings (JNJ→Eli Lilly mis-attribution, peer snapshot failures, empty-map redundancy) are server-side Parallax MCP bugs, NOT loader.md arithmetic flake.
**Impact:** (1) Run 30-min IP audit first — enumerate each asset as public/protected/unclear, classify `regression-corpus/baselines/` as IP or fixtures. (2) Compliance call across US/HK/SG/UK jurisdictions to confirm chain-of-reasoning disclosure requirements. (3) `skills/load-house-view/compile.py` is CONTINGENT on audit proving real loader.md arithmetic non-determinism; deferred absent evidence. (4) Re-parameterize Phase-1 calibration + move baselines server-side. IP protection focuses on calibration values, scoring corpus, and audit ledger rather than the math structure. Explicit: partially overrides user's "hide the magic" preference on feasibility grounds (repo already public, can't un-leak).
**Alternatives:** [DROP] Promote loader.md as "reference standard" — rejected. Only creates leverage when paired with established distribution gravity, which is not yet in place; publishing reference standards prematurely yields more to competitors than it captures. [DROP] Build `/hv/compile` server endpoint as IP play — rejected. Opaque directive bundles don't stop reverse-engineering (can be reverse-engineered through input perturbation), and client-side LLM rendering leaks schema via prose regardless. Creates distribution regression (API key + network + SOC2/data-residency scope). [DROP] Server-side compile as audit/determinism play — deferred to contingent step 3, not dropped. **Flip conditions:** (a) arithmetic non-determinism confirmed in loader.md interpretation (not server bugs); (b) any target jurisdiction prohibits chain-of-reasoning disclosure; (c) repo goes private or distribution model changes.

## 2026-04-22: Next increment = portfolio_diff_harness, not more schema
**Why:** Council (5 advisors + 3 peer reviewers + chairman, MCP domain, Standard scope) converged on End User's "Hello World before/after diff" as strongest proposal. The diff subsumes the invariant smoke-test (Threat Modeler), golden-path test (First Principles), and multiplier-math fixtures (Ship-It Engineer) — all in one user-visible artifact. Platform Thinker's portability vision is strategically right but premature: distributing a schema whose tilt chain hasn't been demonstrated exports the bug. Path A shipped the tilt taxonomy + canonical factors + region list, but no artifact proves any of it moves a portfolio — that is the entire product claim.
**Impact:** Create `skills/_parallax/house-view/tests/portfolio_diff_harness.md` — prose spec, Claude Code executes, no Python infra. Paired Parallax MCP calls (baseline vs view-applied) in same session against same candidate set. Emits side-by-side delta table + summary stats (median |shift|, max shift, sign-flips). Asserts shift inside declared sensitivity band per view. Ships with 5 regression bundles pre-wired. **Kill criterion:** if median shift falls below the threshold across all 5 bundles after next iteration, abandon the current tilt-to-weight architecture. Queued (not shipping now): tilt-to-weight translation hardening, portability layer, staleness guard, plain-English vocabulary pass, prose→YAML extraction hardening.
**Alternatives:** [DROP] Portability layer first (Persona 3) — rejected on timing, not merit. Exports potentially-broken tilt semantics to external publishers. Revisit after diff harness proves chain is load-bearing. [DROP] More schema additions before proving any tilt moves portfolio — rejected as adding appendages to untested engine. Ship-It Engineer's tilt-math fixtures kept as substrate under the diff, not as primary artifact. Peer reviewers flagged 3 unresolved concerns for follow-up: (1) baseline drift — Parallax MCP non-deterministic across sessions, paired-call protocol must be in-harness; (2) magnitude calibration — sensitivity band numbers are educated guesses, first 5 runs will recalibrate; (3) CIO YAML-authoring willingness — regression corpus was Claude-extracted from prose, not CIO-written.

## 2026-04-21: Phase 0 iteration path — schema-first week 1, loader-rewrite next sprint
**Why:** Dogfooding found Q-E schema rigidity (macro_regime confidence 0.30-0.40 on ESG/geopolitical samples) forces schema change anyway; doing (b) loader-rewrite first wastes ~300 LoC against a dying contract. Advisor 5 + peer review confirmed (a) is 1 day / 40 LoC / 5 files vs (b) at 1 sprint / 300+ LoC / 8+ files.
**Impact:** Week 1: optional macro_regime, add taiwan/korea regions, add reshoring theme, confirm free-form excludes, ship ground-truth panel + sector-count divergence assertion. Next sprint: per-tilt parallel universe calls + per-holding get_peer_snapshot aggregation. Phase 1 gate: 2 real CIO PDF stress-test. Upstream Parallax bugs (Q-B/Q-C) tracked separately; verdict flips if upstream ETA ≤10 business days.
**Alternatives:** [DROP] (e) promote to Phase 1 with known-limits docs — rejected because an audit trail of silently-wrong scorings is a compliance liability, not an asset. [DROP] (c) upstream-first / wait — rejected because no Parallax-team ETA exists. (d) PDF stress-test first deferred to Phase 1 gate, not Phase 0 exit. (a)+(b) vs. prose+commitments re-architecture: keep as dissent-worth-hearing; if 2-PDF stress test after (a)+(b) shows schema fighting real prose, revisit.

## 2026-05-02: OpenClaw Architecture — Skill-First for Externalized State
**Why:** Deliberation between stateful specialized agents vs. stateless atomic skills for the Marko OpenClaw meeting assistant. Council (Architect, Behavioral Strategist, Feasibility Analyst) converged on a **Skill-First** approach. In institutional B2B sales, "State" (Blue Sheets, CRM records, meeting notes) must live in the **Obsidian vault and HubSpot**, not in the hidden memory of an agent.
**Impact:**
- The Marko OpenClaw suite will be maintained as a collection of portable, stateless skills (e.g., `marko-prep-meeting`, `marko-autoreason`).
- Skills act as "stateless transformations" on ground-truth files, ensuring transparency and auditability for the user.
- **Portability:** Simplifies migration to OpenClaw (Marko's environment) as skills are self-contained markdown directories in `~/.claude/skills/`.
- **Composability:** A single powerful "OpenClaw" agent can "equip" these skills as tools, avoiding agent-sprawl.
- **Voice Consistency:** Achieved through a mandatory **Layer 8: Voice** context load (`chicago-global-voice`) in every skill, rather than per-agent system prompts.
**Alternatives:** [DROP] Role-based specialized agents (e.g., `marko-sales-analyst`) — rejected due to hidden state, complex symlink management, and agent-sprawl. [DROP] Storing deal state in agent memory — rejected; violates the "Source of Truth must be Obsidian/CRM" invariant.
**Flip conditions:** (a) the complexity of orchestrating 10+ skills exceeds a single agent's cognitive overhead; (b) a specific workflow requires sub-second real-time state that cannot be efficiently externalized to the file system.

## 2026-05-16: Tier 1 inline branding skills slated for convergence to integration-pattern.md
**Why:** PR feat/wire-branding-houseview-skills landed white-label branding wiring across 16 skills. Tier 1 pilots (client-review, due-diligence, deep-dive) inline the `load_visual_branding()` loader call + safe-key allowlist directly per a Pass-1 plan-eng-review decision to remove JIT-load skip risk on the highest-stakes user-facing path. The other 13 wired skills (cio-letter-prep retrofit + 12 Tier 2) use the sentinel + JIT-ref pattern against `_parallax/white-label/integration-pattern.md`. The split leaves 4 sources of truth long-term (1 canonical doc + 3 inline copies). Adversarial-reviewer flagged this in Pass-3 as acceptable for the PR but with a documented convergence follow-up so future loader-contract edits don't silently drift across the 4 places.
**Impact:**
- Follow-up PR scheduled: convert client-review, due-diligence, deep-dive from inline snippet to sentinel + JIT-ref pattern (same as Tier 2). End state: 1 canonical source (integration-pattern.md), 0 inline copies.
- Trigger criteria for the follow-up: (a) one week of stable operation post-merge with no Tier 1 wiring incidents, AND (b) 0 voice-leak observations across all wired consumers (the `load_visual_branding()` wrapper structurally prevents voice access — KeyError-on-access — so this should be 0 by construction).
- Drift-gate test (`skills/_parallax/white-label/tests/test_integration_pattern_referenced.py`) already covers Tier 1 because they retain the sentinel comment as metadata even with inline snippet. Convergence PR will remove the inline Python and add the JIT-ref directive in its place; the test continues to pass.
- The 5-line `load_visual_branding()` wrapper in `loader.py` stays unchanged — it is the structural enforcement layer, and the Tier 1 vs Tier 2 distinction is purely about whether the loader call site is inlined or JIT-loaded.
**Alternatives:** [DROP] Keep Tier 1 inline permanently — rejected on DRY grounds. Maintenance burden compounds: any future loader contract change must update 4 places. [DROP] Converge Tier 1 in the same PR — rejected on risk grounds. Pass-1 chose inline pilot specifically to validate JIT-load reliability empirically before broader propagation; reversing that during the same PR negates the validation.
**Flip conditions:** (a) Tier 1 inline copies drift from canonical doc within first 90 days post-merge (e.g., a maintenance edit modifies one of the three Tier 1 snippets without updating the others) — accelerate the convergence PR; (b) integration-pattern.md JIT-load proves unreliable in production (any observed instance of a Tier 2 skill rendering default-Parallax output when a valid client config was active) — postpone convergence and reconsider the shared-reference architecture entirely; (c) a new wiring requirement is added that needs Tier-1-vs-Tier-2 differentiation — re-evaluate whether the split should be permanent.

## 2026-05-17: integration-pattern.md §9 Versioning section — retroactive approval

**Why:** Surfaced by the dogfood run of `/analyze-implementation` on `feat/wire-branding-houseview-skills`. The §9 Versioning section was added in commit `1afa342` (the integration-pattern.md creation commit) without being listed in the locked plan (which specified §1–§8) and without a DECISIONS.md entry. Content is innocuous — §9 documents the additive-only key contract and points at `test_visual_branding_keys_are_subset_of_load_client_branding` as the structural drift gate. But the change discipline failed: an unlogged expansion is silent drift regardless of whether the addition is desirable.

**Impact:** Retroactively approved. `_VISUAL_BRANDING_KEYS` in `loader.py` is the machine-enforced source of truth; §3 of integration-pattern.md is its human-readable declaration; §9 is the policy statement that both must move together. Treating §9 as policy (not text) makes any future visual-key addition a three-place edit (constant + §3 list + §9 mention) gated by the loader subset test.

**Alternatives:** [DROP] Roll §9 back to remove the silent expansion — rejected; the section content is correct and useful, and removing it would degrade the contract documentation. [DROP] Promote §9 to a real `schema_version` field in `loader.py` — rejected for now; current additive-only discipline is sufficient. Revisit if a non-additive change is ever needed (e.g., key removal or rename).

**Flip conditions:** (a) any new visual key added without updating constant + §3 + §9 in the same commit → escalate to a real `schema_version` integer; (b) a non-additive change becomes necessary (key removed or renamed) → §9 must become a real versioning protocol, not just a policy note.

**Source:** Audit file `docs/analysis/2026-05-17-wire-branding-houseview-skills-analysis.md`.
