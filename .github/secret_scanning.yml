# GitHub native secret-scanning configuration.
#
# Excludes intentional test-only signing fixtures used by round-trip tests.
# See trusted_keys.json (`use: "test-only"`) and .gitleaks.toml for context.
paths-ignore:
  - 'skills/_parallax/house-view/tests/fixtures/test-signing-key.priv'
  - 'skills/_parallax/house-view/tests/fixtures/*.testonly.*'
